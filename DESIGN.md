# DESIGN（設計文書）

本ドキュメントは Hermit Works（`hw`）プラグインの設計思想と、主要な設計判断の内容・理由・
検討時に退けた代替案をまとめたものです。利用方法は [README.md](README.md)、保守環境・検証手順は
[DEVELOPMENT.md](DEVELOPMENT.md)、資材追加・変更の規約は [CONTRIBUTING.md](CONTRIBUTING.md) を
参照してください。本書はそれらと重複する記述を避け、「なぜそう設計したか」に絞って記します。

対象読者は、本プラグインを保守・拡張する開発者です。個々の手順やコマンド仕様ではなく、設計判断の
背景にある前提・制約・比較検討を追えるようにすることを目的とします。

## 目次

- 1. 全体設計思想
  - 1.1 組織メタファー
  - 1.2 一次受けの振り分けとレベルの使い分け
  - 1.3 計画ファイルの台帳化
  - 1.4 秩序より改善を優先する原則（成長原則）
- 2. 主要な設計判断
  - 2.1 エージェント編成と品質ゲートの段階化
  - 2.2 モデル割り当ての判断軸を役職ではなく判断の性質にした理由
  - 2.3 ガードレール設計
  - 2.4 三役審議
  - 2.5 作業ディレクトリ命名
  - 2.6 ライセンス選定
  - 2.7 表記統一の裁定基準
  - 2.8 エージェント追加のデフォルト却下原則
  - 2.9 定型作業のスクリプト/skill化優先
  - 2.10 バージョニングと変更履歴の運用方針
  - 2.11 判断エスカレーション手順（迷った場合の段階的判断）の導入
  - 2.12 手順資産の成長ループ（参照→実行→蒸留→昇格）と二段階蓄積
  - 2.13 利用者作成資材の初出適用時の利用者確認（適用判断のケースパターン化）
  - 2.14 スキル・手順資産の粒度基準と昇格後の分岐対応
  - 2.15 業務特性依存の標準手順の非同梱方針（release 系資材の撤去）
  - 2.16 外部進捗管理ツール連携は契約層のみ同梱
  - 2.17 初出パターン時の進め方確認（AI提案・カスタム・二人三脚・おまかせ）
  - 2.18 新規導入提案の評価ゲート（go/no-go）
  - 2.19 成長ループへのスクリプト化提案（部分的スクリプト化）
  - 2.20 管理番号の運用（番号対応表とセットで利用者と対話する）
  - 2.21 静的解析ツールの導入見送りとレビュー観点の明文化
  - 2.22 個人メモリへのチーム共有すべき事実の流出への対策（記録先の使い分けの条項提案）
  - 2.23 ブランチ運用・バージョン管理方針
  - 2.24 大型文書（CONTRIBUTING・DESIGN）分割の見送りと部分読み込み規約の採用
  - 2.25 AI資産の横展開（エクスポート/インポート）
  - 2.26 利用者向け出力・成果物文書の URL 区切り規約
  - 2.27 権限設定（permissions）パターン提案の注意
  - 2.28 秩序より改善を優先する原則の明文化とガードレール審査ゲート化
  - 2.29 共通ガードレール層の配置基準の規約化と棚卸し運用の導入
  - 2.30 共通ガードレール正典の lib 切り出しと同期スクリプトの新設
  - 2.31 バージョニング方針の改訂（リリース発火基準とMINOR/PATCHの再定義）
  - 2.32 ブランチ運用・リリース手順の改訂（develop ブランチ導入）
  - 2.33 保守運用の都合による配布資材の変更禁止
  - 2.34 サブエージェント実行上限到達時の縮退動作の規範化
  - 2.35 `context: fork` の条件付き採用と判定基準の規約化
  - 2.36 文書内パス参照の実在検証（validate.sh セクション12）と撤退線の事前宣言
  - 2.37 役割の重複検出における公式バンドルスキル軸の追加
  - 2.38 ai-prompt の評価規律: 複数回試行によるブレ幅測定
  - 2.39 品質ゲート受入時の「レビュー対象一覧 × 変更ファイル一覧」突合の必須化
  - 2.40 測定に基づく事実記録への測定条件（測り方・n・日付）の必須化
  - 2.41 確認手段の可用性による縮退の規範化
  - 2.42 qa-review 指摘様式への失敗シナリオ必須化（種別限定）と誤検出抑制条項の追加
  - 2.43 ヒアリング質問設計規律の導入（順序付け・提示形式・打ち切り）
  - 2.44 利用者資材レビューへのプロンプト希釈観点の追加（audit-assets 3軸）
  - 2.45 依頼の2分類（プラグインのメンテナンス／リポジトリの環境整備）の導入
  - 2.46 保守用スクリプトの `.claude/scripts/` 移設と配布/保守境界の再定義
  - 2.47 保守用ローカルコマンド・スキル（`.claude/commands/`・`.claude/skills/`）の規約整備
  - 2.48 許可リスト全体の衛生観点の追加
  - 2.49 qa-review 報告下限の明文化と品質ゲート網羅レビューの既定宣言
  - 2.50 集計結果の自己検査の作業方針への追加（ana-data・ana-metrics）
  - 2.51 qa-review「コメントの過不足」への判定軸の付与
  - 2.52 手順資産の形式への判断根拠（Why）欄の追加
- 3. 運用から得られた知見
- 4. 未決事項・将来の再検討ポイント
- 関連文書

目次は章・節の見出しを追加・削除・改題した際に同一コミットで更新してください。

---

## 1. 全体設計思想

### 1.1 組織メタファー

本プラグインは、単一の汎用エージェントに全業務を担わせるのではなく、10グループ・51名の
専門家エージェントに役割を分割し、「組織」としてモデル化している。狙いは次の3点である。

- **専門性の分離によるプロンプト品質の維持**: 1エージェントの担当領域を狭く保つことで、
  `description`・作業方針・連携条件を具体的に書ける。担当が広いほど記述は抽象化し、
  エージェント選択（ルーティング）の精度が落ちる。
- **司令塔と専門家の分離**: `mgmt-coordinator` と各グループの `*-lead` は自ら実装・執筆を行わず、
  判断と割り振りに専念する。実作業の質は専門家に委ね、司令塔は経路選択と統合の質に責任を持つ。
- **生成と評価の分離**: 変更を作る役割（実装系エージェント）と変更を評価する役割
  （`qa-review`・`sec-audit` 等）を分け、同一の主体が自分の成果物を無批判に合格させる構造を避ける。

これらは README の「ワークフロー設計（5原則）」（直列分解・ルーティング・並列化・司令塔と専門家・
評価ループ）として明文化されている。5原則は理念ではなく、9体の `*-lead` の定義ファイルの
`## 作業設計（ワークフロー5原則）` セクションに逐語一致で埋め込まれた実装規約である。`mgmt-coordinator`
にも同名セクションが補足付きで置かれている（詳細は CONTRIBUTING 2.4「リード級エージェントの追加
セクション」）。

### 1.2 一次受けの振り分けとレベルの使い分け

すべてのプロンプトを `/hw:request` の重い経路（計画→分担→品質ゲート）に乗せることはしない。
質問・軽い調査・軽微な1ファイル修正は直接応答してよいという振り分け方針を README（利用側 CLAUDE.md
向けのテンプレート）に明記している。ただし本リポジトリ自身の CLAUDE.md は、資材＝振る舞い定義という
特性から軽微な1ファイル修正の例外枠を設けていない。理由は次の2点である。

- 会話・質問にまで統括・計画・品質ゲートのオーバーヘッドを課すと、体験として重く、利用が敬遠される。
- hooks 等で「すべてを組織フローに乗せる」ことを機械的に強制する方式は採用していない。強制すれば
  軽い相談まで固定コストが乗り、判断の柔軟性を失うためである（この判断はガードレール設計にも
  一貫して現れる。2.3 節を参照）。

「迷う場合はどちらで進めるか一言添えてから着手する」という一文を振り分け方針の末尾に置いているのも、
機械的な線引きの限界を人（依頼者）の確認で補う設計である。

### 1.3 計画ファイルの台帳化

`.hw/plans/` の計画ファイルは、単なる作業計画ではなく「進捗台帳」と「各担当への委任指示書」を
兼ねる。背景にあるのは、長時間の対話でトークン上限に達し、会話履歴なしで作業を再開しなければならない
状況への対応である。設計上の要点は次のとおり。

- **進捗保存の粒度は委任タスク（ノード）単位に限定する。** タスク内のサブステップや経過ログは書かない。
  粒度を細かくするほど再開時の読み込みコストが増し、書き出しにもトークンを使うため、
  「中断からの再開に必要な最小限」に絞っている。
- **専用の進捗ファイルを新設せず、既存の計画ファイルに状態欄を持たせた。** 情報源を単一に保つことを
  優先し、`/hw:standup` 等の既存の参照先を変えずに済む設計にした。
- **状態値は「未着手／進行中／完了／中止」の4値。** 当初は3値（未着手/進行中/完了）で運用を始めたが、
  打ち切ったタスクが「未完了」のまま再開対象に残り続ける問題が判明し、「中止」を追加した
  （3値のままだと、打ち切り理由が記録できず、再開時に不要なタスクへ手を伸ばしてしまう）。
- **計画ファイルの更新権限は統括（`mgmt-coordinator`）のみに限定する。** 並列実行する複数のサブ
  エージェントに同じファイルを編集させると、編集衝突が起きるため。成果物ファイルは各担当が作成するが、
  計画ファイル自体の書き手は一本化している。
- **文体は簡潔さを優先する。** 計画ファイルは人間向けの読み物ではなくAI（Claude）の作業用ファイルと
  位置づけ、体言止め・記号・省略記法を許容し、丁寧さより誤解のなさとトークン節約を優先する。この
  文体規則は計画ファイルにのみ適用し、利用者向け報告・README等には適用しない。
- **冒頭に案件ヘッダー（背景 Why・案件の完了条件、各2行以内）を、変更を伴う案件ではさらに「スコープ外」欄
  （2行以内。タスク一覧と並べて、ガードレール欄と同じ位置に置く）を設ける。** 仕様駆動開発の知見（Why / What / Constraints / Out of Scope / Tasks の
  5要素で仕様の曖昧さを実装前に潰す）のうち、既存様式に欠けていた Why と Out of Scope を取り込んだ
  もの。Why と完了条件は、中断再開時と委任先が判断に迷った際の拠り所になる。スコープ外の明記は、
  並列委任される各担当が善意で依頼範囲の外へ手を広げる過剰実装を防ぎ、統合時・差し戻し時に範囲逸脱を
  判定する基準を与える。「長文の背景説明は書かない」方針とは行数上限（各2行以内）で両立させている。

生成と評価を分離する原則（1.1節）を、コストとのバランスで運用可能にするための仕組みが品質ゲートの
段階化である。詳細は 2.1 節で扱う。

### 1.4 秩序より改善を優先する原則（成長原則）

本プラグインの方針として採用すべき改善が、過去に定めた秩序（打ち止め・凍結方針・過去裁定等）を
理由に実行不能・見送りになるなら、その秩序自体を改訂・削除の対象とする。既存方針への抵触は、
それ単独では改善提案の棄却理由にしない。可否は実害範囲・検知手段・他層代替可否のメリット審査で
判断し、審査結果と根拠を記録する（成長＝変化なきものは淘汰されるという前提に立つ）。判断内容・
理由・検討時に退けた代替案は 2.28 節を参照。適用の詳細（審査基準・逐語コピー要件）は
[CONTRIBUTING.md](CONTRIBUTING.md) 1.4・1.8 を、プラグイン運用（利用者側規約・過去裁定との衝突時の
扱い）は `agents/mgmt-coordinator.md`「作業方針」節を正本とする。

---

## 2. 主要な設計判断

各判断について、判断内容・理由・検討時に退けた代替案を記す。

### 2.1 エージェント編成と品質ゲートの段階化

**判断**: 品質ゲート（`qa-review`・`sec-audit` 等の評価ノード起動）を、変更の性質に応じて段階化した。
レベル定義・判定手順・差し戻しループの運用は `agents/mgmt-coordinator.md`「品質ゲートの適用判定」節を
正本とし、本節ではその段階化に至った理由と検討時に退けた代替案のみを扱う。

**理由**: 全変更に評価ノードを無条件で2体起動する運用は、文書のみの変更のような低リスク作業にまで
固定コストを課していた。エージェント編成の多角的レビュー（`ana-domain`・`eng-lead`・`strat-lead` の
3観点並列評価）で、コスト削減の本丸は軽量ワーカー層の新設ではなく品質ゲートの段階化にあるという
結論が出たため、これを優先した。

**検討時に退けた代替案**:
- **実働部隊（`haiku` 層）の新設**による定型作業の低コスト化: 委任1回あたり数万トークンという
  固定費が定型作業自体の実行コストを上回り、スクリプト化・skill化の方が上位互換になるため不採用
  とした（2.9節も参照）。
- **品質ゲートを一律レベル1へ緩和**: 評価ノードを外すこと自体が「生成と評価の分離」原則を崩すため、
  緩和の方向ではなく「適用範囲を変更の性質で絞る」方向で設計した。
- **判定への第三者によるリアルタイムな二重チェック**: 判定の誤りは事後共有と定期振り返り
  （3か月または20案件ごとの `qa-lead` 判定基準見直し）で補う設計とし、コストに見合わないとして
  採用しなかった。

### 2.2 モデル割り当ての判断軸を役職ではなく判断の性質にした理由

**判断**: `opus` の採用基準を役職名（リーダーかどうか）ではなく、判断の性質（不可逆性・波及範囲）で
決める。判定フローの正本は CONTRIBUTING 2.5 であり、本節ではその判断軸を役職ではなく判断の性質に
置いた理由のみを扱う。

**理由**: `model` フィールドは特定の世代を指すものではなく階層エイリアスであり、世代交代に自動追従
する。したがって判断軸を「今のモデルの性能」ではなく「役割の恒常的な特性」に置いた。`eng-architect`
が非リーダーでありながら `opus` なのは、サービス境界・通信方式の選定誤りが後続の複数エージェント・
複数成果物のやり直しにつながる、不可逆性の高い判断だからである。逆にリーダー級であっても、
決定権を他へ委ねる助言的な役割であれば該当しない。

**検討時に退けた代替案**:
- **`mgmt-pm`・`biz-cs` の `haiku` 降格維持**: 当時（49体時点）の再評価では、`mgmt-pm` の
  進捗判定・完了報告の妥当性チェック、`biz-cs` のコード読解を伴うつまずき再現確認と対外文書
  （障害告知文）の作成は、いずれも「判断業務」に該当し `haiku` の能力設計（コンテキスト長・
  適応的思考の非対応）でラインを維持できないと判断し、`sonnet` に昇格した。「該当業務を他へ
  移して `haiku` を維持する」案も検討したが、移譲先が同格の `sonnet` エージェントであり、
  委任ホップが1つ増えるだけで純増になるため退けた。
- **`sonnet` 全体の一部 `haiku` 降格**: 「安全に降格できるエージェントほど呼び出し頻度が低く節約額が
  小さく、節約額が大きいエージェントほど高頻度で品質影響が大きい」という構造がある。加えて差し戻し
  ループ（上限3回）が平均1回増えるだけで `sonnet`→`haiku` の単価差による節約は相殺されるため、
  個別検討のうえ全員維持とした。
- **リーダー層の一部 `opus`→`sonnet` 降格**（例: 判断がほぼ可逆な `docs-lead`）: 節約絶対額の小ささと
  「リード職は全員 `opus`」という規約の単純さを優先し、見送った。

### 2.3 ガードレール設計

**判断**: エージェントの作業ガードレールを、①プロンプト記述（ソフト規範）②`frontmatter` の
`tools`/`disallowedTools`（エージェント単位のハード制約）③hooks/permissions/sandbox 設定
（利用側環境のハード制約）④CI/スクリプト検証（事後検出）の4層モデルで整理し、③はプラグイン
同梱による強制としては採用せず①②④を組み合わせる方針を採った（③は利用者へ提案する選択肢
としてのみ扱う）。全エージェント共通のソフト規範は「短文6行」の予算に収め、`.claude/scripts/validate.sh` に
よる逐語一致検証とセットで導入した。

**理由（脅威モデル）**: hermit-works は利用者本人の端末・権限で動作するため、主に想定すべき脅威は
「悪意ある攻撃者」ではなく「エージェント自身の誤作動・過剰な自発性」である（例外は、リポジトリ内
コンテンツや外部取得結果に埋め込まれた指示によるプロンプトインジェクションで、ここだけは第三者の
関与を想定する）。この前提のもとで、破壊的操作・秘密情報の取り扱い・信頼境界・資材の無断変更・
エージェント間の権威詐称の5テーマを、共通ガードレール文として全エージェントに一律で持たせることにした。

**共通ブロックを「短文6行」に絞った理由**: 層①の規範は増やすほど1件あたりの追従率が下がる
（instruction dilution）。破壊的操作・秘密情報等の4テーマはハード制約で代替できる見込みが当面ないため
追加は避けられないが、これ以上の希釈を避けるため、単独行の短文・例外は最小限という原則
（CONTRIBUTING 1.4）を設け、共通ブロックは「6行で予算を使い切っている」ことを明文化した。新規条項は
今後、追加ではなく既存6行との入替・統合を検討する運用とした。

**逐語一致検証を必須にした理由**: 「規範を書いたつもりが実は51ファイル中の一部にしか無い」状態は、
規範が無いより危険である（対策済みという誤った安心が生まれる）。`.claude/scripts/validate.sh` による機械的な
バイト一致検証をセットにすることで、新規追加・編集時の欠落・改変を必ず検出できるようにした。

**ハード制約の導入は実機検証を採用条件とした理由**: `disallowedTools` に書くべきツール名は
ドキュメント上「Task」と「Agent」（新称）が併記されうる状態にあり、名前が実環境と不一致だと
「制約は効かないまま対策済みの記録だけが残る」。これは無策より悪い。そのため、ハード制約を1件でも
採用する場合は「1エージェントで先行適用→実環境で拒否されることを確認→展開」という実機検証を
先頭タスクに置くことを設計原則とした。

**層③の内訳と「同梱しないが提案する」層として扱う理由**: 層③は hermit-works が配布物として
強制できない一方、利用者が自分の環境で設定すれば層①②では届かない範囲を塞げる。内訳は
permissions（Claude Code プロセス内・ツール呼び出し文字列の判定）、hooks（同プロセス内での
任意コード実行。上記のとおり同梱・強制はしない）、sandbox 設定（OS 境界で効き、sandbox 実行
コマンドとその全サブプロセスに適用される）の3つで、強制点の深さが異なる。とりわけ sandbox 設定は
permissions と同じ利用側設定でありながら、ツール呼び出し判定ではなく OS レベルの実行境界で効くため、
Bash パターンの前方一致による意図しない巻き込みやすり抜け（2.27）や、サブプロセス経由での迂回の
影響を受けない。hooks を不採用とした理由（逐次承認なしにコード実行権を得る手段であること）は
sandbox には及ばない。sandbox は設定値であり、コード実行権を与えないためである。層①②で塞ぎきれ
ない残余リスクに対する多層防御の選択肢として、`skills/permission-rules/SKILL.md` から利用者へ
提案する扱いとした。設定名・有効となる設定ソースの制限・適用範囲は同スキルを正本とし、本節では
位置づけのみを記す。

**sandbox に関する記述の裏取り水準とフェイルオープンの含意**: 本件の裏取りは公式ドキュメントの
確認に留まり、実機検証を伴わない（2026-08-04 時点、sandbox の依存ツールを導入できない環境であり
検証不能であった）。上記「ハード制約の導入は実機検証を採用条件とした」という条件は、hermit-works
自身が同梱する層②のハード制約に課したものであり、利用者へ提案するに留まる層③には直接適用
されないが、提案の推奨強度は実機検証が完了するまで抑える。また sandbox は依存ツール欠如時に
無効化されるフェイルオープンな性質を持ち、「制約は効かないまま対策済みの記録だけが残る。これは
無策より悪い」という本節が既に持つ失敗モードと同型である。したがって sandbox の追加を、層①②④の
規範・ハード制約・検証を削減・降格する根拠にしてはならない（加算にのみ用いる）。

**検討時に退けた代替案**:
- **プラグイン同梱 hooks による実行時強制（層③）**: 技術的には配布可能（プラグインを有効化する
  だけで自動的に有効になる）ことを確認したが、不採用とした。理由は、hooks は利用者の逐次承認なしに
  コード実行権を得る手段であり、配布経路（GitHub パス経由の直接配布、マーケットプレイス非公開の
  現体制）が侵害された場合に無承認コード実行へ直結するリスクを負うためである。また、README の
  「hooks 等で機械的に強制する方式は採用しない」という既存の方針（振り分けの文脈で先に定めていた）
  とも整合させた。hooks は「利用者側で作成する、または hermit-works から利用者へ提案する」方式に
  留め、プラグイン同梱による強制はしない。
- **sandbox 設定を独立した第5の層とする案**: 4層モデルの分割軸は「誰の管理下にある制約か・事前か
  事後か」であり、sandbox は (a) 利用者側 settings の設定値、(b) 実行前に効く制約という点で層③と
  同一の座標にある。sandbox が hooks/permissions と異なるのは「強制点の深さ」（プロセス内判定か
  OS 境界か）という別軸であり、これは層③の内訳を分ける軸にはなっても、①〜④と並ぶ序数を1つ増やす
  軸にはならない。新しい層は新しい脅威クラスか新しい制約の所有者が現れたときに立てる規律とし、
  緩和の強度差では層を増やさないこととした。既存の「層③」「層②/④」という序数参照（2.34節、
  CONTRIBUTING.md 1.5-5項・1.9等）を壊さない実務上の利点もあり、層③の内訳への追加（本節
  冒頭の判断）を採った。
- **全エージェントからの一律 `Agent` ツール剥奪**: リーダー層（`mgmt-coordinator`・`*-lead`）と
  三役審議・評価ループの評価者起動という正当な多段委任の手段を壊すため退け、非リーダー41体のみを
  対象とした。連携節には「変更を伴う依頼は原則として自ら起動せず、呼び出し元の判断に委ねる。
  ただし三役審議・評価ループの起動、読み取り専用の調査補助は例外」という実行主体の明文化を対で行い、
  frontmatter のハード制約と文面上の説明を両輪にした（2026-08-03 時点で Skill の context: fork 経由の
  迂回が残余リスクとして判明。詳細は 2.35 参照）。
- **役割クラス別の frontmatter ツール権限テンプレート**（例: 実装系/診断系/管理系で許可ツールの
  型を分ける）: 拡張の余地が乏しく分類を誤りやすいという評価を受け、見送った。
- **危険コマンドの Bash hook によるブロック、保護パスへの書き込み hook**: いずれも③の一部であり、
  hooks 不採用の判断（上記）に伴い保留とした。将来 hooks を採用する場合の再検討対象として記録するに
  留める。
- **診断系エージェント（`sec-audit` 等）からの `Bash` 剥奪**: `disallowedTools`/`tools` は
  「読み取り専用の Bash」を表現できず、既知CVE調査（`npm audit` 等）やコミット履歴走査
  （`git log`/`git grep`）に Bash が必須であるため、剥奪すれば診断能力そのものを失う。
  誤作動時の書き込み経路を1つ塞ぐ利益に見合わないため見送り、代わりに層①（プロンプト記述）で
  「Bash は読み取り系コマンドに限る」と明記する対応に留めた。`qa-review` のみ allowlist 化して
  いるのは、レビューが読み取りのみで完結し Bash 自体が不要なためであり、他の診断系エージェントとは
  前提が異なる。

### 2.4 三役審議

**判断**: 一部のリーダー級エージェント（判断の性質上、複数案の比較や不可逆な決定を扱う役割）に、
「理想主義者・現実主義者・批判主義者」という3つの視点役を並列に起動し、意見を集約したうえで
リーダー自身が裁定する仕組みを導入した。適用条件を「不可逆な判断・リリース可否判定・複数案の比較」
に限定し、日常的な判断には使わない。

**理由**: 単一視点での判断は見落としが生じやすい。特に、採否が後戻りしにくい判断（本プラグイン自身の
方針変更等）では、楽観的な理想像・実行可能性・リスクの3方向から検討させたうえでリーダーが決定する
ことで、判断の質を上げつつ、最終的な意思決定者を単一に保つ（多数決や全会一致を要求しない）。

**マージ規則**: 現実主義者の案を基準案とし、批判主義者の指摘は「具体的な失敗シナリオを伴うもの」に
限り必須反映、理想主義者の残提案は改善バックログとして残す。再審議は1回まで、裁定は
「採用／却下（理由付き）／リスクとして記録し保留」のいずれかに必ず分類する。

**検討時に退けた代替案**:
- **新規エージェントとしての登録**: 視点役を専任エージェント化すると、エージェント追加のデフォルト
  却下原則（2.8節）に照らして正当化が難しく、また判断の主体が分散しすぎる。人格プロンプトを与えた
  サブエージェントの一時的な並列起動という軽量な形にとどめた。
- **多数決・全会一致による決定**: 視点役はあくまで意見の生成者であり、決定権はリーダーに残す設計と
  した。合議制にすると責任の所在が曖昧になるため。

### 2.5 作業ディレクトリ命名

**判断**: エージェントの成果物を保存する作業ディレクトリ名を `.hw/`（`repo-map.md`・
`conventions.md`・`routines.md`・`decisions.md`・`plans/`・`reports/`・`docs/`）とした。

**理由**: プラグイン名 `hermit-works` に由来する名称は、リポジトリ直下に置くドットディレクトリとしては
長く、利用者リポジトリ内で毎回パスに現れることを踏まえ、プラグインの呼び出し名（`hw`）に合わせた
短い形に揃えた。プラグイン名自体・配布パスは変更していない。

**検討時に退けた代替案**: 特になし（呼称の短縮のみの変更であり、代替構造の比較検討は行っていない）。

### 2.6 ライセンス選定

**判断**: 本プラグインは CC BY-ND 4.0（表示 - 改変禁止）で公開し、著作権者が改変版の公開等について
個別に許諾できるデュアルライセンス体制を採る。外部からの貢献（Pull Request）は、著作権を貢献者に
残したまま著作権者へ利用・再許諾を認める許諾型 CLA の同意を条件とする。詳細は
[CONTRIBUTING.md](CONTRIBUTING.md) 7章を参照。

**理由**: 利用（複製・原型のままの再配布）は商用・非商用を問わず自由にしつつ、改変版の無秩序な公開・
再配布は防ぎたいという方針を反映している。CC BY-ND は「改変不可」だが、著作権者からの個別許諾で
改変版の公開を認められる余地を残すことで、原則は守りつつ将来の柔軟性（デュアルライセンス化）を
確保している。貢献者の著作権を譲渡させない許諾型 CLA は、貢献のハードルを不必要に上げない設計である。

### 2.7 表記統一の裁定基準

**判断**: 表記ゆれ（「等」／「など」、文体の統一、送り仮名、カタカナ長音、全角半角記号等）の統一は、
単純な多数決だけでなく、①既存の機械検証（`.claude/scripts/validate.sh` が逐語一致検証する正典文言）との
整合、②修正コストと実害のバランス、③意図的な使い分けかどうかの実例確認、を優先して裁定する。
裁定基準は CONTRIBUTING 6章に明文化している。

**理由**: 件数の多数派に単純に合わせると、（a）検証済みの正典文言（共通ガードレール文・連携節の
委任制限文等）を壊してしまう場合がある、（b）カタカナ長音のように語ごとに慣用が定着していて
一律ルール化がそもそも成立しない領域がある、（c）「等」と「など」のように、同じ用法（体言直後の
列挙）でも表記が割れており、用途別の使い分けルールが機械的にも人手でも判定できない場合がある、
という3つの事情があったため。単純な多数決ではなく、検証コスト・語ごとの実態・機能上の制約を
勘案する裁定へと切り替えた。

**判明した「ゆれではない」使い分けの例**: 「利用者」（hermit-works／対象リポジトリの利用者）と
「ユーザー」（各プロジェクトが提供するプロダクトのエンドユーザー）は指す対象が異なり、統一すると
意味を失う。`(a)(b)(c)` の半角丸括弧番号と、説明的な注釈括弧の全角括弧も、使い分けが一貫しており
統一対象ではない（後者は正典文言に含まれるため変更不可でもある）。文体（ですます調／である調）も、
「読者への説明・依頼はですます調、規範・手順そのものを述べる本文はである調」という文書内の役割分担
として説明でき、`agents/*.md`・`commands/*.md`・CONTRIBUTING 1.4節に共通する一貫パターンと確認できた。

**検討時に退けた代替案**: 表記ゆれ全件の一律機械置換（用法確認を省いた単純な多数決統一）は、
上記の理由により採らなかった。

### 2.8 エージェント追加のデフォルト却下原則

**判断**: 新規エージェントの追加はデフォルトで却下とし、CONTRIBUTING 1.1 の5基準
（境界の非吸収性・反復性・失敗実績・代替不能・恒久性）を**すべて**満たす場合に限り認める。

**理由**: エージェントは増えるほど呼び出し側の選択コストが上がり、境界の重複が組織全体のルーティング
精度を下げる。「あると便利」は追加理由にならない、という立場を取る。削除・改名は名前を対外的に
参照する箇所（`@hw:<name>` 呼び出し・他エージェントの連携節・README・commands・skills）が多く、
破壊的変更として扱う必要があるため、追加の判断は事前に慎重であるべきという設計である。

**運用実例**: 障害時の顧客対応を専任で担うエージェントの追加要望に対し、5基準で判定した結果
（反復的な委任シナリオが実例として3件そろわず、専任化しないと品質が落ちたという失敗実績も無かった
ため）追加を却下し、既存の `biz-cs`（担当業務の拡張）と `hw:incident` スキル（手順の補完）で
対応する裁定を行った。将来、失敗実績が蓄積されれば再判定する前提を残している。この事例は、
5基準が抽象的な理念に留まらず実際の追加可否判定に使われていることを示す。

**検討時に退けた代替案**: 「まず追加して様子を見る」という試行的な追加は、基準5（恒久性）で
明示的に不可としている。削除が破壊的変更で取り消しコストが高いため、試行目的の追加を認めない設計。

### 2.9 定型作業のスクリプト/skill化優先

**判断**: 判断を含まない定型手順ほどスクリプトへ、判断は要るが手順が定型なら skill へ、と実現手段を
寄せる。選び分けの対応表は CONTRIBUTING 1.2 を正本とし、本節ではこの優先順位を採った理由のみを扱う。
スクリプト・CI 定義を選んだ後の追加・変更手順は CONTRIBUTING 8章を参照。

**理由**: エージェント本文に書いた機械的な手順は実行のたびに揺らぎ、検証もできない。判断を含まない
手順ほどコードに寄せることで、再現性と機械検証可能性を確保する。組織図の三重管理
（README・`show-org` コマンド・各エージェント定義）によるドリフトを防ぐため生成スクリプト化した
経緯や、秘密情報スキャンを `.claude/scripts/validate.sh` に機械実装した経緯も、この優先順位に基づく。
一覧の再掲方式もこの優先順位から選び分ける。正本（`agents/*.md` の frontmatter 等）から機械的な
規則で逐語導出できる場合は生成スクリプトで埋め込み `.claude/scripts/validate.sh` で生成差分を検証し
（`/hw:show-org` の組織図）、正本（README「使い方」「スキル」表）から機械導出できない人手の
要約である場合は再掲を新設せず実行時に正本を読む（`/hw:help` のコマンド・スキル一覧）。判定は
「正本を1文字変えたとき、再掲側の正しい姿が一意に決まるか」で行う。

**検討時に退けた代替案**: 定型作業を専任エージェントの担当業務として書き下す方式は、2.8節の
判断基準（代替不能）に照らして基本的に却下される。既存エージェントの本文に手順を書き足す方式も、
スクリプト化が可能な限り採らず、エージェント本文からは「`.claude/scripts/<name>.sh` を実行する」という
参照のみに留める。

### 2.10 バージョニングと変更履歴の運用方針

**判断**: バージョニングは Semantic Versioning に準拠し、プラグインという特性に合わせて次を目安と
する。MAJOR は既存の呼び出し方・利用方法を破壊する変更（エージェント/スキル/コマンドの削除・改名、
必須 frontmatter キーの追加や意味変更等）、MINOR は後方互換を保った機能拡張（agent・command・skill の
追加等）、PATCH は利用方法に影響しない文言修正・バグ修正・ドキュメント整備。変更履歴
（`CHANGELOG.md`）はリリースの都度 `mgmt-release` が取りまとめて記載し、`.claude-plugin/plugin.json`
の `version` フィールドと必ずセットで更新する（片方のみの更新はリリース判定で不可とする）。

**理由**: コードを持たないプラグインでは「破壊的変更」の意味が API ではなく呼び出しインター
フェース（名前・frontmatter・コマンド仕様）に対応するため、SemVer の各段階を利用方法への影響で
定義し直した。エントリの記載を `mgmt-release` に一元化するのは取りまとめの一貫性のためで、実装
担当エージェントが個別に追記する方式は採らない。変更内容は原則としてコミット履歴・差分から
洗い出し、申告ベースでは記載しない。

**検討時に退けた代替案**: 実装担当が変更のたびに変更履歴へ追記する分散方式は、粒度・文体の
不一致と記載漏れ・重複が避けられないため採らなかった。

**追補（2026-08-01）**: 本節が定める MAJOR/MINOR/PATCH の基準、およびリリースをいつ発火するかの
契機は、2.31 により改訂した（利用者面資材への変更に限定したリリース発火基準の導入、MINOR/PATCH を
「利用者が変更を意識する必要があるか」で再定義）。本節の記載自体は当時の判断記録として無改変で
残し、現行の運用は 2.31・CONTRIBUTING 9.2（正本）を参照すること。

### 2.11 判断エスカレーション手順（迷った場合の段階的判断）の導入

**判断**: 規約・命名・表記等の慣習的な判断（技術的トレードオフの裁定を除く）に迷った場合、
`mgmt-coordinator` を除く全エージェント共通で次の3段階手順を踏む（CONTRIBUTING 1.8 が正典。
逐語一致の1行を `.claude/scripts/validate.sh` が検証する）。①文書化された定め（利用者資材＋
`.hw/decisions.md` の判断記録）に従う。②無ければリポジトリ内の類似例から法則性を抽出しつつ
デファクトスタンダードも必ず調査し、一致すれば根拠を明記して採用する。不一致の場合や類似例が
無い場合は独断で決めない。③判断材料・選択肢・推奨案を完了報告の確認事項に記載して呼び出し元へ
委ねる（可逆な判断は推奨案で仮採用と明示のうえ進めてよい）。`mgmt-coordinator` はこの共通文を
そのまま持たず、集約した確認事項を自ら利用者に確認し、結果を `.hw/decisions.md`（1判断1行の
軽量ADR）へ記録する独自の手順を持つ。

**理由**: 慣習的な判断のたびに呼び出し元へ確認を上げると往復コストが積み上がり、組織全体の
自律性が失われる。一方で各エージェントが独断で決めると、リポジトリ内にたまたま生じたパターンや
過去の誤りをそのまま正解として踏襲・拡散してしまうリスクがある。①→②→③の段階を踏むことで、
既に合意済みの判断（文書化された定め）は再確認を省いて往復コストを削減し、未確定の判断は
「類似例＋デファクトスタンダード調査」で裏取りしてから採否を分岐させることで内部の偶然パターンを
無条件には広めず、それでも不一致・前例なしの場合に限り人間の判断を仰ぐ（かつ可逆な判断は仮採用で
前進を止めない）という、自律性の確保と誤り伝播の防止を両立させる設計とした。

**検討時に退けた代替案**: (a) 迷ったら常に利用者へ直接確認する方式は、規約・表記等の頻出判断の
たびに呼び出し元の手を止め往復コストが高く、2.9節の「判断を含まない定型ほど機械・定型手順に
寄せる」という設計思想にも反するため採らなかった。(b) 固定の判断マトリクス（想定パターンを表で
事前列挙する方式）は、規約・命名・表記の慣習判断は対象が開放的で事前の網羅が現実的でないため
採らなかった。(c) 各エージェントが独断で最終決定する方式は、リポジトリ内の類似例が誤り・偶然の
産物である可能性を検証しないまま踏襲・拡散するリスクを排除できないため採らなかった（2.7節の
表記統一裁定基準が単純な多数決を退けた理由と同種の懸念による）。

### 2.12 手順資産の成長ループ（参照→実行→蒸留→昇格）と二段階蓄積

**判断**: 案件フローに「参照→実行→蒸留→昇格」の成長ループを組み込む。案件完了時に統括
（`mgmt-coordinator`）が定型性（再現性のある依頼パターンか。一度きりの調査・単発修正は除外）を
判定した場合、または利用者が明示的に指示した場合に、承認不要で `.hw/procedures/<件名>.md`
（手順資産）へ蒸留する。以降の案件では依頼解析時に手順資産と `.hw/conventions.md` を照合し、
合致すればゼロから計画を分解せずそれを計画のベースとする（利用者側に同種の業務手順・スキルが
あれば、そちらを優先する）（未昇格の手順資産のみが合致する場合の進め方確認は2.17節参照）。
蓄積は二段階とし、再利用実績（目安5回以上）を経て安定した手順資産
のみ、利用者の承認を得て `.claude/skills/` の正式スキルへ昇格する。詳細な形式・保存先・タスクの
持ち方は `agents/mgmt-coordinator.md`「作業方針」を正本とする（形式への判断根拠〔Why〕欄の
追加は2.52節参照）。

**理由**: 依頼のたびに利用者側のAI資産が成長していく状態を hermit-works の基本動作とすることで、
繰り返し依頼のたびに発生していたゼロからの計画分解コストを次回以降で下げる。一方で資産の質は
実際の再利用実績を経てから昇格させることで担保し、蒸留直後の未検証な手順がそのまま組織の正式
スキルとして固定化されることを防ぐ。目安を5回としているのは、2回程度では偶然の連続適用と本当に
再現性のある依頼パターンとを区別できず、使用パターンが蓄積する期間も確保できないためで、急ぎで
スキル化したい場合は利用者の明示指示により実績によらず昇格を提案できる。既存のAI資産・業務手順を
「すでに成長を経た状態」とみなして同じ参照・昇格の仕組みに乗せることで、新規に hermit-works を
導入する組織と、既存資産を持つ組織を区別せず同一機構で扱える（等価性）。

**検討時に退けた代替案**: (a) 全案件を自動的に蒸留する方式は、一度きりの調査・単発修正まで
資産化されノイズが乱立し、死蔵した手順資産の掃除負担が増すため採らなかった。(b) 蒸留を経ず
最初から `.claude/skills/` として生成する方式は、`.claude/` 変更の承認必須という既存ガードレール
により毎回承認往復が発生するうえ、一度きりの手順までスキル化候補として提示されてしまうため
採らなかった。(c) `.hw/` 配下のみで完結させ `.claude/skills/` への昇格を設けない方式は、
hermit-works を外した後もその資産を使い続けたい利用者にとってポータビリティが失われるため
採らなかった。

### 2.13 利用者作成資材の初出適用時の利用者確認（適用判断のケースパターン化）

**判断**: hw が利用者作成のスキル・コマンド・スクリプト等の利用者資材（hw が蒸留した
`.hw/procedures/` の手順資産、および利用者が今回の依頼で使用を明示的に指示した場合を除く）を
依頼に適用しようと判断したとき、`.hw/decisions.md` に当該「依頼パターン×資材」の適用判断の
記録が無ければ初出とみなし、統括が使用可否を利用者に確認したうえで結果を1行記録する。以降は
記録済みの「文書化された定め」として確認を省略し自動適用する（2.11節の判断エスカレーション
手順①と同じ扱い）。詳細な確認方法・記録形式は `agents/mgmt-coordinator.md`「作業方針」を
正本とする。

**理由**: 利用者資材は hw が品質・意図を保証できないため、初回適用の可否は利用者の判断事項と
すべきである。一方で `.hw/decisions.md` の判断記録機構に乗せることで、確認の往復を初回のみに
抑え、2回目以降は既存の判断エスカレーション手順と同様に自動化できる。

**検討時に退けた代替案**: (a) 常に確認なく自動適用する現行方式は、利用者の意図しない資材が
無断で依頼に適用されるリスクを排除できず採らなかった。(b) 毎回確認する方式は、同種の依頼が
繰り返されるたびに往復コストが積み上がり、ケースパターン化による省力化ができないため採らなかった。
(c) 一般エージェント共通条項（validate.sh 逐語検証行）の変更で全エージェントに確認義務を課す
方式は、逐語検証対象の全ファイル（現状51ファイル）を改変する割に、委任元の統括が確認済みの
資材のみを指示に含めれば足りるため採らなかった。

### 2.14 スキル・手順資産の粒度基準と昇格後の分岐対応

**判断**: スキル・手順資産の粒度は「1依頼パターン＝1手順資産（＝昇格後も1スキル）」の単位で保ち、
昇格は選択制とする（手順資産のまま留まることも正当な終着点であり劣化ではない）。昇格後の運用で
分岐パターンが発生した場合は二段階で対応を判定する。第1段は「適用条件が割れるか」で、適用条件は
同一で手順内に条件ステップが増えるだけの軽微な分岐であればスキルを修正する。第2段は、適用条件
そのものが割れる場合にさらに実績の有無で判定し、実績があり安定していれば別スキルへの切り出しを
提案し、実績がなく不安定であれば `.hw/procedures/` の手順資産として蒸留し、実績が付いてから昇格を
検討する。詳細な手続きは `agents/mgmt-coordinator.md`「作業方針」を正本とする。

**理由**: スキルの発動は description との照合によるため、複数の依頼パターンを1スキルに抱え込むほど
照合精度が落ち、読み込まれるコンテキストも増える。分岐の揺れをどこで吸収するかは、承認必須の
`.claude/skills/` 側ではなく承認不要の手順資産側で吸収する方が往復コストが低い。スキル化によって
得られる価値はフロー外での自動発動（description 照合）・利用者による直接利用・他リポジトリへの
可搬性であり、それらを要しない手順は手順資産のままで機能的に等価である。

**検討時に退けた代替案**: (a) 大きなスキルに複数の分岐パターンを内包させる方式は、description
照合の精度低下とスキル本文の肥大化を招くため採らなかった。(b) 分岐が発生するたびに即座に別スキルへ
切り出す方式は、実績のない不安定な分岐までスキルとして乱立させてしまうため採らなかった。
(c) スキル化そのものを廃し手順資産（手順書）のみで運用する方式は、フロー外での自動発動
（description 照合）と他リポジトリへの可搬性を失うため採らなかった。

### 2.15 業務特性依存の標準手順の非同梱方針（release 系資材の撤去）

**判断**: `/hw:release` コマンドと `hw:release-flow` スキルを撤去した。リリース判定・手順のように
業務特性への依存が強い標準手順はプラグインに同梱せず、2.12節の手順資産の成長ループ（参照→実行→
蒸留→昇格）に委ね、利用者側リポジトリで各自の業務に合わせて蒸留・昇格させて育てる方針とする。
あらかじめ組み込まれた定型手順は失うが、リリース準備を担う `mgmt-release` エージェントの判断能力は
失われない。

**理由**: リリース判定基準（何を満たせば出荷可とするか）・承認手続き（誰の承認を要するか）・
リリースノートの記載形式は、プラグイン側では一般化できず利用者の業務にしか存在しない。汎用的な
雛形として同梱すると、本来なら2.12節の成長ループで蒸留されるはずの利用者固有の手順が「既に
同梱品があるから」という理由で参照されるだけに留まり、利用者側の資産が育つ契機を奪ってしまう。
撤去してゼロから蒸留させる方が、この種の手順にとっては成長ループの意図に沿う。

**検討時に退けた代替案**: (a) 優先条項付き雛形（利用者資材があればそちらを優先する断り書きを
添えた同梱スキル）として残置する案は、release は判定基準そのものが業務依存であり雛形として
一般化できる骨格がほとんど残らないため採らなかった。同種の判断が必要になった `hw:incident` /
`hw:add-service` は、障害対応・サービス追加の骨格（連絡順序・確認観点等）が業務非依存の部分として
十分大きく残るため、今回はこの段階（優先条項付き雛形として同梱）に留める線引きとした。
(b) `/hw:review`（品質・セキュリティの総合レビュー）の同時撤去は、review がプラグイン自身の
レビューエージェント群（qa-review・sec-audit 等）を薄くオーケストレーションするだけで業務特性への
依存が弱く、検討の論点が「業務依存ゆえの非同梱」ではなく別種（機能の冗長性）であるため、本件の
対象からは除外した。

**2026-08-02 追補**: 標準手順を同梱しない方針の帰結として、利用者側にリリース手順が文書化されて
いない場合、`mgmt-release` はプラグイン側の暗黙の既定手順で代行せず、リリース手順の構築（ドラフト
提案→利用者合意→文書化）自体を最初の作業として提案し、合意前にリリース判定・操作を進めない
（利用者裁定 2026-08-02）。非同梱は「同梱品が無ければ独自に判断して埋める」ことまでは意味せず、
利用者側の手順が育つ契機を保つという本節の理由と整合させたものである。実体は
`agents/mgmt-release.md` の作業方針に反映する。

### 2.16 外部進捗管理ツール連携は契約層のみ同梱

**判断**: 外部進捗管理ツール（Backlog・Jira・GitHub・Trello等）との連携は、設定契約層（`.hw/tracker.md`
のスキーマ・妥当性検証・`/hw:request` への接続点の挿入）のみをプラグインに同梱し、ツール別のAPI実装
（接続層）とステータス遷移規則・コメント記法等の業務依存の運用規約（運用層）は同梱しない。主要な設計
判断は次の4点。①正本は2系統（計画・進捗は `.hw/plans/`、要求・議論は外部トラッカー側）を維持し、
hermit-works 側は取得日時付きスナップショットによる時点キャッシュに留める（2.9節「正本と再掲」の
判定に基づき、外部課題の全文複製・生成＋差分検証機構は新設しない）。②APIキーを直接扱う経路
（API直叩き）は既定で無効化し、利用者の明示的な有効化と初出承認があった場合のみ発火させる（露出面の
縮小）。③関係者情報は「共有すべき事実」「個人のローカルな主観」「書いてはいけない機微情報」の3層に
分離する。④業績評価等の主観判断はそのまま記録せず、宛先別の執筆規約（書き方の指針）に変換して保存
する。設定スキーマ・妥当性検証・許可/禁止リスト・厳格度プロファイル・投稿本文許可リスト等の詳細は
本書へ転記せず、正本（`skills/tracker-setup/SKILL.md`・`skills/tracker-sync/SKILL.md`・
`skills/stakeholders/SKILL.md` の各記載、および `.hw/decisions.md` 2026-07-30 の裁定記録2行
〔「外部進捗管理ツール連携の基本方針」「外部トラッカー連携 U1〜U10 の裁定」〕）に委ねる。

**理由**: ツール別クライアント実装を同梱すると個別APIの追従保守が発生し続け、2.15節「業務特性依存の
標準手順の非同梱方針」と同じ理由（プラグイン側では一般化できず、汎用同梱は利用者側の成長ループが
育つ契機を奪う）により、契約層を越えた同梱は避けた。設定・スキーマの正本は改変できない側（プラグイン
資材＝スキル本文）に一本化し、README・DESIGN への再掲は行わない。2.9節「正本を1文字変えたとき再掲側
の姿が一意に決まるか」の判定に照らすと、スキーマ・許可リストは人手判断を伴う要約的な性質であり、
機械導出可能な生成＋差分検証の対象ではない。よってREADME「作業ディレクトリ規約」にはファイル名の
索引のみを置き、本節にも設計判断の理由のみを記録する。

**検討時に退けた代替案**: (a) ツール別スキル（`hw:tracker-backlog`・`hw:tracker-jira` 等）を個別に
同梱する案は、API追従の保守負債が継続的に発生するうえ2.15節の非同梱方針と衝突するため採らなかった。
(b) 外部ツールを唯一の正本とし `.hw/` 側の設定・スナップショットを廃止する案は、課題への書き込み
権限者の範囲に応じた既定の切替（厳格度プロファイル）やガードレール（非信頼入力宣言・投稿前承認等）
の粒度を `.hw/` 側の資材なしに表現できないため採らなかった。(c) 契約層自体を設けず利用者の任意実装に
委ねる案（現状維持）は、接続点の規約が存在しないまま利用者実装が乱立し、正本2系統の前提（2.9節）や
関係者情報3層の保護が個別実装ごとに再発明・再検討される非効率を招くため採らなかった。

### 2.17 初出パターン時の進め方確認（AI提案・カスタム・二人三脚・おまかせ）

**判断**: `/hw:request` の依頼解析時、合致する手順資産・スキルの無い初出の依頼パターンでは、
計画立案前に統括が進め方をAI提案／カスタム／二人三脚／おまかせの4択（この順で固定）で利用者に
確認する。合致するのが未昇格の手順資産（`.hw/procedures/`）のみの場合は、AI提案フローの中で
当該手順資産を推奨（先頭）案として提示し、この手順で進めてよいか確認できるようにする。合致する
正式スキルがある場合は従来どおり確認なしで計画のベースに使う（利用者資材の初出確認〔2.13節〕は
既存ルールのまま）。非対話実行（定期実行等、利用者に質問できない文脈）ではおまかせ相当として続行
し、その旨を計画ファイルに記録する。`/hw:plan` は対象外（成果物が計画そのものであり利用者レビュー
が前提のため）。詳細な確認方法・選択肢の定義・記録形式は `agents/mgmt-coordinator.md`「作業方針」を
正本とする。

**理由**: 初出の依頼パターンは、参照できる手順資産・スキルが無く計画品質が最も不確実な局面であり、
統括が単独で進め方を決め切ったまま走ると利用者の意図とずれるリスクが高い。一方で2.12節の成長ループ
により蒸留が進むほど「合致あり」の案件が増え、本確認は自然に発生頻度が下がるため、導入初期に確認の
発火頻度が高くなること自体は、手順資産の蓄積と並行する一時的なコストとして許容する裁定である。
おまかせを最後尾に置くのは、以後の確認なしに走り切る性質上、齟齬が生じたときの手戻りリスクが4択の
中で最大であるため。本節は2.13節（利用者作成資材の初出適用時の確認）と対称をなす、「依頼パターン
そのものの初出」に対する確認である。

**検討時に退けた代替案**: (a) 従来どおり確認なしで統括が単独判断する方式は、初出パターンで進め方が
利用者の意図とずれたまま走り切ってしまうリスクを排除できないため採らなかった。(b) 手順資産・スキル
の合致有無によらず全案件で毎回確認する方式は、定型案件でも往復が発生し、2.11節の判断エスカレーション
手順が目指す往復コスト削減の方針に反するため採らなかった（手順資産・スキル合致時に確認を省くことで
対応した）。(c) 案件の規模・リスクの大小で確認発火の条件を絞り込む方式は、線引きの基準自体が複雑化
するうえ、2.12節の蒸留により発火頻度は自然に減衰していくため、追加の条件分岐は不要と判断し
採らなかった。

### 2.18 新規導入提案の評価ゲート（go/no-go）

**判断**: 新規導入提案（現行リポジトリ・プロダクトに存在しない技術・ライブラリ・アーキテクチャ・
仕組み等の導入依頼で、導入の合意が `.hw/decisions.md` 等に文書化されていないもの）は、計画立案の
前に strat／AI／アーキテクト系エージェントによる評価（採用推奨／条件付き採用／見送り推奨のいずれかの
所見と、根拠・主要リスク・概算規模）を経て、利用者の採用判断を仰ぐ。採用（条件付き含む）と
判断された場合のみ計画へ進み、見送り・保留の場合は評価成果物の報告をもって完了とする。判断結果は
`.hw/decisions.md` に記録し、採用済みテーマの再依頼は2回目以降本ゲートを通さず通常経路へ進む。
非対話実行時は評価まで実施し、採用判断（go/no-go）は利用者専権であるため保留のまま実装へは
進まない。詳細な定義・評価担当の選定方法・確認手順は `agents/mgmt-coordinator.md`「作業方針」を
正本とする。

**理由**: 変更依頼の形を取る新規導入提案は、従来 go/no-go 評価を経ずに新機能の経路（要件→設計→
実装→QA）へそのまま進んでおり、導入の価値・実現性が未評価のまま実装コストが先に発生していた。
本ゲートが問う「やるべきか（Whether）」は、2.17節が問う「どう進めるか（How）」と異なる次元の
判断であり、両者は相補の関係にある（順序は本ゲートが先）。相談・質問形式の依頼であれば従来から
評価のみで完結していたところ、依頼の言い回し（「導入すべきか」と「導入して」）だけで評価の
有無が分かれてしまう非対称が生じていたため、これを解消する裁定である。

**検討時に退けた代替案**: (a) 全ての変更依頼で毎回本ゲートを発火させる方式は、既存合意に基づく
実装・改修にまで評価往復が生じ、2.11節が目指す往復コスト削減の方針に反するため採らなかった
（導入合意の文書化有無で対象を絞ることで対応した）。(b) 2.17節の進め方確認の4択に5つ目の
選択肢として統合する方式は、Whether（やるべきか）と How（どう進めるか）という異なる次元の判断が
1つの確認に混在してしまい、見送り判断時には進め方の選択自体が無意味になるため、独立したゲートと
した。(c) 統括自身が評価まで完結させ利用者確認を省く方式は、生成者と評価者の分離・go/no-go は
利用者専権という原則に反するため採らなかった。

### 2.19 成長ループへのスクリプト化提案（部分的スクリプト化）

**判断**: 2.12節の成長ループ（参照→実行→蒸留→昇格）に、手順資産の蒸留時および再利用時を発火点
として、手順に含まれる判断を含まない機械的ステップ（検証・集計・整形・生成・一括置換等）を
スクリプト化する分岐を追加した（利用者裁定 2026-07-31）。手順全体ではなく該当ステップのみを
対象とする部分的スクリプト化を可とする。当初は作成を毎回提案→承認としたが、同日中の利用者裁定2
（2026-07-31）により、昇格前（親の手順資産が `.hw/procedures/` にとどまる段階）で配置先が
`.hw/scripts/` 内に限られる場合は、蒸留と同型で承認不要のまま作成まで実施してよい扱いに置き換えた
（完了報告に明記し、利用者の削除依頼で戻せる）。提案→承認が必須なのは、利用者側規約の場所
（`scripts/` 等）への配置（スキル昇格時の移動提案を含む）に限る。見送り・削除は記録し、以後は
利用者の明示指示がない限り同一候補を再作成・再提案しない。詳細な発火条件・提案文言・記録形式は
`agents/mgmt-coordinator.md`「作業方針」を正本とする。

**理由**: 2.9節「定型作業のスクリプト/skill化優先」が示す、判断を含まない機械的手順はコードに
寄せることで再現性と機械検証可能性を確保するという思想を、hermit-works 自身の資材だけでなく
利用側フローにも展開した。エージェント本文・手順資産に書いた機械的手順は実行のたびに揺らぎ、
検証もできず、都度トークンも消費する。蒸留・再利用という反復が実証された接点に発火点を絞ることで、
提案の頻度を抑えノイズ化を避けた。承認要否の線引きは「利用者の資材空間に出るか」に置く。
`.hw/scripts/` は hw の作業領域であり、蒸留（`.hw/procedures/` への保存）が承認不要であるのと
同じ性質を持つため、そこへの機械的ステップの切り出しにまで提案・承認の往復を挟むと、機械的作業を
コードへ寄せる契機自体が失われやすい。一方、利用者側規約の場所への配置は利用者の資材空間そのものへの
書き込みであり、従来どおり提案・承認を要する。

**検討時に退けた代替案**: (a) 蒸留時のみを発火点とする案は、蒸留時点で見送った候補がその後の
再利用局面で再提案されないため採らなかった。(b) 案件の計画・実行の都度提案する案は、検出機会は
最大になるが確認往復が増えフローが重くなり、2.11節が目指す往復コスト削減の方針にも反するため
採らなかった。(c) 利用者リポジトリの `scripts/` 等、hw由来ではないディレクトリへ直接配置する案は、
hw由来資材と利用者資材の境界が曖昧になるため採らなかった（既定を `.hw/scripts/` とし、裁定2でも
この線引きは維持している）。(d) 昇格前も `.hw/scripts/` への作成まで毎回承認を求める当初案は、
往復コストがスクリプト化の利点を上回るとして、利用者裁定2により撤回した。

### 2.20 管理番号の運用（番号対応表とセットで利用者と対話する）

**判断**: 利用者向け出力（スタンドアップ・定期レポート・案件完了報告・計画要約等）で管理番号（計画の
タスク連番 T1…、調査・レポートの項目番号 B1/C1/J1 等、その他資材内で定義される既存の管理番号）を使う
場合、番号→内容（一言説明）＋出典（番号を定義するファイルのパス）の「番号対応表」を添える運用を導入
した（利用者裁定 2026-07-31）。対応表の提示後は、同一会話・同一レポート系列内で番号ベースの簡潔なや
り取りを受け付けてよい。既存の番号体系・採番方法は変えず、あくまで提示方法の規約であって新たな採番制
度ではない。AI間でのみやり取りする内部報告（利用者が直接読まない中間成果物）は対象外とする。この除外
は報告の呼称（「完了報告」等）ではなく宛先（利用者が直接読むか、AI間でのみやり取りされるか）で判定す
るものであり、呼称が「完了報告」であっても利用者へ返す最終報告（案件完了報告等）は対象に含まれる。詳
細な規約・対応表の書式は `agents/mgmt-coordinator.md`「出力」を正本とする。

**理由**: 長い課題名・タスク名を利用者とのやり取りのたびに書き下すのは冗長で、双方の負荷になる。
一方で番号だけで対話すると、番号が指す対象を利用者が把握できない、あるいは出典の異なる番号
（複数の計画・複数のレポート系列）が同席する場面で取り違えが起きるリスクがある。番号対応表を
必須の添付物とすることで、番号による効率化と、対象を取り違えないための出典明示を両立させる裁定
である。番号は出典文書内でのみ一意という既存の性質（計画のT番号、レポートのB/C/J番号等、文書単位
の採番）を変えずに前提とすることで、既存の各種番号体系への影響を最小化した。

**検討時に退けた代替案**: (a) 番号を使わず毎回正式名称・課題名で書き下す案は、これまでどおり
確実だが冗長で、番号ベースの効率化という目的自体が失われるため採らなかった。(b) 恒久的な横断
採番台帳を新設し、全ての管理番号をそこに一元化する案は、計画のタスク連番・レポートの項目番号等、
既存の文書単位の採番と二重管理になり、かえって対応関係の管理コストが増すため採らなかった（出典
文書内一意という現行の性質を維持することを優先した）。(c) 番号対応表を省き番号のみでやり取りする
案は、往復コストは最小になるが、番号は出典文書内でのみ一意であるため複数文書の番号が併存する
場面で取り違えのリスクを排除できず、出典明示という利用者裁定の趣旨に反するため採らなかった。

### 2.21 静的解析ツールの導入見送りとレビュー観点の明文化

**判断**: シェルスクリプト向け静的解析ツール（shellcheck）の導入は見送り（利用者裁定 2026-07-31、
2.18節の新規導入提案の評価ゲートを経た判断）、代替として `agents/qa-review.md`「担当業務」節に
シェルスクリプト観点（quoting・エラー処理の意図妥当性・外部入力/git ref の値検証・trap クリーン
アップ・パイプ/サブシェルの変数スコープ）を明文化した。判定結果・根拠の正本は `.hw/decisions.md`
の B1 行（判断記録）と `agents/qa-review.md`「担当業務」節（今回の実装成果物）であり、本節では
その要旨と検討時に退けた代替案のみを扱う（評価過程の詳細は参考として `.hw/reports/` の評価書
`2026-07-31-b1-shellcheck-eval-{infra,strat}.md` に残るが、これらは gitignore 対象の環境ローカル
成果物であり正本ではない）。

**理由**: 過去の是正実績を突き合わせると、shellcheck が直接検知できたと言えるのは1件のみで、
残りは「抑止の意図が妥当か」「引用漏れの文脈的な要否」等、機械検知ではなく人手判断を要する種類で
あった。本件は2.18節の新規導入提案の評価ゲートが対象とする「導入の合意が文書化されていない新規
導入依頼」そのものであり、評価ゲートを経て利用者が見送りを裁定した（2.8節「エージェント追加の
デフォルト却下原則」の反復性・失敗実績要件と同様の考え方が、この見送り判断にも通じる）。一方、
過去の是正はいずれも人手レビュー（`qa-review`）で検出・是正済みという実績があるため、静的解析
ツールという新規依存を追加するのではなく、既に実効性のある人手レビューの観点を構造化する方を
コストに見合う対応として選んだ。

**検討時に退けた代替案**: (a) shellcheck 導入（形態B: devcontainer 同梱 + CI、
`.claude/scripts/validate.sh` は変更しない）は、追加コストが実質ゼロ（Dockerfile 1行・CI追加取得なし）と
評価されたが、便益（過去是正のうち機械検知可能は1件のみ）が導入動機を裏付けるには弱く、コストの
小ささのみを理由に採用しなかった（スクリプト規模の拡大、または観点表運用後に同種の不具合が再発
した場合は再評価対象とする）。(b) `bash -n` 等の標準機能による構文チェックは、現状のテスト実行
（`.claude/scripts/tests/run-*.sh`）で構文エラーが実行時に必ず露見しており、追加できる限界的価値が実質
ゼロのため採らなかった。

### 2.22 個人メモリへのチーム共有すべき事実の流出への対策（記録先の使い分けの条項提案）

**判断**: チーム共有すべき事実（決定・規約・運用ルール・案件の背景等）が個人メモリ（Claude Code
の自動メモリ）に流れる問題への対策は、利用側 CLAUDE.md への「記録先の使い分け」条項の承認付き
提案（プロンプト誘導）で行う（利用者裁定 2026-07-31）。`/hw:init` が未記載時に条項追記を提案し、
`/hw:audit-assets` が既存の利用側資材にこの条項があるか、チーム共有すべき事実が個人メモリ頼み
の運用になっていないかを点検する。既存項目（一次受け振り分け方針）と同形式で、利用者の承認なし
に追記しない運用とする。条項例文・点検観点の詳細な文言は `commands/init.md` 手順5・
`commands/audit-assets.md` 手順2（ai-prompt ルーティング）を正本とし、本節では再掲しない。

**理由**: プラグインは Claude Code 本体のメモリ機能に直接介入できず、利用側 CLAUDE.md は
セッションのたびに常時読み込まれる資材であるため、誘導点として最も筋が良い。また「共有すべき
事実」を記録する受け皿（`.hw/decisions.md`・`.hw/conventions.md`・`.hw/docs/` 等）は README
「作業ディレクトリ規約」節・`commands/init.md` の初期セットアップで既に整備されており、一方
「共有すべき事実」「個人のローカルな主観」の区分自体は `skills/stakeholders/SKILL.md` に既存
であるため、これらを流用すれば責務の拡張は「使い分けを促す条項の追加」のみで済む（機微情報層
は本件の対象外）。

**検討時に退けた代替案**: フック（PreToolUse でメモリディレクトリへの書き込みを検知して警告・
ブロックする）によるハード制御は採らなかった。本プラグインはフックを同梱しておらず、フック
導入は保守・検証の責務を一段増やすこと、フックは書き込み内容が「チーム共有すべき事実」か
「個人のローカルな主観」かを判定できず、個人的な内容の保存にも毎回摩擦が生じることが理由で
ある。これは README「使い方」節の「コマンド未指定プロンプトの扱い（一次受けの振り分け）」が
振り分け方針を hooks で機械的に強制しない既存方針と同型の判断である。

**改訂（2026-08-02〜03）**: 条項例文に、利用者から指摘・訂正・裁定を受けて記録する際は**個人メモリへ
書き込む前に**共有要否を判断する旨（保存前判断）を追加し、`/hw:audit-assets` の点検観点にも同判断の
有無を加えた（利用者依頼 2026-08-02〜03）。契機は、本プラグインの保守セッション自身が利用者の裁定を
まず個人メモリへ保存し、「チームで共有すべき考え方」との指摘を受けて共有文書へ移し直した実事例。
記録先の静的な使い分け（どこに置くか）だけでは書き込み時点の判断が誘導されないため、タイミング
（保存前）を条項に明文化した。あわせて、共有すべき内容が既存の規約・手順の誤り・不足に起因する
場合の反映先は `.hw/` への記録ではなく当該資材（CLAUDE.md・スキル・コマンド・業務手順等）の修正を
第一候補とする旨を条項に含めた（利用者指摘 2026-08-03。記録は資材に紐づかない決定・事実の受け皿で
あり、資材の振る舞いへの訂正を記録で代替すると資材と実運用が乖離するため。訂正を資材へ反映して
乖離を防ぐ方向は 2.12 の成長ループとも通じる）。なお本リポジトリの保守セッション向けには、
CLAUDE.md「記録先の使い分け」節に同趣旨の自己適用版を置く（そちらの正本は当該節自身）。
エージェント共通ガードレール層（常時層等）への追加は採らなかった。
自動メモリへ書き込む主体は利用側 CLAUDE.md を常時読み込む主セッションであり条項ルートで経路を
網羅できること、常時層の希釈コスト（CONTRIBUTING 1.4・本書 2.28）を避けることが理由である。

### 2.23 ブランチ運用・バージョン管理方針

**判断**: 資材（エージェント・コマンド・スキル・スクリプト・CI・文書）の変更は main への直接コミット
を禁止し、`feature/<topic>`（開発環境まわりの変更は `chore/<topic>`）ブランチで行った上で squash
マージ（または1コミットに整えた上でのマージ）する。version（`.claude-plugin/plugin.json`）・
CHANGELOG はブランチ作業では更新せず、独立したリリース作業として main 上で一括更新する（version
更新・タグ付け・CHANGELOG コミット・push は、既定方針どおり利用者の明示指示があるまで実施しない）。
並行して複数の案件・ブランチが進む場合は `git worktree` で作業ツリーを分離する。ブランチ命名・
マージ手順・後発マージ側の rebase・生成物競合時の再生成・ブランチ掃除・リリース手順の具体的な
操作列は CONTRIBUTING 9章を正本とする。

**理由**: 本プラグインは GitHub パス配布のため、マーケットプレイスの審査やタグ取得のような配布
ゲートを経由せず、利用者は任意時点の main を直接参照する。したがって main は常時シッパブルな状態を
保つ必要があり、CONTRIBUTING 5章の検証必須がその状態の担保として機能するのは、変更が必ずブランチ経由で検証を
通過してからマージされる場合に限られる。version はプラグインの呼び出しインターフェースを取得する
ゲートではなく、変更内容を利用者へ伝える目印（2.10節）であるため、コミットの都度更新する必然性は
なく、CHANGELOG に書く価値のあるまとまりができた区切りで一括更新すれば足りる。また、並行セッション
が日常的に発生する運用である以上、作業ツリーの分離を明文化しないと、ブランチ切り替えに伴う作業
内容の取り違え・巻き戻しのリスクが残る。squash マージにより main 上の履歴の粒度は案件単位に粗く
なるが、ブランチ内のコミット履歴自体は必要に応じて参照できるため、この粒度低下は許容する。

**検討時に退けた代替案**: (a) マージのたびに version を更新する方式は、並行して走る複数ブランチの
間で `plugin.json` の `version` フィールドが恒常的に競合し、マージのたびに手作業の解消コストが
発生するため採らなかった。(b) changesets 方式（各ブランチ・変更に変更内容を記した断片ファイルを
積み、リリース時にまとめて集約する）は、断片ファイルの管理・集約という追加の仕組みを常設すること
になり、変更頻度・貢献者数がまだ小さい本リポジトリの規模には過剰であるため見送った。

**2026-08-02 改訂での位置づけ**: 本節が記す「main 上でリリースコミットを一括して行う」運用は、
2026-08-02 の裁定で `develop` ブランチを導入し、作業ブランチの分岐元・PR 先を `develop` に、
リリースを `develop`→`main` のリリース PR 方式に改めたことで置き換わった。新方針の判断・理由・
検討時に退けた代替案は 2.32 を参照してください（正本は CONTRIBUTING 9章）。

### 2.24 大型文書（CONTRIBUTING・DESIGN）分割の見送りと部分読み込み規約の採用

**判断**: CONTRIBUTING.md・DESIGN.md の分割は見送り（利用者裁定 2026-07-31）、代替として保守案件で
これらの大型文書を参照する際は、委任側（統括）が章・節番号を指定し、読む側は指定された章・節のみを
部分読み込みする規約を採用した（全文読みは、文書横断の整合確認等スコープ上必要な場合に限る）。規約
の詳細は CONTRIBUTING 1章を正本とする。

**理由**: これらの文書は利用者への配布物ではなく保守セッションでのみ消費されるため、分割によって
得られる読み込みコスト削減は、部分読み込みの徹底でも同等に得られる（全文読み1回1.5〜2万トークン
相当に対し、該当章のみの部分読みは2〜3千トークン相当）。加えて、両文書への参照は本プラグイン内の
文書・スクリプトに50箇所超、DESIGN 2.x 節への直接参照だけでも10箇所超存在し（いずれも 2026-07-31
時点の簡易 grep 集計）、`.claude/scripts/validate.sh` も章番号への依存を持つ。分割はこの既存の参照網とスクリプトの前提を広範囲で壊すが、部分読み込みの規約化は参照網
に手を入れずに実施できる。

**検討時に退けた代替案**: (a) DESIGN.md の分割は、各節が短く狙い読みで足りること、DESIGN 2.x 節への
既存の参照慣行が10箇所超存在し分割によってこれらの参照が壊れることから採らなかった。(b)
CONTRIBUTING.md の章分割は、規約としては成立しうるが移行コストが大きいため、部分読み込み規約の
効果を実測したうえで要否を判断する第2弾の検討事項として保留した。

### 2.25 AI資産の横展開（エクスポート/インポート）

**判断**: 成長ループ（2.12節）で育った手順資産・スクリプト・昇格済みスキルを他リポジトリへ持ち出す
`hw:export-assets`・`hw:import-assets` の対スキルを新設した。対象は `.hw/procedures/`・
`.hw/scripts/`・昇格済みスキル（`.claude/skills/`）に限り、`.hw/stakeholders.md`・`.hw/tracker.md`・
案件履歴（`plans/`・`reports/`・`decisions.md` 等）はリポジトリ固有情報として固定除外する。展開先では
手順資産として実績リセットのうえ取り込み、同期はせず一回きりシード＋手動再取り込み（import 再実行）
とする。詳細な手順（分類提示・混在資産の切り離し・4択の衝突解決・非信頼入力の扱い等）は正本である
`skills/export-assets/SKILL.md`・`skills/import-assets/SKILL.md` に委譲する。

**理由**: 2.15節の非同梱方針により、業務特性に依存する手順は最初から同梱せず利用者側で蒸留・昇格
させている。この結果、価値ある手順資産・スキルは各リポジトリで個別に育つが、複数リポジトリを運用
する利用者にとっては同じ手順をゼロから育て直すコストが繰り返し発生する。2.12節の成長ループと2.15節
の非同梱方針を維持したまま、この重複コストのみを解消するには、育った資産を「実績リセットして手順
資産から育て直す」形で持ち出す機構が整合する。展開先で昇格済みスキルをそのまま昇格状態で持ち込まず
手順資産へ降格させるのも、2.12節が定める「二段階蓄積」（実績を経てからの昇格）の原則を展開先でも
保つためである。

**検討時に退けた代替案**: (a) 共有資産リポジトリを立てて各リポジトリから参照する方式は、参照先の
可用性・バージョン整合をプラグイン側で保証できず、単独作業前提（2.16節の外部連携と同様の思想）から
外れるため採らなかった。(b) 展開元・展開先の自動同期は、2.15節の非同梱方針と同様、利用者ごとの資産
の変化を機械的に追従させると却って意図しない上書き・競合を招くため採らず、一回きりシード＋手動
再取り込みに留めた。(c) 衝突時の無人マージは、手順資産の記述が自然言語であり機械的なマージでは
意味的な整合を保証できないため採らず、AI草案＋利用者確認を必須とした（マージ判断基準・出自履歴の
統合・共通祖先がある場合の3-way比較・マージ後資産の品質ゲート扱いの高度化は4章の未決事項とする）。

### 2.26 利用者向け出力・成果物文書の URL 区切り規約

**判断**: 回答・成果物文書に URL を記載する際、裸 URL は前後を空白・改行で区切り、直後に句読点・
助詞等の文字を直接続けない規約を導入した（利用者裁定 2026-08-01、依頼会話＋AskUserQuestion）。
可能な限り markdown リンク `[表示名](URL)` または `<URL>` を用いる。適用範囲は
統括（`mgmt-coordinator`）＋文書系エージェント（docs-lead/docs-tech/docs-user/docs-l10n）の成果物
文書・回答の双方とする。利用者向け出力を持つ commands は、`mgmt-coordinator` ペルソナを明示宣言する
もの（report/request/audit-assets/draft-docs/optimize-assets 等）は正本「出力」節の追記がそのまま
適用されるため追加テキストなしで充足し、既存の参照文を持つもの（plan 等）もその参照文で充足する
（適用範囲外の standup 等を除き、新規の参照文挿入は行わない。全エージェントへの共通行化は採らなかった。
理由は後述）。サブエージェント間でのみやり取りする内部報告（利用者が
直接読まない中間成果物）は、2.20節の管理番号運用と同じ整理で対象外とする。詳細な規約文言は
`agents/mgmt-coordinator.md`「出力」節を正本とする。

**理由**: 利用者が実際に踏んだ事象は、回答中の URL 直後に文字が続いて誤った URL 扱いになりリンク
先へ遷移できないというものであり、発生箇所は統括の回答に限らず文書系エージェントの成果物・回答も
含まれる疑いがあった。そのため統括のみへの適用では対策として不十分であり、文書系エージェントを
適用範囲に含めた。一方で、全エージェント共通の適用は、2.3節の instruction dilution（3章）のコストと、
共通行の追加審査基準（CONTRIBUTING 1.8・2.28）に照らして本規約の重要度に見合わないと判断し、
URL を含む利用者向け出力を実際に生成する範囲（統括＋文書系＋対象 commands）に限定した。

**検討時に退けた代替案**: (a) 全エージェント共通行として追加し `.claude/scripts/validate.sh` で逐語検証する
案は、適用漏れのない完全カバーが得られる一方、共通行の追加審査基準（CONTRIBUTING 1.8・2.28）に
照らし、instruction dilution のコストが本規約の重要度に見合わないため採らなかった。(b) 統括＋commands
出力のみに絞る案は、追加コストが最小になる一方、文書系エージェントの成果物・回答が適用範囲外となり、
利用者が事象を観測したのは文書系エージェントの出力である疑いがあったため、対策として不十分と判断し
採らなかった。いずれも利用者裁定（2026-08-01）で退けられた。

**追補（2026-08-04・commands 挿入0件判断の訂正と機械検証化）**: 上記「利用者向け出力を持つ commands
は `mgmt-coordinator` ペルソナの明示宣言・既存の参照文で充足し、追加テキストなしで足りる」という
当初判断は、2026-08-04 に統括自身が `/hw:request` のメイン会話実行での PR 報告中で URL を `**` 装飾＋
全角括弧で挟みリンク不能にする規約違反を犯したことで、誤りと確定した（利用者指摘 2026-08-04）。
原因は、ペルソナ宣言（「`mgmt-coordinator` として振る舞え」）が定義ファイルの規範を自動では運ばない
ことにある。エージェント定義の全文がシステムプロンプトに載るのはサブエージェント起動時のみであり、
利用者が最も頻繁に使う経路であるコマンド→メイン会話実行では、`agents/mgmt-coordinator.md`「出力」節
は一度もコンテキストに載らない。加えて、URL 区切り規約行は当時 `.claude/scripts/validate.sh` の逐語検証対象
外であり、「展開したつもり」を検出する層が無かった（2.3節が戒める、規範を書いたつもりが一部にしか
無い状態は規範が無いより危険な状態の再現）。是正として、commands 全12ファイルへ自己完結の1行を
再掲し、正典定数を `.claude/scripts/lib/guidelines.sh` に置いた上で `.claude/scripts/validate.sh` に逐語存在検証を
追加した（`.hw/plans/2026-08-04-url-rule-commands-enforcement.md`）。文言正本は
`.claude/scripts/lib/guidelines.sh` の当該定数、規約そのものの正本は従来どおり `agents/mgmt-coordinator.md`
「出力」節のままとする。あわせて2点を本追補で訂正・拡張した。第一に、本文で「適用範囲外」とした
standup 等を含め、利用者向け出力を持つ commands は全件を対象とする（適用範囲を「規約の適用対象
ペルソナか」ではなく「利用者向け出力を生成する経路か」で判定し直した）。第二に、品質ゲートの指摘を
受け、正本と docs 系4体の既存行にも今回の事故形態を示す装飾記号の具体例（`**` 強調・全角括弧）を
追記し、実行経路間（メイン会話／サブエージェント）の文言非対称を解消した。教訓として、ペルソナ宣言に依存
した規範適用はコマンド→メイン会話という実行経路では成立しない。メイン会話経路にも効かせたい規範は、
commands 側への再掲と機械検証（逐語存在検証）を要する。上記「検討時に退けた代替案」の記述はいずれも
当時の判断記録として無改変で残す。

### 2.27 権限設定（permissions）パターン提案の注意

**判断**: 導入先で sec-audit 相当の提案 `deny: Bash(git push --force*)` が前方一致により
`--force-with-lease` を巻き込み、正当な運用（force-with-lease による push）が停止する事故が
発生した。原因は末尾 `*` の直前にスペースが無いパターンが単語境界を持たず、より長いオプション名
まで巻き込む前方一致であることの見落としと、既存 deny 3行の書き分け（前任による切り分け）を
差分確認せず丸ごと置換したことの2点である。Claude Code の permissions（`.claude/settings.json`
等）を提案・変更しうる `agents/sec-audit.md`・`agents/sec-appsec.md`・`agents/infra-devops.md`
の3ファイルに、同一の注意ブロック（5項目の箇条書き）を逐語一致で追加した。

**仕様の裏取り**: 資材へ書く内容は、公式ドキュメント（Claude Code の permissions/settings
リファレンス）の確認と、ローカル環境でのヘッドレス実行（`claude -p`）による実機検証の両方が
一致した事実のみに限定した。ドキュメントに具体例のない「スペース付きパターンが引数なしの単体
コマンドにも一致するか」等の項目は実機検証で解消してから反映した。裏取りの経緯・検証ケースは
`.hw/artifacts/2026-08-01-permissions-spec.md` に残す。

**配置判断**: 対象3ファイルの `## 作業方針` 末尾（判断エスカレーション手順の直前）へ逐語一致で
追加する方式を採った。全エージェント共通の1.5節ガードレール6行ブロックへの追加は、1.4節の設計
原則2「この共通ブロックは短文6行で予算を使い切っている」に抵触するため見送った。また、権限設定
の提案・変更に関与しない大多数のエージェントにまで一般化すると、希釈（instruction dilution。
1.4節）により当該注意の実効性がかえって薄まるため、permissions を扱う3エージェントに限定した。

**検討時に退けた代替案**: 3ファイル間の逐語一致を `.claude/scripts/validate.sh` に静的検証として追加する
案は、スコープを最小化するため見送った（CONTRIBUTING 2.4「三役審議」の2ファイル同期と同方式で、
変更時の目視確認に委ねる）。是正案・deny 提案自体の妥当性は、品質ゲートの sec-audit（診断）・
qa-review の観点確認、および `commands/audit-assets.md` 手順2 / `commands/optimize-assets.md`
手順3 の permissions レビュー観点で継続的に点検する。

**追補（2026-08-01・ハイブリッド構成への移行）**: 上記5項目ブロックの逐語コピー方式は、3ファイル
（合計15行）を占有し続ける点でコンテキスト圧縮の余地があった。そこで詳細規則・確認手順を新設
スキル `skills/permission-rules/SKILL.md` へ移し、3ファイル側は検証済みトリガー行1行
（「Claude Code の権限設定（`.claude/settings.json` 等の `permissions`）を提案・変更・レビューする際は、
作業前に必ず `hw:permission-rules` スキルを読み込み、そのガイダンスに従う。」＝配置した逐語文）
のみを保持するハイブリッド構成に移行した。

移行は2.3「ハード制約の導入は実機検証を採用条件とする」に従い、クリーン環境（汚染源のない模擬
利用者リポジトリ、中立プロンプト、実サブエージェント委任）での実機検証を経てから採用した。
検証は3試行を経ており、1・2回目は統括審査で却下（委任のロールプレイ代替・循環検証・生ログ不足・
対照実験の環境汚染等の不備）、3回目に統括が直接実施した検証で採用基準を満たした。結果は
サブエージェントからのスキル参照が可能（項目1=可）、検証済みトリガー行1行で7/7試行が初手で
`hw:permission-rules` に到達しミスファイアなし（項目2=可）。参照方式は `${CLAUDE_PLUGIN_ROOT}`
によるプラグイン内ファイルの直接参照ではなく、パス非依存の Skill ツール（スキル名参照）を採用した
（利用者側リポジトリでは cwd にプラグインのファイルが存在せず参照ファイル方式が使えないため）。
残余リスク（発火はモデルの指示追従に依存する確率的挙動でありハーネスによる強制ではない点、
sec-appsec は個別未試行だが同一機構である点、スキル到達後も提案パターン形が検証済み形から逸脱しう
る点）は、スキル本文の確認手順（検証済みパターン形の使用を明示）と品質ゲート（qa-review +
sec-audit）で緩和する方針とした。検証成果物は
`.hw/artifacts/2026-08-01-permission-skill-verification.md`（確定版。却下した2回分の経緯を含む）。

**追補（2026-08-01・validate.sh 検証対象化）**: 上記追補の時点では、トリガー行1行の3ファイル間
一致は目視確認のみに委ねていた。共通ルール管理の成長対応案件（CONTRIBUTING 1.4 のトリガー行新設
要件(ii)）に伴い、`.claude/scripts/validate.sh` セクション9(h)（`GUIDELINE_PERMISSION_TRIGGER_LINE` 定数）
による逐語検証を追加し、以後は欠落・改変を機械的に検出する。旧前提（トリガー行は機械検証対象外）
を参照していた記述が CONTRIBUTING.md 2.4「複数ファイル間の逐語同期ブロック」に残存していたため、
同節も本案件で更新済みである。経緯の詳細は 2.29 を参照。

### 2.28 秩序より改善を優先する原則の明文化とガードレール審査ゲート化

**判断**: 「秩序より改善を優先する（成長原則）」を1.4節に宣言として新設した。あわせて
CONTRIBUTING 1.8 の「別枠の共通行追加は原則打ち止め」という定めを「審査ゲート」として再定義し、
1.4 にも同旨の条項（原則6）を追加した。既存方針・過去裁定（本プラグインの内部規約に限らず、
利用者側の規約・過去裁定を含む）への抵触は、それ単独では改善提案の棄却理由にしない。可否は
実害範囲・検知手段・他層代替可否のメリット審査で判断し、審査結果と根拠を記録する運用に統一した
（審査基準・逐語コピー要件は CONTRIBUTING 1.4・1.8 を正本とし、本節は決定の経緯のみ記す。
プラグイン運用への適用は `agents/mgmt-coordinator.md`「作業方針」節を正本とする）。

**理由**: 「打ち止め」「凍結」という文言は、額面どおりに読むと運用上「既存の枠を使い切っている」
こと自体が新規提案の自動的な却下根拠になりうる。しかし枠を使い切っている事実と、個々の提案の
メリット（実害範囲・検知手段・他層代替可否）は別の論点であり、前者だけで後者の審査を省略すると、
本来採用すべき改善が「秩序の維持」を理由に機械的に見送られるリスクが生じる。過去の裁定・凍結
方針は、その時点の判断としては正当であっても、状況（脅威モデル・利用実績・制約）の変化により
見直しを要することがある。既存秩序を無条件の拒否根拠にしない運用へ切り替えることで、改善提案の
可否を常にメリット審査に基づかせ、必要であれば秩序側（既存方針）を改訂・削除する対象とみなす
ことを明文化した。

**実在制約との関係**: instruction dilution（層①規範の希釈。2.3節）という制約自体は、本節の
運用変更によって消えるものではない。共通ブロックの「6行/1行」予算制約は実在するリスクに基づく
ものであり、維持する。本節が変えたのは「予算を使い切っている」ことを理由にした無審査の棄却を
禁止し、追加提案は既存条項との入替・統合の検討を含むメリット審査を経て可否判断する、という
手続き面である。審査の結果、追加を却下する（＝実質的に従来と同じ結論になる）ことも当然ありうる。

**検討時に退けた代替案**:
- **打ち止め方針の完全削除**（6行/1行の予算制約自体を撤廃する）: instruction dilution の制約は
  方針を削除しても実在し続けるため、予算制約自体をなくす選択は採らなかった。予算を空ける・増やす
  経路（共通ブロックの定期棚卸し、hooks 層③採用の再評価）は将来の検討課題として4章に残す
  （追補 2026-08-01: このうち棚卸しは CONTRIBUTING 1.9 として運用化した。2.29節参照。
  hooks 層③再評価は未決のまま4章に残る）。
- **個人メモリへの記録**: 本判断のようなチーム（プラグイン運用）で共有すべき方針変更を、記録先の
  使い分け方針（2.22節）に反して個人メモリに記録する案は、共有すべき事実が個人に閉じてしまうため
  不採用とした。

### 2.29 共通ガードレール層の配置基準の規約化と棚卸し運用の導入

**判断**: 2.28 の審査ゲート化を前提に、新規の共通ルールをどの層・形式に置くかの判定順（機械層 →
トリガー行＋スキル層 → 常時層）と、常時層・トリガー行層の定期的な棚卸し運用を導入した。2.27
追補で採用したハイブリッド構成（permission-rules トリガー行）を、以後の新設案件が従うべき標準の
既定先と位置付け、トリガー行を新設する際の3要件（実機検証・`.claude/scripts/validate.sh` への逐語検証
追加・対象エージェントの限定）を明文化した。詳細な判定順・要件・棚卸しの発動条件と点検内容は
CONTRIBUTING.md 1.4・1.9 を正本とし、本節は決定の経緯のみ記す。

**理由**: 1.8 の審査ゲート化（2.28）により「既存方針への抵触は単独棄却理由にしない」運用へ移行した
結果、今後は個別提案ごとに配置先（層①〜④のどこか、常時層かトリガー行かなど）の判断基準が繰り返し
問われることが見込まれた。2.27 追補で先行実施した「詳細規則をスキルへ委譲しトリガー行1行のみを
層①に残す」構成は、実機検証（クリーン環境・実サブエージェント委任・7/7到達）を経た前例であり、
これを個別事例のまま留め置かず、以後の新設案件が参照できる標準の判定順として規約化した。また、
審査ゲート化だけでは既存の常時層・トリガー行が陳腐化・重複した場合に誰がいつ点検するかが定まって
いなかったため、審査発生時（1.8）と定期振り返り（品質ゲート判定基準の定期振り返り。3か月または
20案件ごと・`qa-lead`）に相乗りする棚卸し運用を CONTRIBUTING 1.9 として新設した。

**検討時に退けた代替案**:
- **正本定数のスクリプト内 lib への切り出し＋同期スクリプトの新設**（依頼元の再検討報告で提示された
  案A〔配置基準の規約化〕・案B〔正本定数のlib切り出し＋同期スクリプト〕・案C〔棚卸し運用〕のうち
  A・Cのみ採用）: 正本定数は現状 `.claude/scripts/validate.sh` 単一ファイル内に閉じており重複同期の実害が
  顕在化していないこと、lib切り出しはスクリプトの実行方式・読み込み順の見直しを伴い本案件（配置
  基準・棚卸し運用という文書側の規約整備）よりスコープが大きいことから、同時実施は見送り別案件と
  した。
- **常時層の棚卸しを審査ゲート（1.8）のみに委ね、定期棚卸しを設けない**: 審査ゲートは新規追加の
  都度にしか発火しないため、追加提案が来ない期間に既存行が陳腐化しても点検の契機がない。4章に
  残していた「共通ブロックの予算を空ける経路」を放置し続けることになるため、品質ゲート判定基準の
  定期振り返りに相乗りする経路を併設した。

### 2.30 共通ガードレール正典の lib 切り出しと同期スクリプトの新設

**判断**: 2.29 では、依頼元の再検討報告が提示した案A（配置基準の規約化）・案B（正本定数の
スクリプト内 lib への切り出し＋同期スクリプトの新設）・案C（棚卸し運用）のうちA・Cのみを採用し、
Bは別案件とした（2.29「検討時に退けた代替案」参照）。本節は、そのBを実施した記録である。逐語検証
用の文言定数（`GUIDELINE_COMMON_LINES`・`GUIDELINE_ANCHOR_LINE`・`GUIDELINE_TK2_E1_LINE`・
`GUIDELINE_INJECTION_NOTE_LINE`・`GUIDELINE_DECISION_PROCEDURE_LINE`・
`GUIDELINE_EXTERNAL_INPUT_NOTE_LINE`・`GUIDELINE_SNAPSHOT_TEMPLATE_NOTE_LINES`・
`GUIDELINE_IMPORT_UNTRUSTED_INPUT_NOTE_LINE`・`GUIDELINE_PERMISSION_TRIGGER_LINE` と対象ファイル
リスト定数）を、値を1バイトも変えずに `.claude/scripts/validate.sh` から `.claude/scripts/lib/guidelines.sh` へ
移設し、`.claude/scripts/validate.sh` は `SCRIPT_DIR` 相対でこれを source する構成にした。あわせて、
正典の文言を変更した際に対象ファイル群へ機械的に反映する `.claude/scripts/sync-guidelines.sh` を新設した。
以後、正典＝lib（`.claude/scripts/lib/guidelines.sh`）・検証＝`.claude/scripts/validate.sh`・伝播＝
`.claude/scripts/sync-guidelines.sh` の3分掌とする（CONTRIBUTING 1.5・1.8・5章参照）。

**理由**: 逐語一致検証の対象ファイルは現状51ファイルに及ぶ（2.3節参照）。共通行の文言を1件変更する
たびに手編集で51ファイルへ反映する運用は、2.29時点では「重複同期の実害が顕在化していない」として
見送りの根拠になっていたが、1.8の審査ゲート運用が定着し常時層の変更提案自体は今後も起こりうる
前提に立つと、変更のたびに51ファイルを手編集するコストと入力ミスのリスクは無視できない。lib切り出し
により、文言変更の作業は「正典1箇所（lib）の編集＋`.claude/scripts/sync-guidelines.sh` の実行」に短縮され、
1.8 の審査ゲート（別枠の共通行追加の可否審査）が変更コストに代わる唯一のブレーキとして機能する
構成になった。

**validate.sh 内定数方式の旧設計判断との関係**: 2.3節が定めたハード制約導入の実機検証原則、および
1.5節・1.8節が定める逐語一致検証そのものは変更していない。`.claude/scripts/validate.sh` が正典の実体を
直接保持していた状態から、`SCRIPT_DIR` 相対 source によって正典の実体だけをlibへ分離したもので、
`.claude/scripts/tests/` が validate.sh を独立ディレクトリへ複製して検証する既存の仕組み（フィクスチャ
独立性・テスト独立性という validate.sh 側の設計意図）とは競合せず両立する（`REPO_ROOT` 相対にせず
`SCRIPT_DIR` 相対を選んだ理由の詳細は `.claude/scripts/validate.sh` 冒頭コメント参照）。

**検討時に退けた代替案**:
- **2.29 時点の判断を据え置き、lib切り出しを見送り続ける**: 上記「理由」のとおり、51ファイルの
  手編集運用を維持する根拠（重複同期の実害が顕在化していない）が薄れたと判断し、見送りの解除を
  選んだ。
- **`.claude/scripts/sync-guidelines.sh` を CI・hook から自動実行する**: 正典変更の伝播を保守者の確認なしに
  自動反映すると、置換の失敗（旧文言が対象ファイルに見つからない等）を無人で握りつぶすリスクがある。
  本案件では保守者が明示実行する道具に留め、CI・hook等からの自動実行は組み込まなかった（自動化の
  要否は4章の未決事項として残す）。
- **`REPO_ROOT` 相対での source**: `.claude/scripts/tests/` が validate.sh を独立ディレクトリへ複製して
  検証する既存のテスト構造と整合しないため、`SCRIPT_DIR` 相対を採用した。

### 2.31 バージョニング方針の改訂（リリース発火基準とMINOR/PATCHの再定義）

**判断**: 2.10 の運用を次のとおり改訂する。(1) **リリース発火基準**: 利用者面資材
（`agents/`・`commands/`・`skills/`・`.claude-plugin/plugin.json`・`README.md`）に変更が及ぶ
場合に限りリリース（バージョン更新）を発火し、保守面資材（`.claude/scripts/`・`.github/`・
`CONTRIBUTING.md`・`DESIGN.md`・`DEVELOPMENT.md`・`CLAUDE.md`）のみの変更は次の利用者面リリースへ
同乗させる（両面が混在する変更はリリース側として扱う。「発火なし」はコミット自体が main へ載らず
配布されないという意味ではない）。(2) **MINOR/PATCH の再定義**: MAJOR は従来どおり呼び出し
インターフェースの破壊とし、MINOR を「呼び出しインターフェースは不変でも利用者が変更を意識する
必要がある場合」、PATCH を「意識する必要がない場合」と再定義する。判定に迷う場合は MINOR に倒す。
(3) **操作的判定テスト**: MINOR/PATCH の判定代理として「CHANGELOG に利用者向けの行として書くべき
内容があるか」を用いる。手順の具体は CONTRIBUTING 9.2（正本）に委譲する。

**理由**: v0.4.0 判定（2026-08-01、`.hw/reports/2026-08-01-release-draft.md` 2章）で、
`mgmt-coordinator.md` への振る舞い変更1点を根拠に MINOR と推奨したが、「hw プラグインの呼び出し
インターフェースは無変更」「保守スクリプトは利用者が直接使う資材ではない」という PATCH 側の反論とも
両論併記になり、判定が割れた。原因は2点ある。第一に、旧2.10の MAJOR/MINOR/PATCH の定義が「呼び出し
インターフェースへの影響」のみを基準にしており、開発規約・保守スクリプトの実質的な変更量（新しい
運用手続きの新設等）を評価する軸を持たなかったこと。第二に、保守面のみの変更が利用者面の変更と
同列にリリース発火の対象になっており、リリースの意味（利用者へ変更を伝える目印。2.10「理由」参照）
と、実際に発火する契機がずれていたこと。V-1でリリース発火をそもそも利用者面の変更に限定し、V-2で
MINOR/PATCH を「利用者が変更を意識する必要があるか」という統一の軸に揃えることで、この2点を同時に
解消する。V-3 の操作的テストは、この軸を判定者ごとにブレなく適用できるようにするための代理指標
である。

**検討時に退けた代替案**:
- **旧定義を維持し、運用の勘所（判断者の裁量）で補う**: v0.4.0 の判定分岐が示すとおり、呼び出し
  インターフェース基準のみでは開発規約・保守スクリプトの実質変更を評価できず、判断者ごとに結論が
  割れる余地を残す。明文の再定義が必要と判断した。
- **保守面のみの変更を機械的に PATCH 固定にする**: リリース発火基準（V-1）と MINOR/PATCH の判定
  基準（V-2）は別の軸であり、混同すると「保守面の変更をリリースするなら常に PATCH」という誤った
  固定観念を生む。保守面のみの変更はそもそもリリースを発火しないため、この対応は不要と判断した。
- **リリース発火の判定を「変更ファイルの網羅列挙」等の機械検証で完全自動化する**: 対象領域（利用者
  面/保守面のパス範囲）は明確化できるが、混在時の扱いや意識要否の実質判断は人の判断を要するため、
  CONTRIBUTING 9.2 の目安を判断材料に `mgmt-release` が判定する運用（従来どおり）を維持し、完全
  自動化は見送った。

**遡及の扱い**: 本節は 2026-08-01 以降の判定に適用し、v0.4.0 以前の判定（旧2.10 に基づく判定）は
改訂しない。参考として、v0.4.0 相当の変更を新方針で再判定すると、`mgmt-coordinator.md` の振る舞い
変更は MINOR の例示「新しい機能・使い方の追加」に該当し（既存呼び出しに新しい判断規律が追加される
ため）、保守スクリプト（`.claude/scripts/lib/guidelines.sh` 等）はそもそも保守面のみでリリース非発火の
対象となる。両論併記だった旧判定は新方針下では単純化されるが、これは当時の判定を遡って変更する
ものではない。

**2026-08-02 追補**: 本節(1)〜(3)の判定手順を `agents/mgmt-release.md` へ固有パスリスト・
CONTRIBUTING の節番号込みで再掲した判断は、2026-08-02 の利用者裁定により見直した。配布資材
（`agents/`・`commands/`・`skills/`）は利用者リポジトリ側で動く前提であり、本リポジトリ固有の
パスリスト・節番号参照をそのまま同梱すると他リポジトリでの誤適用を招くため（2.15「業務特性依存の
標準手順の非同梱方針」と同旨）、固有内容は本リポジトリの保守用コマンド `.claude/commands/release.md`
へ移設し、`agents/mgmt-release.md` は汎用表現へ改めた。詳細は 2.32 を参照。

### 2.32 ブランチ運用・リリース手順の改訂（develop ブランチ導入）

**判断**: 2.23 の運用を次のとおり改訂する。ブランチ構成を `main`（リリース確定・配布ライン）と
`develop`（統合ライン）の2本立てとし、作業ブランチ（`feature/<topic>`・`chore/<topic>`）は
`develop` から分岐し PR は `develop` 向け（squash マージ、従来規約を維持）とする。リリースは、
利用者の指示を受けて `develop` 上で version・CHANGELOG のリリース準備コミットを作成→push→
`develop`→`main` の PR（リリース PR）を作成→利用者がマージコミット方式でマージ（リリース確定、
利用者のみ）→タグ `v<version>` は `develop` 側のリリース準備コミットへ打刻（マージコミット方式
であるため両ブランチの祖先になり back-merge 不要）→タグ push、という手順に改める。リリース PR は
squash マージを禁止する。GitHub の既定ブランチは配布が既定ブランチ参照であるため `main` のまま
維持し、作業 PR は `--base develop` を明示、`git-cleanup-branch.sh` は起点 `develop` を明示して
使う。`main` のブランチ保護（PR 必須・直接 push 禁止）を推奨設定として文書化する（適用は利用者
操作）。発火判定・バージョン種別判定・操作的判定テスト（2.31）自体は変更しない。手順の具体は
CONTRIBUTING 9章を正本とする。

**理由**: 2026-08-02 の v0.5.0 リリース作業で、旧方式（`main` 上でのリリースコミット）における
承認の関所であった `.claude-plugin/plugin.json`・`CHANGELOG.md` への Edit/Write の permissions
ask が、承認ダイアログが表示されない UI 上の不具合により機能せず、作業が停止した（利用者判断で
中止・切り戻し）。これは、承認の関所をファイル編集という粒度の細かい操作に置いていたために、
UI 側の一過性の不具合がそのままフロー全体を止める単一障害点になっていたことを意味する。承認の
関所を「`main` へのリリース PR マージ（利用者のみが実行できる操作）」という、GitHub 側の操作で
しか実行できない粒度に一本化することで、Claude Code の permissions ask の UI 状態に承認の実効性
を依存させない構成にした。タグを `develop` 側のリリース準備コミットに打刻するのは、リリース PR を
マージコミット方式でマージすることで当該コミットが `main`・`develop` 双方の履歴上の祖先になり、
`main` 側に打刻し直す back-merge を不要にできるためである。

**検討時に退けた代替案**:
- **(a) 従来どおり `main` 上でのリリースコミットを継続し、permissions ask で運用する**:
  2026-08-02 の中断が示すとおり、ask はダイアログ非表示という UI 側の不具合で機能停止しうる。
  リリースという後戻りコストの高い操作の唯一の関所として頼るには脆弱である。承認の関所を
  GitHub 側の操作（PR マージ）へ移す方が、Claude Code 側の UI 状態に依存しない。
- **(b) `release/v<version>` のようなリリース専用ブランチを都度作成する方式**: リリースの
  たびにブランチが1本と PR が1回増え、`develop`→`main` を直接 PR で結ぶ方式に比べて運用の
  手間が純増するため、増分の効果に見合わないと判断し採らなかった。
- **(c) リリース PR を squash マージする**: `develop` と `main` の間で内容が同一でもコミットが
  食い違う履歴乖離を招き、次回以降のリリース PR での偽コンフリクト、タグ検出の破綻（`develop`
  側のタグが `main` の祖先にならない）につながるため採らなかった（詳細は CONTRIBUTING 9.2）。

**既知の残存リスク**（品質ゲート qa-review H1 / sec-audit Medium-1 の指摘、2026-08-02）:
(a) リリース PR の squash 禁止は手続き上の規定であり技術的な強制を伴わない。マージ担当者が
GitHub UI で誤って Squash and merge を選択すると、履歴乖離・偽コンフリクト・タグ検出破綻が
発生しうる。マージボタンの許可はリポジトリ全体設定であるため、`develop` 向け PR の squash 許可と
`main` 向けリリース PR の squash 禁止を、単純な設定のみでは両立できない（対応は CONTRIBUTING 9.1
のブランチ保護推奨を参照。利用できる場合の緩和策に留まり、根絶するものではない）。
(b) `.claude-plugin/plugin.json`・`CHANGELOG.md` への Edit/Write の permissions ask（4行）を
廃止したことで、リリースフロー外での当該2ファイルの意図しない編集（誤操作・プロンプトインジェク
ション等）に対する一段目の機械的な足止めが失われた。以後は書面規約（CONTRIBUTING 9.2「作業
ブランチでは更新しない」）・品質ゲート（qa-review）での検知・後段の push ask／PR マージ deny に
委ねる。いずれも運用・人手確認への依存が残る点を残存リスクとして認識する。

**2026-08-02 追補: 配布資材（`agents/mgmt-release.md`）の汎用表現化**

**判断**: 上記のリリース手順改訂と同日、`agents/mgmt-release.md` の判定手順（2.31 の実務3ステップ）
に含めていた本リポジトリ固有のパスリスト（`agents/`・`commands/`・`skills/`・
`.claude-plugin/plugin.json`・`README.md` 等）・CONTRIBUTING 9.2 の節番号参照・固有例を除去し、
汎用的な表現へ改める。固有の判定手順は本リポジトリの保守用コマンド `.claude/commands/release.md`
（配布物ではない）に自己完結させる。

**理由**: `agents/mgmt-release.md` はプラグイン配布物であり、利用者リポジトリ側にそのまま導入されて
動く前提である。本リポジトリ固有のパスリスト・節番号参照を配布資材に含めると、利用者リポジトリでの
判定時に本リポジトリの構成（`agents/`・`.claude-plugin/plugin.json` 等の存在・CONTRIBUTING 9.2 の
節番号）を誤って前提としてしまい、誤適用を招く。これは 2.15「業務特性依存の標準手順の非同梱方針」と
同じ原則（配布資材には利用者の業務・リポジトリに依存する固有内容を持ち込まない）の帰結であり、
リリース判定手順という業務特性の強い領域では特に顕著に現れる。

**検討時に退けた代替案**: 固有パスリスト・節番号参照を残したまま「本リポジトリの例」等の注記を
添えて配布する案は、注記の見落としによる誤適用リスクが残り、そもそも汎用エージェントとして
再利用可能な形にならないため採らなかった。

**2026-08-02 追補: `git-cleanup-branch.sh` の既定引数を `develop` に変更**

**判断**: 上記のブランチ運用改訂時点では `git-cleanup-branch.sh` は起点 `develop` を第1引数で
明示して使う運用としていたが、これを引数省略時の既定 `develop` に変更した（旧・origin の既定
ブランチ自動検出は廃止）。`main` 等 `develop` 以外を起点にしたい場合のみ第1引数で明示する。
既存の安全策（クリーン確認・ブランチ名検証・`fetch --prune`・`ff-only`・`[gone]` のみ削除）は
維持する。あわせて `.vscode/tasks.json` を新設し、VSCode のタスクからも実行できるようにした
（既定値 `develop` の入力欄で起点を上書き可）。

**理由**: 作業ブランチの起点は `develop` に固定されているため、後始末のたびに `develop` を
引数で明示させる運用は、定型作業であるにもかかわらず手間だった。省略時の既定を `develop` に
変えることで通常の後始末を引数なしで完結させ、`develop` 以外を対象にする例外的な場合のみ
明示を求める方が実態に合う。

**2026-08-03 追補: リリース準備コミットの `release/v<version>` ブランチ化**

**判断**: リリース準備コミット（version 更新・CHANGELOG 追記）を `develop` へ直接コミットする
例外（本節冒頭の「判断」で定めた手順1〜2）を廃止し、`develop` から分岐した `release/v<version>`
ブランチ上で作成して `develop` 向け PR（リリース準備 PR）で統合する方式へ改める（利用者裁定
2026-08-03。正本は CONTRIBUTING 9.1・9.2）。

**理由**: v0.6.0 のリリース作業時、`develop` に適用された GitHub リポジトリルール（PR 必須）に
より直接 push が拒否され、9.1 の「直接コミット例外」が実態（ブランチ保護）と食い違った。本節の
「検討時に退けた代替案 (b)」はリリース専用ブランチ方式をブランチ・PR の純増を理由に退けていたが、
`develop` の保護を維持したまま直接コミットの例外経路を設ける方が設定・運用とも複雑になるため、
PR 1回の増分を受け入れて (b) を採用し直した。これにより `develop` への書き込み経路が「PR 経由」に
一本化され、直接コミット例外の廃止で規約も単純になる。タグの打刻先（リリース PR のマージコミット
の第2親＝`develop` 側のリリース準備コミット）・リリース PR の squash 禁止等、本節の他の判断は
変更しない。

**2026-08-05 追補: issue クローズの自動化はスクリプト方式（CI 案は仕様上不成立のため破棄）**

**判断**: 案件完了時の issue クローズは CI で自動化せず、`.claude/scripts/close-linked-issues.sh`
（dry-run 既定・`--apply` と y/N 確認で実行）による手動運用の支援に留める。develop 向け PR の
マージ契機で紐づく issue を自動クローズする CI（`on: pull_request` + GraphQL の
`closingIssuesReferences`）は実装・レベル3ゲート合格まで進めたが、着手時の一次情報再確認により
**GitHub の closing キーワードは PR が既定ブランチを対象とする場合にのみ解釈され、それ以外の
ブランチ向け PR ではキーワードが無視されリンク自体が作成されない**ことが判明したため破棄した
（利用者裁定 2026-08-05。正本は CONTRIBUTING 9.1、スクリプトの変更手順は同 8章）。

**理由**: 本リポジトリの既定ブランチはプラグイン配布が既定ブランチ参照であるため `main` のままと
しており（本節冒頭の判断）、案件の統合先は `develop` である。この組み合わせでは GitHub 側に
「PR ↔ issue」のクローズ用リンクが作られないため、リンクを前提とする自動化（GraphQL 経由・
GitHub 標準の自動クローズとも）は原理的に発火しない。PR 本文を自前で正規表現パースすれば発火は
可能だが、それは「GitHub が解釈しなかった記述をこちらが解釈する」ことであり、誤クローズの責任を
CI（無人実行）に負わせることになる。クローズは利用者に通知が飛び実質的に取り返しがつかないため、
無人での自動実行より、対象の抽出と確認を機械化したうえで実行判断を人に残す方式を選んだ。

**検討時に退けた代替案**: (a) 既定ブランチを `develop` に変更する — プラグイン配布が既定ブランチ
参照であるため配布仕様と衝突する。(b) PR ごとに GitHub の Development パネルで手動リンクし
`closingIssuesReferences` 経由で CI 発火させる — PR ごとの手作業が残り、手動クローズを置き換える
効果が薄い。(c) main へのリリース時にクローズする — リリース単位でしか閉じられず、案件完了と
issue の状態が長期間ずれる。

**証跡**: PR #62（base=develop、本文に `Closes #57`）の `closingIssuesReferences` が空であること、
issue #57 側のタイムラインに ConnectedEvent がなく cross-referenced のみであることを実データで確認
（2026-08-05、直近8 PR で同様）。一次情報は GitHub 公式ドキュメント「Linking a pull request to an
issue」の「If the pull request targets any other branch, then these keywords are ignored, no links
are created, and merging the PR has no effect on the issues」。

### 2.33 保守運用の都合による配布資材の変更禁止

**判断**: 本リポジトリ自身を保守するための運用上の仕組み（保守用の定期実行・保守者向けの報告
形式等）を理由に、配布対象の資材を変更しない。保守運用は資材の外（claude.ai の routines・
gitignore 済みの `.hw/` 配下・GitHub の issue／ラベル・セッション上の都度指示）で完結させ、
資材の変更は利用者一般に価値のある製品改善に該当する場合に限る（利用者裁定 2026-08-02。
正本は CONTRIBUTING 1.10）。

**理由**: 本リポジトリは開発リポジトリであると同時に配布物そのものであり（GitHub パス配布）、
保守都合の変更は利用側リポジトリへそのまま届く。開発リポジトリ固有の運用要望が資材に混入すると、
利用側には無関係な挙動・文言が配布物を汚染し、「製品の改善か、開発側の運用か」という境界の判断も
セッションごとに揺らぐ。禁止を規約として明文化することで、保守運用の要望を受けたセッションが
まず資材の外の実現手段を検討する既定動作を共有する。

**経緯**: 2026-08-02、AI 関連記事を収集してプラグイン改良アイディアを GitHub issue として提案する
保守用の定期実行（claude.ai routines。資材変更を伴わないため資材の外で構築）を整備した際、外部
進捗管理ツール連携が `/hw:standup` から呼ばれないことを補う standup コマンドの改修が提案されたのに
対し、利用者が「プラグインは変えてはならない。これはあくまでこのリポジトリを保守するための運用
だから」と裁定した。当初この原則は保守セッションの個人メモリに保存されたが、チームで共有すべき
考え方であるとの利用者指摘（2.22 と同旨）を受け、本節と CONTRIBUTING.md 1.10 に移管した。

### 2.34 サブエージェント実行上限到達時の縮退動作の規範化

**判断**: Claude Code 環境が持つ実行上限（ネスト深さ・同時実行数・セッション累計）へ到達し、委任
（Agentツール）や評価者（qa-review・sec-audit 等）の起動が失敗する場面での縮退動作を、層①の
プロンプト規範として明文化した。追記先は `agents/mgmt-coordinator.md`「作業方針」の委任フォールバック
条項とし、既存の「起動できない環境では分担計画を返す」という記述を、上限到達による起動失敗にも
同じ扱いを適用する形へ拡張した。規範は主に3点で、(1) 起動不可・上限到達のいずれでも分担計画への
フォールバックに落とす、(2) 評価者を起動できない場合は自分で代替評価せず「品質ゲート未実施」の事実と
残存リスクを明示して利用者判断に委ねる（生成と評価の分離を縮退時も不変条件とする）、(3) 並列委任の
ファンアウトは同時実行上限を意識し、超える分は分割して順次起動する、というもの。詳細は正本
`agents/mgmt-coordinator.md` 作業方針の委任フォールバック条項に委ねる。

**理由**: 実行上限到達は Agent ツールの失敗、またはツール自体が取り上げられ「残作業を自分でやる」
よう促される形で現れ（issue #27）、これはエラーとして目立たず、委任も品質ゲートも「静かに」崩れる
おそれがある。2.3 のガードレール4層モデルでいえば、上限は hermit-works の資材（②）や利用側設定
（③）ではなく Claude Code 実行基盤自体の制約であり、hermit-works 側では解除・回避も④の事後検出も
できないため、①（プロンプト規範）で検知し安全側に倒す振る舞いを定義する対応とした。上限値・環境変数名は
バージョンで変動する既定値であり（一次情報の CHANGELOG で複数回の改定を確認済み）、本文には数値を
持ち込まず挙動規範のみを記載し、資材の陳腐化を避けた。

**検討時に退けた代替案**:
- **`agents/*-lead.md` 9体の共通5原則ブロックへの展開**: 作業設計5原則ブロックは機械検証では
  なく、CONTRIBUTING.md（2.4節の「既存リードから逐語コピーし変更しない」旨の定め）が定める人手の
  逐語コピー運用で9体の一致を保つ規約の対象であり、9体×手動追従という波及コストに対し、縮退規範は
  リーダー層の委任判断（mgmt-coordinator）に一元化されていれば足りるため、費用対効果が見合わず
  見送った。
- **上限の数値・環境変数名を資材へ明記**: 深さ・同時実行数・累計の既定値はバージョン間で変動して
  きた実績があり（CHANGELOG で確認済み）、明記すると改定のたびに資材が陳腐化する。挙動規範を主とし、
  数値は書かない、または「変動する既定値の例」に留める方針とした。

---

### 2.35 `context: fork` の条件付き採用と判定基準の規約化

**判断**: `skills/repo-map/SKILL.md`・`skills/conventions/SKILL.md` の2スキルに限り、frontmatter へ
`context: fork`＋`background: false`（`agent` は省略し既定の `general-purpose` を用いる）を条件付きで
採用した（issue #28）。「条件付き」とは、(1) クリーン環境での実機検証（非リーダーエージェントからの
フォーク挙動・`.hw/` 書き込み成否・差分列挙の到達） (2) `repo-map` への注入耐性文言追加と正典リスト・
`.claude/scripts/validate.sh` 検査(d)の対象化 (3) 両スキル本文の実行主体記述・差分列挙指示、`commands/init.md`
末尾1文の改訂 (4) 本節および CONTRIBUTING 4.2/4.5 の規約整備 (5) 導入前後のトークン内訳の効果測定、
の5点を実装に織り込むことを指す。加えて、対象スキルの選定を個別判断のまま残さず、CONTRIBUTING 4.2の
オプション項目節へ fork 可否の判定基準3点（(1) 利用者との往復を含むか (2) 呼び出し元の会話文脈に依存
するか (3) 明示的なタスクを持つか）として規約化し、対話・判断を含むスキルはフォークしない方針とした。
対象外8スキルへの拡大・専用 agent type の新設は今回のスコープ外として見送った。適用条件・チェック
リストの詳細は正本 CONTRIBUTING 4.2/4.5 に委ねる。

**理由**: issue #28 が挙げた `context: fork` の仕様面の根拠は一次情報照合で事実として正確だった一方、
対象8スキルの除外理由づけは「対話を含むので分断される」という弱い説明に留まっていた。フォーク先は
独立したサブエージェントとして起動され、利用者への確認・承認に用いるツール（`AskUserQuestion` 相当）
が実行文脈から除去されるため往復自体が成立しない（会話履歴を引き継がないことも重なる）、という更に
強い理由に立て直し、以後のスキル追加時にも再利用できる判定基準として明文化した（評価成果物
`.hw/artifacts/2026-08-03-issue28-fork-eval-arch.md` §5）。また、フォークは呼び出し元エージェントの
層①（プロンプト規範によるガードレール）と層②（ツール制約）の双方に影響しうる（同 §2.2）。ハード制約
の迂回可能性を伴う変更を規約整備なしに個別スキルの判断だけで導入しないよう、規約整備自体を採用条件の
1つに固定した。

**issue の事実誤認2点の訂正**:
1. **バージョン依存の向きが逆**: issue は「`background` 未対応の古い環境（v2.1.218 未満）では同キーが
   無視されバックグラウンド実行になり危険」との想定に基づいていたが、事実は逆である。`context: fork`
   自体は v2.1.0 で導入され、v2.1.218 で初めて既定バックグラウンド化と `background: false` による
   オプトアウトが同時に導入された。したがって v2.1.218 未満は常にフォークが同期（ブロッキング）実行
   される既定だったため `background` キーが無視されても実害はなく、`background: false` の明記が本当に
   必要なのはむしろ v2.1.218 以降である（評価成果物 `.hw/artifacts/2026-08-03-issue28-fork-eval-tech.md`
   §2）。`background: false` を必ず添えるという実装方針自体は結果的に正しいが、根拠説明は訂正した。
2. **フォーク先のツールプールは呼び出し元からの継承ではなく agent type 由来**: issue はフォーク先が
   呼び出し元の `tools`/`disallowedTools` を引き継ぐ前提を含んでいたが、実際はフォーク先エージェント
   （既定 `general-purpose`）のツールプールがそのまま使われる。これは非リーダー41体に課している
   `disallowedTools: Agent` 等のツール制約（層②）がフォーク経由では効かない可能性を意味し、実機検証を
   採用条件へ含めた直接の根拠である（同 -eval-arch.md §2.2(a)・-eval-tech.md §3）。

**検討時に退けた代替案**:
- **代替A: 走査量そのものの抑制**（生ログを丸ごと展開せず、検出は列挙のみ・全文読みを避ける等）。
  根因（全体走査の生ログ堆積）へ直接効き、ガードレール構造・実行主体・規約に触れずコストもほぼ
  ゼロと評価されたが、issue #28 が明示した「2スキルへの `context: fork` 導入」というスコープを
  超える改修になるため今回は見送った。将来の再検討候補として残す。
- **代替B: 検出フェーズのみのスクリプト化**（`repo-map` 手順1のビルド定義検出等、完全に機械的な
  部分をスキル同梱スクリプト化する）。フォークより効果が確実でガードレール層②への影響もないが、
  同じくスコープ外のため見送った。
- **代替C: フォーク対象の見直し**（効果が大きいのは `/hw:init` ではなく、`hw:conventions` を前提
  確認で呼ぶ `/hw:draft-docs`・`/hw:optimize-assets`・`/hw:audit-assets` 経路という指摘）。対象選定を
  効果ベースで見直す余地は残るが、対象2スキルの選定自体は判定基準3点に照らして妥当と評価されており、
  今回のスコープ変更は見送った。
- **`agent:` の明示指定＋呼び出し元の限定**: 層②迂回（R1）が実機検証で確認された場合の代替施策として
  挙げられたが、まず `agent` 省略（既定 `general-purpose`）で実装し実機検証条件を満たすことを優先する
  方針とし、迂回が確認された場合の代替策として保留した。

**追補（2026-08-03・実機検証後の最終裁定）**: 採用条件(1)の実機検証で、`disallowedTools: Agent` を
持つ非リーダー（拒否リスト型41体中40体、`qa-review` を除く）から `context: fork` スキルを呼ぶと、
`Agent` ツールの拒否をすり抜けてサブエージェント生成（子 `spawnDepth+1`）が成立することを確認した
（構造的迂回。CONTRIBUTING 1.7 の記述を実態に合わせて訂正済み）。一方、フォーク先の実権限は
`agent:` 省略時は呼び出し元（親エージェント）が実際に持つツールプールに束縛されるため、読み取り
専用エージェントが書き込み等の余剰権限を得る権限昇格は、実機検証の範囲では既定構成において発生
しなかった。「構造的迂回」と「権限昇格」を切り分け、前者は解消手段がないまま残余リスクとして
受容し、後者は既定構成（`agent:` 省略）で不発であることを採用継続の根拠とした
（利用者最終裁定 2026-08-03、`.hw/decisions.md` 同日付「issue #28 追加裁定」）。この実機検証は
限定的な試行数（各構成1〜2回）に基づく。前者（構造的迂回の成立）は `Agent` ツール拒否のエラー
発生有無という機構的な確認だが、後者（権限昇格の不発）のうち qa-review 経由の確認はモデルの行動
観察ベースであり、機構的な確認ではない点に留意する。対象スキルを拡大する場合は、`agent:` 省略時の
フォーク先ツール不在を機構レベル（エラーメッセージ）で複数回確認することを追加の検証条件とする
（CONTRIBUTING 4.2 に規約化）。

追加調査した機械的な封じ手4候補は、いずれも副作用なしに構造的迂回だけを止められなかった:
1. `disallowedTools: Skill`（全面拒否）は迂回を止めるが、対象スキルを選べず `hw:conventions` 等
   他の `context: fork` スキルも道連れで不可視化する。
2. `disallowedTools: "Skill(hw:repo-map)"`（個別指定）は機能せず、1と同一の全面拒否に縮退する。
3. 許可リスト型（`tools:`）に `Skill` を加えても迂回自体は成立し、防げない（ただし実権限は
   呼び出し元の許可ツールに束縛されるため実害は限定的）。
4. `agent:` 明示指定はフォーク先の実権限を固定・限定でき権限昇格の実害は排除できるが、迂回
   （サブエージェント生成そのもの）は止められない。

`agent:` 明示指定（専用低権限エージェントへの固定）は退けた。理由は次の2点: (a) 呼び出し元の
権限に関わらずフォーク先の権限が指定エージェント自身の定義に固定される＝呼び出し元の制約を
経由しない固定の昇格経路を新設することになり、迂回の実害を防ぐ代わりに別の固定権限を持つ経路を
恒常的に追加することになる、(b) 専用エージェントの新規追加は本案件のスコープ（2スキルへの
`context: fork` 導入）を超えて `agents/` 追加規約（CONTRIBUTING 2章）の適用範囲まで広げる。
加えて `agent:` はプラグインスコープ名（`hw:<name>`）を逐語で書かないと無警告で `general-purpose`
にフォールバックする落とし穴があり（CONTRIBUTING「静かに失敗するガードレールを作らない」原則に
抵触しうる）、採用するとしても別途の周知が必須になる。

詳細な実機検証記録は `.hw/artifacts/2026-08-03-issue28-fork-verification.md`（迂回の一次確認・
(b)(c) の可否）・同 `-fork-sealing-options.md`（封じ手4候補の成立可否・`agent:` 挙動の解明）を参照。

**追補（2026-08-05・根拠記述の訂正）**: issue #50 の検証で、上記「理由」段落が当初「フォーク先は
会話履歴を持たないため、利用者への確認・承認の往復（`AskUserQuestion` 相当）自体が成立しない、という
より強い理由に立て直し、以後のスキル追加時にも再利用できる判定基準として明文化した（評価成果物
`.hw/artifacts/2026-08-03-issue28-fork-eval-arch.md` §5）。」としていた因果が不正確であると判明した
（一次情報照合 `.hw/artifacts/2026-08-05-issue50-fork-fact-check.md`）。往復不能の直接の理由は会話
履歴の不在ではなく、フォーク先が通常のサブエージェントとして起動され確認手段が実行文脈から除去され
ることにあり、本文を上記のとおり訂正した。会話履歴の非継承自体は事実として正しく、判定基準2
（会話文脈への非依存）の根拠として引き続き有効である。なお、スキルの `context: fork` は会話履歴ごと
引き継ぐ「会話のフォーク」（サブエージェント型 `fork`）とは別機構であり、後者に与えられるツール
フィルタの免除は前者には及ばない。判定基準3点と「対話・判断を含むスキルはフォークしない」という
結論は、訂正後の根拠のもとでも変更しない。

---

### 2.36 文書内パス参照の実在検証（validate.sh セクション12）と撤退線の事前宣言

**判断**: `.claude/scripts/validate.sh` にセクション12として、Markdown文書（README.md・CLAUDE.md・
CONTRIBUTING.md・DESIGN.md・DEVELOPMENT.md・`agents/*.md`・`commands/*.md`・`skills/*/SKILL.md`）中の
バッククォート付きリポジトリ内パス参照の実在検証を追加した（issue #34）。除外は4分類
（`.hw/` 配下・プレースホルダ・ワイルドカード〔0件一致はERROR〕・利用者側資材の例示パス明示
除外リスト）とし、各分類のスキップ件数を必ずサマリーへ出力する（0件でも出力し、除外規則が
機能したうえで0件なのか、そもそも検出漏れで動いていないのかを区別できるようにする。セクション5の
既存設計と同思想）。詳細な検出仕様・除外リストは正本 CONTRIBUTING 5章と `.claude/scripts/validate.sh`
冒頭コメントを参照。

**理由**: 本リポジトリは資材間のパス相互参照（例: 各文書が正本・関連文書を指し示す記述）が
極端に多く、ファイルのリネーム・削除時に散文中の参照だけが取り残され「静かに嘘になる」死角が
あった（issue #34、idea-scout起票）。既存のセクション5（README一覧突合）はREADME内の表形式一覧
のみが対象で、CONTRIBUTING/DESIGN等の散文中のバッククォート付きパス参照は検査対象外だった。
CONTRIBUTING 1.4 原則4「検知手段のない規範は追加しない」に沿い、人手のレビュー観点として
求めるのではなく機械検証として実装した。

**除外設計の考え方**: 検出対象を広く取る（バッククォート内の単語のうち規約プレフィックス＋
拡張子に一致する全て）一方、そのままでは「まだ存在しない生成物への言及」（`.hw/` 配下の
将来生成パスの例示）や「意図的な例示のための架空パス」（利用者側資材の例示）まで誤検知する。
これらを都度WARN読み飛ばしにせず4分類として名前を与え、件数をサマリーに出力することで、
除外規則自体が形骸化していないか（該当件数が不自然に増減していないか）を継続的に目視できる
設計とした。除外リストへの追加は無制限に許容せず、CONTRIBUTING 5章に追随手順（利用者側資材の
例示パスを新たに書く場合は `DOC_PATH_REF_EXCLUDE_LIST` へ追加すること）を明記し、除外の追加が
本文修正と切り離されて漏れることを防いだ。なお、抽出後トークンが `..` を含む場合はこの4分類の
判定・実在確認へ到達する前に短絡し `doc-path-ref-traversal` として無条件にERROR化する
（差し戻し対応 SEC-L1/qa-L3）。除外4分類の枠組み自体を変更するものではなく、実在確認前の
安全側の入力拒否として独立に働く。詳細は `.claude/scripts/validate.sh` 冒頭コメント参照。

**撤退線（事前宣言）**: 誤検知（意図した記述が機械的にERROR化される事例）が累計3件に達した
時点で除外規則の見直しを行い、5件に達した時点で本チェック自体の撤去を検討する。実装前から
運用実績を見て判断を後回しにせず、しきい値を先に約束しておくプラクティス（参考: zenn.dev の
aizuchi_works による記事 402ac5f5118c64 に倣った）を踏襲した。件数のカウント・見直し判断の
記録は `.hw/decisions.md` に残す。

**既知の限界**: 現時点で以下2点は未対応であり、実害が顕在化した場合は本節の撤退線（誤検知
カウント）の運用に乗せて判断する（品質ゲート差し戻し qa-review M1/N3）。(a) フェンス付き
コードブロック（```` ``` ```` で囲まれた範囲）内のバッククォート付きパス例示も区別せず抽出
対象とする。フェンス状態のトグル実装は行わず、コードブロック内の例示が誤検知になった場合も
個別実装ではなく撤退線の運用でまとめて扱う判断とした。(b) バッククォートの対応は1行内で完結
する前提とし、複数行にまたがる記法には対応しない（本リポジトリでは現状未使用の記法のため
実害なし）。

**2.21（shellcheck導入見送り）との不抵触の整理**: 2.21は外部静的解析ツール（shellcheck）という
新規依存の導入可否を扱ったもので、過去の是正実績の大半が機械検知になじまない人手判断（quoting
の文脈的要否等）だったことを理由に見送った。本節の対応は性質が異なり、(a) 外部ツールへの新規
依存を追加せず、既存 `.claude/scripts/validate.sh` の作法（bash + POSIX標準ツールのみ）を踏襲した自前
チェックの追加である点、(b) 検出対象が「パス参照の実在」という機械判定になじむ二値の性質で
あり、2.21が退けた「人手判断を要する種類の指摘」には該当しない点、の2点で2.21の判断根拠と
抵触しない。CONTRIBUTING 1.4 原則1「同じ効果が層④（`.claude/scripts/validate.sh`）で得られるなら
そちらを優先する」の方向にも沿う。

### 2.37 役割の重複検出における公式バンドルスキル軸の追加

**判断**: `hw:optimize-assets`「役割の重複」観点（ai-prompt 担当）の比較対象へ、hw 組織の
専門家エージェント・コマンド・スキルに加えて「Claude Code 本体が同梱するバンドルスキル・
他プラグイン提供スキル」を追加した（issue #35）。把握はセッションで利用可能なスキル一覧と
Claude Code のリリースノートの参照によるものとし、固定のスキル名リストは資材に書き込まない。
処分案には既存3案（利用者側を維持／hw 側へ委譲／併存）に加え、第4案「公式スキルへ委譲（公式提供で
足りる分は利用者側を整理）」を追加した。加えて手順4「最適化計画」に、バンドルスキルは版により増減するため
「Claude Code 更新後の再点検」を計画の残課題として記録させる旨を追記した。詳細な検出手続き・
処分案の文言は正本 `commands/optimize-assets.md` を参照。

**理由**: Claude Code 本体のバンドルスキルは版を追うごとに増え続け、利用者が独自に作成した
スキルが後から公式スキルと役割重複する状況が構造的に発生する。従来の「役割の重複」観点は
比較対象が hw 組織の資材に限定されており、この種の重複は検出対象外だった（issue #35、
idea-scout起票）。

**検討時に退けた代替案**: 固定のスキル名リストを資材に同梱する案は、バンドルスキルの構成が
Claude Code のバージョンごとに増減し即座に陳腐化するため採らなかった。2.15節が業務特性依存の
定型手順を同梱せず利用者側で育てる方針を採った判断と同じ思想（一般化・固定化できない情報を
資材にハードコードしない）に基づく。

---

### 2.38 ai-prompt の評価規律: 複数回試行によるブレ幅測定

**判断**: `agents/ai-prompt.md`「プロンプトの改善」「評価基準・評価セットの作成」の2項目、および
`agents/ai-lead.md`「品質・リスク判断」項目に、複数回試行によるブレ幅測定の評価規律を追記した
（issue #38）。要旨は、同一ケースを複数回実行し平均点だけでなく最低点・ブレ幅も併せて判定・報告
する、改善前後の比較は同条件・同回数で行い単一実行同士の比較で優劣を断じない、見逃し（FN）と
過剰抽出（FP）を区別し業務上の重大度に応じた重み付けを検討する、評価結果の報告に試行回数・日付・
対象モデルを添える、試行回数は評価対象の重要度に応じて決める、評価基盤が無い場合は基盤整備を
先に提案する、というもの。詳細な追記文言・位置は正本 `agents/ai-prompt.md`（`agents/ai-lead.md` は
1行）を参照し、本節では複製しない。

**理由**: LLM出力は非決定的であり、単発実行の満点評価は実力の証明にならない（issue #38、根拠と
なる記事2本の実在・内容一致を確認済み）。改善前後を単発実行同士で比較すると、たまたま良い（悪い）
結果が出ただけの偶然を改善（劣化）と誤認するおそれがある。最低点・ブレ幅・試行条件（回数・日付・
対象モデル）を報告に明示させることで、後日の再評価や第三者による追試が条件を突き合わせて検証
できるようにした。

**検討時に退けた代替案**:
- **固定回数の規範化**（例:「必ず10回実行する」等の数値の明記）: 評価対象の重要度・コストは案件
  ごとに異なり、軽微な確認まで一律の試行回数を課すと不必要にコストが嵩む一方、重要な判断に対して
  形式上の固定回数で足りると誤認されるおそれもある。試行回数は評価対象の重要度に応じて都度判断する
  規律とし、数値の固定化は採らなかった（issue リスク欄の裁定）。
- **hw 自身の資材（`agents/`・`commands/`・`skills/`）の品質評価への同枠組みの適用**: 本規律は
  利用者プロダクトのプロンプト評価を対象とし、hw 自身の資材評価（qa-review 等の既存品質ゲート）
  への持ち込みは含意しない。適用すると保守フロー自体に複数回試行のコストが常時発生し、レベル別
  品質ゲートの運用と衝突するため、今回のスコープには含めず射程外とした（将来の再検討の余地は残す）。

---

### 2.39 品質ゲート受入時の「レビュー対象一覧 × 変更ファイル一覧」突合の必須化

**判断**: 評価ノードの完了報告を統括が受け入れる際、指摘の有無によらず、委任時に添えた変更ファイル
一覧との突合を含む受入条件を適用し、不備は「品質ゲート未実施」として差し戻す規律とした（issue #37）。
従来は指摘ゼロ時に限定されていた突合規定を、全受入に適用する形へ置換・統合し、この差し戻しは
差し戻し回数 n/3 に不算入と明記した。対象ファイルを担当が意図的にレビュー対象外とした場合は、
理由が明記されていれば統括の判断で受入可とする例外の道を残した。配置は既存の同節規範（上限3回・
エスカレーション等）と同様に統括専用節への直接追記とし、適用主体が統括1者に限られるため
CONTRIBUTING 1.4 配置基準のトリガー行＋詳細スキル方式は採らなかった。詳細な受入条件の文言・位置は
正本 `agents/mgmt-coordinator.md`「差し戻しループと振り返り」節を参照し、本節では複製しない。
関連する参照文・追記は `commands/request.md`・`agents/qa-review.md`・`agents/sec-audit.md`・
`agents/sec-privacy.md` にも及ぶが、これらも正本ではなく参照・波及にとどまる。

**理由**: 委任時の diff 添付・担当による対象ファイル一覧の報告という往路の整備に対し、復路の照合が
指摘ゼロ時限定だったため、指摘あり受入・成果物が実在しない受入・手続き不備による差し戻しが n/3 に
算入されるかどうか未規定だった（issue #37）。全受入へ適用範囲を広げることで、統括が対象ファイル
一覧・確認観点の記載・成果物の実在という3点を機械的な確認観点として照合できるようにし、確認漏れの
見逃しを防いだ。なお、本条件の検知手段は `.claude/scripts/validate.sh` による当該文言の存在検証までであり、
統括が個々の受入時に3点照合を実際に運用として遵守しているかどうかは機械検証の対象外である
（受入そのものは会話上の判断行為であり、運用時の遵守を検知する接点がない）。CONTRIBUTING 1.4
原則4「検知手段のない規範は追加しない」に対しては、同節「トリガー行の新設要件」(ii) が文言自体の
欠落・改変の逐語検証を原則4の検知手段として認める既存パターンであることに倣い、本件も文言の存在
という限定的な検知手段を確保して原則4を充足すると整理した上で、運用時の遵守を機械的に担保する
手段までは用意しないという限界を受容した。

**検討時に退けた代替案**:
- **指摘ゼロ時限定の現状維持**: 復路照合の対象外だった「指摘あり受入」「成果物不在」「手続き不備」に
  よる差し戻しの n/3 算入可否が未規定のまま残り、issue #37 が指摘する不整合を解消できないため
  採らなかった。
- **スクリプトによる運用時の機械突合**（受入判断自体をスクリプトで検証する案）: 受入は統括が
  会話上で行う判断行為であり、実際に3点を照合したかどうかを外形的に検知する接点がないため、
  `.claude/scripts/validate.sh` への新規検証追加は見送った。同スクリプトが担保できるのは当該条項の文言が
  正本に存在することまでであり、それを超える運用時検証は本件のスコープ外とした。

---

### 2.40 測定に基づく事実記録への測定条件（測り方・n・日付）の必須化

**判断**: `agents/mgmt-coordinator.md`「作業方針」の decisions.md 記録行（1行形式の定義箇所）・
計画ファイル様式の記述・完了報告様式の記述の3箇所に、測定・集計に基づく事実（件数・所要時間・
成功率・傾向等）を記録する際は測定条件（測り方・n・日付）を添える旨を追記した（issue #48）。
decisions.md の1行形式は維持したまま、根拠が測定由来の場合は `根拠:` に測り方と n を添える例
（例: `根拠: validate.sh 実行 3回中3回失敗（2026-08-04）`）を示す。計画ファイルについては測定値・
傾向を記す場合の測定条件（測り方・n・日付）なしの断定を禁じ、完了報告については同様の断定禁止に
加え、条件を添えられない伝聞・推測を「〜とみられる」等事実と区別できる表現で記す旨も追記した。
対象は測定・集計・観測に基づく事実に限り、慣習判断・裁定の記録（測定を伴わないもの）には求めない。
計画ファイル様式の同旨の再掲は `commands/request.md` 手順2・`agents/mgmt-planner.md` にも及ぶが、
これらは正本ではなく再掲にとどまる。
詳細な追記文言・位置は正本 `agents/mgmt-coordinator.md`「作業方針」を参照し、本節では複製しない。

**理由**: `.hw/decisions.md`・計画ファイル・完了報告で測定・集計に基づく値が測定条件なしの断定として
記録されると、単発観測が既成事実として増幅されるおそれがある（issue #48。根拠記事は運用報告記事で、
複数経路からの実在・要約一致を確認済み）。測り方・n・日付を明示させることで、後日の再評価や第三者に
よる事実確認の際に条件を突き合わせて検証できるようにした。対象を測定由来の事実に限定したのは、
慣習判断・裁定の記録まで一律に条件記載を求めると1判断1行の軽量な様式が崩れ、記録コストが不必要に
嵩むため。なお、2.38（ai-prompt の複数回試行によるブレ幅測定）も同一記事を根拠の一部に持つが、
2.38 は利用者プロダクトのプロンプト評価に関する運用規律であるのに対し、本節は hw 自身の記録様式
（decisions.md・計画ファイル・完了報告）の記載規範を対象とし、両者は独立の判断である。

**検討時に退けた代替案**:
- **全記録への条件記載義務化**（慣習判断・裁定の記録も含め一律に測定条件を求める案）: 1判断1行の
  軽量な decisions.md 様式が崩れ記録コストが嵩むため採らなかった。対象は測定・集計に基づく事実に
  限定した（issue リスク欄の裁定）。
- **`.claude/scripts/validate.sh` による機械検証**（測定条件の記載有無・妥当性をスクリプトで検証する案）:
  記録内容が測定に基づくかどうか、条件表記が妥当かどうかの判定は文言の意味解釈を要し、静的検査の
  接点がないため見送った。

---

### 2.41 確認手段の可用性による縮退の規範化

**判断**: `agents/mgmt-coordinator.md`「作業方針」に、利用者への確認手段（`AskUserQuestion` 等）が
実行文脈により利用できない場合の縮退を定める1条を新設した（issue #50）。対象は同節内で確認手続きを
定める複数箇所（判断エスカレーション・手順資産の昇格提案・利用者作成資材の初出適用・初出パターン時の
進め方確認・新規導入提案の評価ゲート（go/no-go））で、各箇所への個別追記ではなく1条への集約とした。
規範の要旨は、
(1) 確認手段の不在を実行文脈由来（特にサブエージェントとして起動された場合は確認系ツールが除去され、
その事実は事前に告知されない）の制約として扱う、(2) その場合は当該手続きを「確認済み」として扱わず、
確認事項・判断材料・選択肢・推奨案を完了報告に明記して呼び出し元に委ね、自分の判断で
先へ進めない、(3) 本条の判定は非対話実行時の「おまかせ相当で続行」規定より先に行う、というもの。
なお (3) の「おまかせ相当で続行」に該当する既存の定めは「初出パターン時の進め方確認」のみであり、
評価ゲート（go/no-go）は非対話実行時も判断を保留する定めであるため、正本側でも両者を書き分けている。
あわせて README「できないこと」節に、統括をサブエージェントとして起動した場合は確認を伴う手続きが
実行できない旨を1行追記した。詳細な条項文言は正本 `agents/mgmt-coordinator.md`「作業方針」の
当該条項に委ね、本節では複製しない。

**理由**: 公式ドキュメントは `AskUserQuestion` を含む一部ツールをサブエージェント起動時に一律除去する
と明記しており（第1フィルタ。バックグラウンド実行かどうかを問わない）、実機でも確認した
（2026-08-05・サブエージェント1体・foreground 起動・n=1。ツール定義に不在、`ToolSearch` でも
検出不可、除去の事実自体がシステムプロンプトに一切記述されない）。一方、2.26 追補が明らかにした
とおり、`agents/*.md` の定義全文がシステムプロンプトに載るのはサブエージェント起動時に限られる。
この2つの事実を重ねると、統括の確認手続きは、それが定義として読み込まれる唯一の経路（サブエージェント
起動）において、その手続きを実行する手段自体が失われているという構造になる。2.26 追補が指摘した
「メイン会話経路には規範が届かない」問題の裏返しとして、「サブエージェント経路には規範は届くが
実行手段が届かない」問題が存在した。README がサブエージェント起動（`@hw:mgmt-coordinator`）を
利用者への案内経路として明示している以上、この経路を辿った利用者ほど、確認を要する手続きが無警告で
機能しない事態に遭遇しやすいという点も、集約1条を独立の判断として起票した理由である。

**隣接節との関係**: 2.34（サブエージェント実行上限到達時の縮退動作の規範化）と同じ「委任・確認の
手段が失われた場合は自分の判断で進めず呼び出し元に委ねる」縮退思想を共有するが、発生条件は異なる。
2.34 は実行基盤側の資源制約（ネスト深さ・同時実行数・セッション累計）への到達によって Agent ツール
等の起動が失敗する場合を扱うのに対し、本節は資源制約とは無関係に、サブエージェントという実行形態
そのものに対して特定ツール（確認手段）が設計上常に除去される場合を扱う。両者は別条件のため統合は
せず、別条とした。

**検討時に退けた代替案**:
- **5箇所への個別追記**: 各手続きの記述末尾に縮退文をそれぞれ追記する案。issue #50 自身が
  instruction dilution として避けるべきと指摘しており、同一規範を5箇所へ複製すると将来の改訂時に
  5箇所の追従漏れリスクを生むため採らなかった。集約1条から5手続きを名指しする下向き参照に統一した。
- **README案内のみで塞ぐ**: README「できないこと」への案内追加のみで対応し、
  `agents/mgmt-coordinator.md` 側への追記を行わない案。README の案内に反して利用者が
  `@hw:mgmt-coordinator` のサブエージェント起動を選んだ場合、統括自身に縮退規範が存在せず無防備に
  確認手続きへ進んでしまい、issue #50 が指摘した実行不能かつ無警告の状態を解消できない。案内と
  規範整備の両輪を要すると判断し、README単独での解決は採らなかった。

---

### 2.42 qa-review 指摘様式への失敗シナリオ必須化（種別限定）と誤検出抑制条項の追加

**判断**: `agents/qa-review.md`「作業方針」の指摘必須項目の定めに、(1) 欠陥系の指摘（バグ・境界
条件・null/未定義・エラーハンドリング漏れ・競合状態・リソースリーク等）には根拠に加えて失敗
シナリオ（どの入力・状態で問題が実際に顕在化するか）を必須項目とする、(2) 失敗シナリオを書けない
欠陥系の懸念は重要度を確定した指摘として扱わず「要確認」として区別する降格ルール、(3) sec-audit
の誤検出抑制条項に相当する、実際に到達しうるコードパスか・前提条件が成立しうるかを確認する条項、
の3点を追加した（issue #51）。可読性・命名・規約準拠・設計一貫性の指摘は従来どおり根拠のみで足り、
失敗シナリオは求めない。欠陥系とそれ以外の境界は「担当業務」節の特定項目への限定ではなく、正本
`agents/qa-review.md`「作業方針」の列挙＋「等」と適用外列挙（可読性・命名・規約準拠・設計一貫性）
で定まり、テストの妥当性確認・シェルスクリプト観点等いずれにも明示されない類型は(2)の降格ルールが
受け皿となる。既存1行の改訂＋新規2行以内、計3行以内に収めている。「担当業務」節は変更していない
（出力様式の変更であり担当範囲の変更ではない）。詳細な
条項文言は正本 `agents/qa-review.md`「作業方針」の当該箇所に委ね、本節では複製しない。隣接する
2.21（静的解析ツール導入見送りとレビュー観点の明文化）も同じく `agents/qa-review.md` 側への
明文化型の判断だが、対象は「担当業務」節のシェルスクリプト観点であるのに対し、本節は「作業方針」
節の指摘の書き方（出力様式）を対象とする点で異なる。

**理由**: 指摘系エージェント4体を比較すると、sec-audit は「悪用シナリオ」、qa-a11y は「影響を
受けるユーザー」、qa-perf は「計測」をそれぞれ指摘の必須項目としているのに対し、qa-review のみ
発現条件の記述を求めていなかった（統括の確認、2026-08-05・4ファイルの該当行を直接参照）。この
非対称の下では、qa-review の指摘は「根拠」の抽象的な記述だけで形式上充足でき、受け手が指摘の
成立可否を判定できないという issue #51 の問題意識につながる。デファクトの裏付けとして、Claude
Code 純正のコードレビュー報告ツール `ReportFindings` は `failure_scenario`
（"Concrete inputs/state → wrong output/crash"）を required フィールドとして定義しており
（統括が2026-08-05に本セッションのツール定義を直接参照して確認、n=1）、失敗シナリオの明示は
本プラグイン固有の要求ではなく外部ツールの設計とも整合する。一方、CONTRIBUTING 1.4 原則4
「検知手段のない規範は追加しない」に対しては機械検証を追加しておらず、検知は差し戻し時の統括／
qa-lead の目視という人手に依存する残存リスクを受容している（前例: 2026-08-01 URL 規約の docs
系4体逐語一致を機械検証対象外とした裁定、`.hw/decisions.md` 該当行）。隣接する2.39（品質ゲート
受入時の「レビュー対象一覧×変更ファイル一覧」突合の必須化）は品質ゲートの受入側（統括が委任・
成果物を照合する規範）を扱うのに対し、本節は評価ノード（qa-review）自身の出力様式を扱い、両者は
規範の層が異なる。

**検討時に退けた代替案**:
- **無条件必須化**（欠陥系に限定せず全指摘に失敗シナリオを求める案）: 可読性・命名等シナリオ化に
  なじまない指摘まで対象になり、書けない懸念を無理に脚色させる副作用があるため採らなかった。
- **降格ルールのみで種別を切らない案**（失敗シナリオの必須化を欠陥系に限定せず、降格ルールだけを
  全指摘に設ける案）: 可読性・命名等の指摘まで一律「要確認」へ降格されてしまい、確定指摘として
  扱うべきものまで扱いが下がる副作用があるため採らなかった。
- **`.claude/scripts/validate.sh` への逐語検証の追加**（利用者裁定 2026-08-05）: 技術的に不能なわけでは
  ない。9(g)（`skills/import-assets` の固定1ファイルを対象とする逐語検証、
  `.claude/scripts/validate.sh:1192-1196`）の先例が示すとおり、単一ファイル対象の逐語検証はスクリプト側の
  設計として可能である。それでも追加しなかったのは、費用対効果の判断として人手検知（差し戻し時の
  統括／qa-lead の目視）で足りると裁定したためであり、残存リスクを受容した上で将来の再評価の余地を
  残す。

---

### 2.43 ヒアリング質問設計規律の導入（順序付け・提示形式・打ち切り）

**判断**: `agents/ana-requirements.md`「作業方針」に、依頼元へのヒアリング項目を後戻りコストの
大きい論点から順序付け・選択肢提示・目安3〜7問での打ち切りとサマリ提示という質問設計の規律を、
`agents/mgmt-coordinator.md`「作業方針」に、非可逆な要件論点は1問ずつ確認する切り分けを、それぞれ
新設した（issue #53）。対話（1問ずつの提示・合意取得）の実行者は `AskUserQuestion` を持つ統括であり
`ana-requirements` 側は持たないため、`ana-requirements` は整えた質問を完了報告の確認事項として返す
側にとどまる。詳細な条項文言・適用条件は正本 `agents/ana-requirements.md`・`agents/mgmt-coordinator.md`
それぞれの「作業方針」を参照し、本節では複製しない。

**理由**: (a) 全エージェント共通の判断手順（2.11節）が既に「選択肢・推奨案を添えて完了報告の確認事項
として呼び出し元に委ねる」ことを定めており、未充足だったのは後戻りコストの大きい論点を先に問う
順序付け・目安3〜7問での打ち切り・確定事項サマリの3点のみであるため、新条項はこの差分のみを足し、
既存の判断手順文は再掲しなかった。(b) 統括 `agents/mgmt-coordinator.md`「作業方針」の既存条項は
確認事項を複数件まとめて利用者へ提示する方針を定めており、これは規約・命名・表記等の慣習判断を
対象とする。「1問ずつ確認する」規律を無条件で足すとこの既存条項と自己矛盾するため、対象を
「非可逆で後戻りコストが大きい要件論点」（本節が新設する規律の対象）と「規約・命名・表記等の慣習
判断」（既存条項が一括提示のまま維持する対象）とに切り分けることでのみ両立させた。(c) 提示する
選択肢数・打ち切り問数の目安（2〜4個・3〜7問）は根拠記事の経験則であり実測データを伴わないため、
断定的な上限ではなく「目安」と明記した（2.40節が求める慎重さと同趣旨）。(d) 本規律の遵守状況を
検証する機械検証は追加していない。検知手段は差し戻し時の統括／qa-lead の目視という人手にとどまり、
順守漏れが発生してもその場では機械的に検出されないという残存リスクを受容している（利用者裁定
2026-08-05。CONTRIBUTING 1.4 原則4「検知手段のない規範は追加しない」との関係は退けた代替案を参照）。

**検討時に退けた代替案**:
- **無条件の「1ターン1問」**（対象を切り分けず全確認事項に1問ずつ確認を課す案）: 統括の一括提示
  方針（理由(b)）と自己矛盾するため採らなかった。対象を要件論点と慣習判断とで切り分けることで
  解消した。
- **`ana-requirements.md` のみへの追記**（統括側の作業方針は変更しない案）: 1問ずつの提示・合意
  取得という対話の実行者は `AskUserQuestion` を持つ統括であり `ana-requirements` は持たないため、
  `ana-requirements` 側だけに規律を置いても実際の対話がその規律どおりに運ばれるかは統括の裁量に
  委ねられたままとなり、効果が担保できない。両エージェントの作業方針に対で新設した。
- **二人三脚（2.17(c)）から `ana-requirements` への専用委任動線の新設**: 統括側にも質問設計の
  規律を置いたことで、質問の作成元が `ana-requirements` か統括かによらず規律が及ぶため、専用動線を
  新設しなくても目的を達成できると判断し見送った。
- **`.claude/scripts/validate.sh` への逐語検証の追加**（利用者裁定 2026-08-05）: 技術的に不能なわけでは
  ない。9(g)（`skills/import-assets` の固定1ファイルを対象とする逐語検証）の先例が示すとおり、
  単一ファイル対象の逐語検証はスクリプト側の設計として可能である。それでも追加しなかったのは、
  質問設計という文言の意味解釈を要する規律の遵守判定を静的検査に載せる費用対効果が低いという
  判断として、人手検知（差し戻し時の統括／qa-lead の目視）で足りると裁定したためであり、
  残存リスクを受容した上で将来の再評価の余地を残す。

**隣接節との関係**: 2.17（初出パターン時の進め方確認）は依頼パターンの初出時に「どう進めるか」を
4択で選ぶ確認であるのに対し、本節は選ばれた進め方の内側で発生するヒアリング質問そのものの設計を
扱い、位相が異なる。2.41（確認手段の可用性による縮退）は本節が新設する1問ずつの確認も
`AskUserQuestion` 等の確認手段が前提であり、それが使えない実行文脈では2.41の縮退規定に従って
呼び出し元へ委ねる関係にある。2.11（判断エスカレーション手順）は「選択肢・推奨案を添えて委ねる」
という共通土台を既に定めており、本節はその上に順序・提示形式・打ち切りという出し方の規律を
上乗せする差分にとどまる。2.40（測定条件の必須化）は測定・集計に基づく事実記録を対象とするため
本節の目安数値は対象外だが、実測を伴わない値を断定的に書かないという慎重さの姿勢を共有する。

---

### 2.44 利用者資材レビューへのプロンプト希釈観点の追加（audit-assets 3軸）

**判断**: `/hw:audit-assets` 手順2の ai-prompt 委任対象レビューに、量による埋没
（instruction dilution）・行動判断できない記述・禁止のみで代替がない記述の3軸を追加した
（issue #52）。観点の本体は正本 `agents/ai-prompt.md`「担当業務」節に置き、
`commands/audit-assets.md` 手順2の ai-prompt 委任行は既存の評価観点（曖昧・矛盾・過不足）の
列挙への語句挿入にとどめる。詳細な条項文言はそれぞれの正本を参照し、本節では複製しない。

**理由**: (a) 根拠記事の数値基準（3層構造・100行以内・CRITICAL 5個以下等）は利用者リポジトリの
規模・性質に依存するため一律の基準として資材には書かず、観点としてのみ持たせた。2.21（静的解析
ツール導入見送り→レビュー観点の明文化）が機械的な閾値ではなく人手判断のための観点構造化を選んだ
姿勢、および2.40が求める、実測を伴わない値を断定的に書かない慎重さと同趣旨である。(b) 遵守状況を
検証する機械検証は追加せず、検知は差し戻し時の統括／qa-lead の目視という人手にとどめる。
CONTRIBUTING 1.4 原則4「検知手段のない規範は追加しない」に対しては、単一資材内の記述品質という
文言解釈を要する観点の遵守判定を機械検証に載せる費用対効果が低いと判断し、人手検知で足りるとする
残存リスクを受容した（利用者裁定2026-08-05）。2.43が同一の扱いを先例として採っている。(c) 指摘の
出し方は切り分ける。構造の再編（先頭側への配置・別ファイルへの分離・行動指示への具体化・代替の
併記）は是正案として提示するが、どの規範を最重要として絞るかという優先順位判断は利用者の裁量に
属するため確認事項として呼び出し元へ返す。この切り分けは、`commands/audit-assets.md` 末尾の
既存規定（意図が読み取れない資材は是正案を出す前に確認事項として報告する）、および利用者の資材は
承認なしに変更しない原則の延長であり、優先順位の決定は利用者の意図そのものに関わるため機械的な
是正案化の対象から外した。(d) 観点の本体を `agents/ai-prompt.md`「担当業務」節に置き
`commands/audit-assets.md` 側は既存長文行への語句挿入にとどめたのは、CONTRIBUTING 1.4 が警告する
希釈をコマンド定義自身に自己適用したためである。

**検討時に退けた代替案**:
- **根拠記事の数値基準をそのまま資材に書く案**（100行以内・CRITICAL 5個以下等の直書き）:
  利用者リポジトリの規模・性質に依存し一律の基準として妥当しないため採らなかった。
- **3軸の指摘をすべて確認事項に留める案**: 構造の再編まで確認事項化するとレビューの実用性が
  下がるため採らず、構造の再編は是正案として即応させ、優先順位判断のみを確認事項に絞った。
- **`commands/audit-assets.md` 側に観点の本体を書く案**: 手順2は既に資材種別ごとの長い委任行の
  列挙であり、そこへ観点本体を書き足すこと自体がコマンド定義に希釈を持ち込む。正本を
  `agents/ai-prompt.md` に置き委任行は語句挿入にとどめることで回避した。
- **`/hw:optimize-assets` 側へ配置する案**: 境界は構成（複数ファイル間の指示の分散）と品質
  （単一資材内の記述品質）で分かれる。今回の3軸は単一資材内の記述品質を対象とするため、
  audit-assets 側の担当とした。

**隣接節との関係**: 2.21 は `agents/qa-review.md`「担当業務」節へのシェルスクリプト観点の
明文化であり、機械検証（静的解析）を見送り人手レビューの構造化を選んだ判断であるのに対し、
本節は `agents/ai-prompt.md`「担当業務」節への希釈観点の明文化であり、対象は利用者資材の
プロンプト品質という点で異なる。2.43 はヒアリング質問設計という別テーマだが、実測を伴わない値を
断定的に書かない姿勢（理由(a)）と、機械検証を追加せず人手検知の残存リスクを利用者裁定のもとで
受容する姿勢（理由(b)）を本節も共有する先例にあたる。CONTRIBUTING 1.4（ガードレール追加の設計
原則）は本節が観点の置き場所（`agents/ai-prompt.md` 側）を判断する際の自己適用元であり、正本は
同章を参照する。

### 2.45 依頼の2分類（プラグインのメンテナンス／リポジトリの環境整備）の導入

**判断**: 本リポジトリで受け付ける依頼を「プラグインのメンテナンス」（利用側リポジトリに届いて
動く製品＝1.10 が列挙する配布対象の資材への変更）と「リポジトリの環境整備」（本リポジトリで
作業を回すための仕組み＝保守作業専用文書・`.claude/`・`.hw/`・`.vscode/`・`.devcontainer/` 等の
整備。本節時点では `.github/workflows/` は1.10の配布対象側にあり本分類の列挙には含まれて
いなかったが、2.46の改訂で保守面（本分類側）へ移った）の2分類で受け付け、分類により許容範囲
（リポジトリ固有の事情の書き込み可否・保守運用都合を理由にした変更の可否・CHANGELOG／version の
対象可否・品質ゲート）が変わることを規約化した（利用者裁定2026-08-05）。正本は CONTRIBUTING 1.11。

**理由**: 1.10 は「配布資材を保守都合で変更しない」という禁止を定めるが、依頼を受け付ける時点で
自分がどちらの土俵にいるかを判定する動作までは定めていなかった。土俵の判定が曖昧なままだと、
リポジトリの環境整備で足りる依頼が配布資材の変更として着手される、あるいは逆に配布物側の妥当な
改善提案が「保守都合」と誤判定されて資材の外へ押し出される、という双方向の誤判定が起こりうる。
2分類とその許容範囲差を正本に明文化することで、受付時に土俵を先に固定してから作業に進む既定動作を
共有する。

**検討時に退けた代替案**:
- **1.10 に埋め込む案**: 1.10 は「配布資材を保守都合で変更しない」という単一の禁止規定であり、
  そこへ2分類の判定枠組み（4観点の比較）を混載すると、禁止規定としての焦点がぼける。したがって
  1.10 直下に独立した節（1.11）として新設した。
- **CLAUDE.md のみに書く案**: チーム共有すべき規約の正本は CONTRIBUTING.md に置くという定め
  （CLAUDE.md「記録先の使い分け」節、2.22 と同旨）に反するため採らなかった。CLAUDE.md 側には
  受付時の判定動作を1〜3行に圧縮し、詳細は CONTRIBUTING 1.11 へ参照を飛ばす形にとどめている。
- **1.10・1.11 いずれの列挙にも該当しないパスの扱いを、9.2 のフォールバック規定（該当しないパスは
  保守面扱いを既定とし、実質的な影響がある場合や判定に迷う場合はリリース側〔重い側〕に倒す）に
  倣って「迷えば重い側（プラグインのメンテナンス）に倒す」と書く案**: qa-review 差し戻し対応の
  過程で一度採用したが、利用者裁定（2026-08-05）により撤回した。9.2 のフォールバックは「リリース
  発火の要否」という可逆な判定（取りこぼしても次回リリースで回収できる）を対象とするため機械的に
  倒してよいが、1.11 の分類は「何をやってよいか」を決めるゲートであり非可逆性が高く、同じ規定を
  写すのは不適切だった。本リポジトリの判断エスカレーション規律（`.claude/scripts/lib/guidelines.sh` の
  `GUIDELINE_DECISION_PROCEDURE_LINE`・`agents/mgmt-coordinator.md`「作業方針」・CONTRIBUTING 1.8）
  は「文書化された定め・類似例・デファクトスタンダードに一致すれば採用し、一致しない・明らかでない
  場合は独断で決めず利用者に確認して記録する」であり、「迷えば機械的に倒す」はこれと矛盾する。
  該当しないパスの判定は「配布されて意味があるか」の基準で明らかな場合のみ自動採用し、明らかで
  ない場合は判断材料・選択肢・推奨案を添えて利用者に確認し `.hw/decisions.md` に記録する、という
  既存の判断エスカレーション規律をそのまま適用する形に改めた。混在時（配布資材＋保守作業専用文書）
  の「プラグインのメンテナンス側を適用する」規定は、配布資材を含むことが明白で判定に迷う余地が
  ないため自動判定の範疇にとどまり、この撤回の対象外として維持した。

**経緯**: 2026-08-05、PR #63 の fixtures に関する問答の中で `.claude/commands/` の保守用ローカル
コマンド（`release.md`・`release-notes.md`）が話題になり、利用者が「このリポジトリでの作業は
『プラグインのメンテナンス』か『リポジトリの環境整備』のどちらかになる。依頼がそのどちらに当たるかで、
やって良いことが変わることを明示しておいて」と裁定した。あわせて、この考え方は個人メモリではなく
チーム共有の正本（CONTRIBUTING.md）へ記録するよう明示の指示があった。その後の qa-review 差し戻し
対応で1.11に追加したフォールバック規定（列挙に該当しないパスは「迷えば重い側に倒す」）に対し、
同日中に利用者が「明らかな場合は自動でも良いけど、迷うようなら倒すのではなくユーザーに判断を仰ぐのが
正しいあり方。この方針はこのプラグインの基本」と裁定し、上記の代替案の撤回に至った。

### 2.46 保守用スクリプトの `.claude/scripts/` 移設と配布/保守境界の再定義

**判断**: 保守用スクリプトを `scripts/` から `.claude/scripts/` へ移設し、配布/保守の線引きの
基準を「配布されるか」から「利用側リポジトリで意味を持つか」へ改めた。あわせて `.github/workflows/`
を1.10 の配布対象列挙から外し、配布側の資材が保守用スクリプトを参照することを禁じた。ルート
`scripts/` は将来配布用スクリプトを置く場合の予約パスとして残す（利用者裁定 2026-08-06。正本は
CONTRIBUTING 1.10）。

**理由**: `source: "./"` によりリポジトリ全体が物理配布されるため「配布されるか」では線が引けず、
`scripts/` 配下8本（本体7本＋共有ライブラリ `lib/guidelines.sh` 1本）すべてが本リポジトリ保守専用
という実態と1.10の「配布対象」という位置づけが乖離していた。物理的にディレクトリを分けることで、分類の判定を都度の解釈でなくパスで決着させる。

**検討時に退けた代替案**:
- **案A: ディレクトリは動かさず1.10/1.11 の列挙側で `scripts/` を保守面へ移す案**: 文書改訂のみで
  済み低コストだが、配布資材と保守資材が同一ディレクトリ階層に同居し続けるため、新規スクリプト
  追加時にどちら側かの判定が毎回必要になる。パスで決着させる案（採用案）に比べ再発余地が残る。
- **案C: `scripts/` 配下をスクリプト単位で切り分け、配布資材から参照されるものだけ配布対象と
  する案**: 参照の有無で分類が揺れ続ける。実際の参照は「`validate.sh` の秘密情報検出と同等の
  観点で」という概念参照のみで、参照先の実体を必要としない性質だった（本案件で参照自体を
  除去した）。

**経緯**: 2026-08-06、PR #70（CONTRIBUTING 1.11 の新設）の作業中にスコープ外として切り出された
issue #71 への対応。統括の裏取りで issue 記載との差分（`close-linked-issues.sh` は起票時点で
develop 未マージ）と issue 未記載の事実2点（`commands/report.md` のみが保守スクリプトの実行を
配布資材から指示していた／`scripts/lib/` が CONTRIBUTING 8.1・8.2 の列挙から漏れていた）を検出し、
いずれも利用者裁定で本案件のスコープに取り込んだ。

### 2.47 保守用ローカルコマンド・スキル（`.claude/commands/`・`.claude/skills/`）の規約整備

**判断**: 保守用ローカルコマンド（`.claude/commands/<name>.md`）・保守用ローカルスキル
（`.claude/skills/<name>/SKILL.md`）の配置・命名・frontmatter・README非更新・配布/保守判定の
委譲先を CONTRIBUTING 3.6・4.6として明文化した。あわせて `validate.sh` のfrontmatter必須項目
検証・文書内パス参照の実在検証の対象に `.claude/commands/*.md` を追加した（利用者裁定
2026-08-06。正本は CONTRIBUTING 3.6・4.6）。

**理由**: `.claude/commands/` には既に保守用ローカルコマンド2件（`release.md`・
`release-notes.md`）が実在し、各ファイル自身が冒頭で「保守用ローカルコマンドである」旨を自己
宣言していたが、この宣言を裏づける規約がCONTRIBUTING側に無く、配置・命名・README非更新・判定
基準が各コマンドの自己申告のみに依存していた。2.46（issue #71）で `.claude/scripts/` が
8.1・8.2 の適用範囲・配置表へ収載され配布/保守境界の明文化が進んだのに対し、
`.claude/commands/`・`.claude/skills/` は取り残され非対称になっていた。加えて `validate.sh` は
frontmatter必須項目検証・文書内パス参照実在検証のいずれも `.claude/commands/` を対象に含んで
おらず（秘密情報スキャンは `git ls-files` 全件走査のため既に対象内。事実確認F3）、`release.md`
が `.claude/scripts/git-changelog.sh` を参照する箇所（事実確認F4）は issue #71 の移設時に人手
追従で救われたに過ぎず、機械検証による保証が無い状態だった。

**検討時に退けた代替案**:
- **独立章として新設する案**: 8章が `.claude/scripts/`・CI 定義という別の実現手段を独立章として
  持つのと平仄を合わせる案も検討したが、コマンド・スキルという実現手段そのものは既に3章・4章が
  扱っており、保守用ローカルはその中の一変種（配置先と配布判定が異なるだけ）に過ぎない。独立章に
  すると3章・4章との内容重複（frontmatterテンプレート・`$ARGUMENTS`の使い方等）を再掲する必要が
  生じ、正本の二重化を招く。既存節への追加節（3.6・4.6）とし、共通部分は3.1〜3.5・4.1〜4.5への
  参照のみで済ませた（利用者裁定2026-08-06）。
- **1.10/1.11 へ判定基準ごと集約する案**: 3.6/4.6の中に「配布かどうかの判定基準」自体を書く案も
  検討したが、1.10/1.11 が既に配布/保守判定の正本であり、3.6/4.6 に基準を複製すると2.45/2.46で
  積み上げてきた「正本の二重化を避ける」原則（CONTRIBUTING 5章「正本と再掲」）に反する。3.6/4.6
  は1.10/1.11 への1行の委譲文にとどめた。
- **既存2件（`release.md`・`release-notes.md`）の自己宣言文を撤去し3.6の参照のみに置き換える案**:
  各コマンド本文が閉じた文脈で読めることを優先し、既存2件の宣言文はそのまま先例として残した
  （本案件のスコープ外。改訂が必要になった場合は別案件で扱う）。

**経緯**: 2026-08-06、issue #72。issue #71（2.46）で `.claude/scripts/` の配布/保守境界を再定義
した際、`.claude/commands/` が同種の資材でありながら取り残されている非対称に統括の裏取りで
気づいた。利用者裁定で(1)節の置き場所は独立章でなく3章・4章への追加節、(2)`validate.sh` の拡張
はfrontmatter必須項目検証・パス参照実在検証の両方、(3)`.claude/skills/` は規約を書きつつ
`README.md` の `.claude/skills/`（利用側リポジトリの正式スキル昇格先）との用語衝突整理も行う、
(4)`/close-issues` 化は別issueへ、の4点が決まった。

### 2.48 許可リスト全体の衛生観点の追加

**判断**: `skills/permission-rules/SKILL.md`「## 手順（deny ルール等を提案・変更・レビューする際の
確認手順）」節に、既存のエントリ単位の確認手順（手順1〜6）に続けて、許可リスト全体の衛生を確認する
手順を新設した。観点は3点——(a) 全体像の説明可能性（`allow`/`ask`/`deny` の各エントリ総数・
ワイルドカードを含むエントリを挙げ、リスト全体が何を許可しているかを説明できるか。判定根拠を
必ず添え、可否の断定は利用者に委ねる）、(b) 剪定・見直しの契機の有無、(c) 許可理由の追跡可能性
——で構成する。あわせて、適用対象を全体レビュー・許可リストの見直しを目的とする依頼に限る範囲
限定、機微情報のマスク指針、剪定提案時にエントリ単位の既存手順の併用と用途確認を求める規定を
同手順内に置いた。`commands/audit-assets.md` 手順2 の sec-audit 委任行は、既存の列挙（破壊的・危険な
コマンド、秘密情報の混入、過剰な権限許可）へ短い語句を挿入するにとどめ、観点の本体は上記の正本へ
委譲した（issue #68）。

**理由**: (a) 根拠記事は5軸の診断枠組みを提示し、許可リストについてはそのうち4軸で弱いと評価して
いる。本案件はこの4軸のうち3軸を採り、残る1軸は既存資材（`/hw:optimize-assets`・
`hw:export-assets`）と射程が重なるとする起票時（issue #68）の整理を踏まえて対象外とした。
(b) 観点の本体を
`commands/audit-assets.md` 側に書かず正本（`skills/permission-rules/SKILL.md` の手順節）へ集約
したのは、2.44 と同じ判断枠組み——委任行への直接追記はコマンド定義自身への希釈の自己適用になる
という扱い——を踏襲したものである。(c) 剪定提案（観点(b)）は「許可を減らす＝作業を止める」方向の
変更になりうるため、新旧差分の提示・正当なコマンドの列挙・利用者側リポジトリの検索という既存の
エントリ単位手順を必ず併用する旨を手順内に明記した。(d) 許可理由の追跡可能性（観点(c)）の是正案
として `.claude/settings.json` 本体へのコメント付記は提案しない。公式ドキュメントには同ファイル
へのコメント許容の記載が無く（標準 JSON）、実機未検証のまま許容されると断定する記述を持ち込まない
ためである。

**検討時に退けた代替案**:
- **`commands/audit-assets.md` 側に観点の本体を書く案**: 手順2は既に資材種別ごとの長い委任行の
  列挙であり、そこへ観点本体を書き足すこと自体がコマンド定義に希釈を持ち込む（2.44 と同型）。
- **`agents/sec-audit.md`「担当業務」節に観点の本体を置く案**: sec-audit は一般監査を常時担うため、
  許可リスト全体の衛生という限定的な観点を担当業務へ置くと一般監査のたびに常時プロンプト長を
  消費し続ける。
- **エントリ総数等の具体的な数値を閾値として規範化する案**: 環境ごとの許可リストの規模・性質に
  依存し一律の基準として妥当しないため採らなかった（2.44 と同趣旨）。
- **対象を hooks の `if:` 条件・sandbox 設定等の設定全般へ拡張する案**: 射程が過大になるため、
  本案件では `allow`/`ask`/`deny` の許可リストに絞った。
- **`.claude/settings.json` へのコメント付記を是正案として提案する案**: 公式ドキュメントに同ファイル
  へのコメント許容の記載が無いため採らなかった。

### 2.49 qa-review 報告下限の明文化と品質ゲート網羅レビューの既定宣言

**判断**: `agents/qa-review.md`「作業方針」節に、(1) 重要度の下限を自ら引き上げて指摘を省略せず
Medium/Low/Nits の指摘も件数と内容を残す旨（詳細は成果物ファイルへ逃がしてよい）、(2) 呼び出し元
から報告範囲を絞るよう明示的に求められた場合に限り絞ってよく、その場合は絞った旨・除外した重要度・
除外件数を報告に明示する旨、の2条項（2行以内）を新設した。あわせて `agents/mgmt-coordinator.md`
「品質ゲートの適用判定」節に、品質ゲートとして起動するレビューは網羅レビューを既定とし委任
プロンプトに報告範囲を絞る指示を書かない旨の宣言を1行追加した。同節が挙げる重要度別件数の例
（Critical/High/Medium/Low の4段階）は qa-review・sec-audit・sec-privacy で共有されており、
qa-review の5段階（Nits を含む）にそのまま合わせて例へ `Nits:0` を足すと sec-audit・sec-privacy
（Nits を持たない）側で新たな不整合を生むため、例の段階数自体は据え置いたまま「段階は各評価ノード
の定めに従う（qa-review は Nits を含む）」旨を添える是正とした（issue #64）。
新設した(2)は既存の「要確認」への降格ルール（失敗シナリオを書けない欠陥系の懸念を確定した指摘
として扱わない規定）とは競合しない。降格は*確定した指摘として扱うかどうか*の規定であるのに対し、
本節が扱うのは*確定した指摘を重要度で切り捨てず報告するかどうか*の規定であり、両者は独立の軸を
扱う。詳細な条項文言・位置はそれぞれの正本 `agents/qa-review.md`「作業方針」節・
`agents/mgmt-coordinator.md`「品質ゲートの適用判定」節を参照し、本節では複製しない。

**理由**: (a) `agents/qa-review.md`「作業方針」節は重要度（Critical/High/Medium/Low/Nits）の
**付与**は規定していたが、どの重要度から報告するかという**下限**はどこにも明文化されておらず、
この非対称の下ではレビュアーが暗黙に閾値を引き上げて Medium 以下の指摘を省略しても規約違反に
ならなかった（issue #64）。この非対称を放置すると、統括が受け取る重要度別件数（`agents/
mgmt-coordinator.md`「品質ゲートの適用判定」節の記録行）の意味が不安定になる。(b) 起票の根拠と
した記事は、レビュー担当への指示の書き方（用途の明示・フレーミング）によって Critical・High の
検出は保たれる一方で Medium 以下の指摘が報告から落ちる事例を報告しており、本案件が採用したのは
この「報告する基準は指示の書き方で暗黙に動きうる」という定性的な指摘そのものである。記事が示す
検出率の数値は、比較対象の一方が異なるモデルという条件下・n=1（2026-08-07 取得、記事自身が
「傾向として読むべき数字」と限界を明示）の一事例に過ぎず、2.40 が求める測定条件の明記に照らし、
本節では数値そのものを設計判断の根拠に用いない。(c) 新設した条項の遵守状況を検証する機械検証は
追加していない。検知手段は品質ゲート受入時の統括の受入照合（2.39）と差し戻し時の目視にとどまり、
レビュアーが下限を暗黙に引き上げていないかどうかを外形的に検知する接点がないという残存リスクを
受容している（前例 2.42・2.43・2.44 と同じ扱い）。隣接節との関係を示すと、2.42（qa-review 指摘
様式への失敗シナリオ必須化）は指摘に*何を書くか*という書式を扱うのに対し、本節は*どこから報告
するか*という下限を扱い、位相が異なる。2.39（品質ゲート受入時の突合の必須化）が定める統括の
受入照合は、本節の条項が実際に遵守されているかを確認する数少ない機会にあたる。2.40（測定条件の
必須化）は理由(b)で検出率の数値を設計判断の根拠に用いないと判断した際の直接の適用元である。
2.44（プロンプト希釈観点の追加）とは、委任プロンプトへ用途を毎回明記する案を希釈として退けた
判断の枠組みを共有する。

**検討時に退けた代替案**:
- **毎回のレビュー用途明示**（issue 案。「網羅レビュー／出荷可否判定」等の用途を委任プロンプトに
  毎回明記する案）: 品質ゲートとして起動するレビューは常に網羅であるため定型文化するだけで実質的
  な効果がなく、委任は計画ファイルのパスと動的差分のみとする既存原則（`agents/
  mgmt-coordinator.md`「作業方針」節）に対しても指示の希釈を増やすだけであるため採らなかった。
  宣言型の既定にすることで委任プロンプトへの毎回の追記を不要にした。
- **指摘系エージェント横断展開**（sec-audit・qa-a11y・qa-perf にも同趣旨の報告下限条項を展開する
  案）: 指摘系4体共通の行予算を圧迫すること、加えて指摘系エージェントごとに重要度体系（sec-audit
  は Nits を持たない4段階、sec-privacy は段階を列挙していない）と報告の性格が異なり、本件の枠組み
  をそのまま持ち込めるかは別途検討を要することから、本案件のスコープには含めず見送った。
- **見送り**（issue #64 の指摘を採用しない案）: 重要度の付与規定のみで下限規定を欠く非対称が
  issue #64 で具体的に指摘されており、放置すると統括が受け取る重要度別件数の意味が不安定なまま
  になるため採らなかった。
- **`.claude/scripts/validate.sh` への逐語検証の追加**: 報告下限の遵守は文言解釈を要する運用規律
  であり、先例 2.42（失敗シナリオ必須化）・2.43（ヒアリング質問設計規律）と同じく人手検知
  （品質ゲート受入時の統括の受入照合・差し戻し時の目視）で足りると判断し、残存リスクを受容した
  上で見送った。

### 2.50 集計結果の自己検査の作業方針への追加（ana-data・ana-metrics）

**判断**: `agents/ana-data.md`・`agents/ana-metrics.md` の「作業方針」節に、それぞれ1項目ずつ
自己検査の規律を新設した（issue #66）。挿入位置は両ファイルとも共有逐語行（「規約・命名・表記等の
慣習的な判断…」）の直前。ana-data は既存項目「相関と因果を区別し…」の直後に置く。内訳（①〜④）を
1項目の括弧内に集約する形式は 2.44 と同趣旨（指示の希釈の抑制）。ana-metrics は ana-data の逐語
コピーではなく、監査・レビュー文脈に起こし直した独自の文言とした。ana-data は自ら集計を実行する
役割であるのに対し、ana-metrics は集計を自ら実行せず ana-data に依頼する連携関係にある
（`agents/ana-metrics.md`「連携」節）ため、自ら集計した結果を検査する枠組みではなく、ana-data から
受け取った集計結果・監査結果の数値をレビュー時に検査する枠組みで書いた。ana-metrics の内訳は
ana-data の①〜④をそのまま並べず、既存項目「KPI定義は必ず一意に計算できる形（定義式・対象・
除外条件）で文書化」との重複を避けるため、既存項目が**設計時**の定義厳密化であるのに対し新項目は
**数値を読むとき**の検査であるという切り分けを保ったまま、ana-metrics に効く観点（0件・null は
計測欠落と実態ゼロの切り分け／桁差は単位・粒度／修飾語と母集団の一致）に絞った。
詳細な追記文言・位置は正本 `agents/ana-data.md`・`agents/ana-metrics.md`「作業方針」を参照し、
本節では複製しない。

**理由**: 既存の「作業方針」は集計の入力側（スキーマ確認、集計条件とクエリの添付）は規律化済み
だったが、出てきた数値そのものを疑う規律を欠いていた（issue #66）。起票の根拠とした記事
（Zenn・Qiita 同題記事）はKindleスクレイピングの単一事例（1時間のうちに4回の誤集計、n・日付の
明記なし）を報告しており、2.40 が求める測定条件の明記に照らし本節では記事の事例・数値そのものを
設計判断の根拠に用いていない。採用したのは「集計は例外を出さずに静かに間違いうる」という定性的
指摘のみである。「既知の正解を1件混ぜる」（ana-data 内訳④）を必須化せず「可能な場合は」の条件
付きに留めたのは、検算対象となる既知の正解を用意できるかどうかが対象データに依存し、一律の必須化
は妥当しないため。

**検討時に退けた代替案**:
- **4項目を並列の4バレットにする案**: 「作業方針」節はすでに複数の規律を列挙する構成であり、
  自己検査の内訳をそのまま4項目に展開すると指示の希釈を招くため、2.44 と同趣旨で1項目への
  集約を選んだ。
- **ana-metrics へ ana-data と同一文言を逐語コピーする案**: ana-metrics は集計を自ら実行せず
  ana-data に依頼する連携関係（`agents/ana-metrics.md`「連携」節「KPIの実績集計・計測データの
  品質確認は ana-data に依頼し…」）にあり、「自分の集計を検査する」文言をそのまま持ち込むと
  実態と合わない。監査・レビュー文脈に起こし直した。
- **「既知の正解を1件混ぜる」の必須化**: 対象データによっては検算に使える既知の正解を用意できない
  場合があるため、必須ではなく「可能な場合は」の条件付きに留めた。
- **`.claude/scripts/validate.sh` への機械検証の追加**: 自己検査の実施は会話上の判断行為であり
  外形的に検知できる接点がないため、先例 2.42・2.43・2.44・2.48・2.49 と同じく人手（品質ゲート
  受入時の統括の受入照合・差し戻し時の目視）に委ね、残存リスクを受容した上で見送った
  （CONTRIBUTING 1.4 原則4「検知手段のない規範は追加しない」）。

### 2.51 qa-review「コメントの過不足」への判定軸の付与

**判断**: `agents/qa-review.md`「担当業務」節の「コメントの過不足」判定に、既存の可読性バレット
の直後へ専用バレットを1件新設し、判定軸を明文化した（issue #82）。軸は独立した2つを併記する
——①置き場所軸（その情報の置き場所としてコードコメントが適切かどうか）、②正確性軸（コメントが
コードと乖離していないか＝コメント腐敗）。①は過剰と判定する対象を抽象原則のみで済ませず3点具体
列挙し（コードを読めば分かること=WHAT、変更履歴、課題管理番号への参照のみの記述）、残すべきものを
コードから復元できない理由に相当する記述として対にする。①には利用者側規約優先の旨の但し書きを添えるが、
この但し書きの射程は①のみに限り②へは掛けない。②は規約の有無によらず適用する独立指摘とする。
詳細な条項文言・挿入位置は正本 `agents/qa-review.md`「担当業務」節を参照し、本節では複製しない。
新設バレットを独立バレット化したことで、同ファイル「作業方針」節の失敗シナリオ必須化条項（2.42）
が定める適用外列挙（可読性・命名・規約準拠・設計一貫性）からの継承が暗黙には効かなくなるという
品質ゲートの指摘（issue #82）を受け、当該列挙そのもの（および 2.42 側の逐語再掲2箇所）は変更せず、
新設バレット冒頭に「可読性レビューの一部として適用する」旨を明記して帰属を明示する解で決着した。

**理由**: (a) `agents/qa-review.md`「担当業務」節の「コメントの過不足」には判定基準が無く、
指摘が担当者の主観に依存するか、あるいは指摘自体が出ない状態だった（issue #82）。LLM生成コードは
冗長コメントが付きやすいという背景も踏まえ、判定軸を明文化した。(b) 軸を2つ採ったのは、置き場所
軸（issue #82 本体が主眼とする、情報をコードコメントに置くこと自体の適否）と正確性軸（実装変更に
追従していない記述を誤りとして扱う観点）が性質の異なる別軸であるため。正確性軸は Anthropic 公式
プラグイン pr-review-toolkit（`anthropics/claude-code` リポジトリの plugins に実在し、説明は
"Comprehensive PR review agents specializing in comments, tests, error handling, type design,
code quality, and code simplification"）の `comment-analyzer`（6エージェント構成のうちの1つで、
対象はコメントの正確性・陳腐化）と同軸であり、置き場所軸に合流させず併記する構成を採った。実在は
記事の記述に加え、統括が同リポジトリを直接確認して裏取りした（2026-08-07 直接確認、n=1）。
(c) 過剰と判定する対象を抽象原則のみで書かず3点の具体列挙（WHAT・変更履歴・課題管理番号参照）に
とどめたのは、抽象原則のみでは運用時の解釈がレビュアーごとにブレるため。根拠とした記事は著者の
マージ済みPRを対象にした事例（2026-08-07 WebFetch 実在確認、n=1）を示すが、著者自身が計測上の
交絡（追加コード行数の変化）を注記しており、2.40 が求める測定条件の明記に照らし、本節では記事が
示す数値そのものは設計判断の根拠に用いない。本案件が記事から採用したのは「判定軸を有用性から
置き場所へ移す」という観点と「具体列挙で書く」という書き方の2点のみである。参照した記事は
<https://zenn.dev/uzu_tech/articles/86a2ef05a7d649> と
<https://zenn.dev/shirochan/articles/70b01405674957> の2本で、いずれも2026-08-07 WebFetch 実在
確認、n=1である。
(d) 「規約が無い場合の既定」の但し書きの射程を①置き場所軸のみに限定し②正確性軸へ掛けなかった
のは、②はコードとの乖離という事実に基づく誤りの指摘であり利用者側規約の有無によらず適用すべき
だからである。独立した2つの規律を同一バレット内に併記する際、一方の除外規定（規約優先の但し書き）
が他方まで覆う書き方をすると規律同士が干渉するという失敗形は 2.48（許可リスト全体の衛生観点の
追加）が issue #68 のゲート指摘で扱った論点と同型であり、本節でも射程を明示的に区切ることで同種の
干渉を避けた。(e) 数値閾値（コメント比率・行数等）は資材に書かない。利用者リポジトリの規模・言語に
依存し一律の基準として妥当しないため、2.44・2.48 と同じ判断枠組みを踏襲した。(f) 新設した条項の
遵守状況を検証する機械検証は追加していない。検知手段は qa-review 自身の目視にとどまり、判定軸が
実際に適用されているかどうかを外形的に検知する接点がないという残存リスクを受容している
（CONTRIBUTING 1.4 原則4「検知手段のない規範は追加しない」への裁定。先例 2.44・2.48）。

**検討時に退けた代替案**:
- **書き手側 eng-* 11エージェントへの同原則の波及**（コメントを書く際の規律として作業方針へ
  展開する案）: 書き手側11エージェント共通の行予算を圧迫する希釈懸念があり、本案件は指摘系
  qa-review への判定軸付与にとどめ、書き手側への展開は見送った。
- **品質ゲート レベル1「コード内コメント」との関係注記**（`agents/mgmt-coordinator.md`
  「品質ゲートの適用判定」節への相互参照追加）: レベル1の「コード内コメント」は変更対象の性質に
  よる品質ゲート適用レベルの分類であるのに対し、本節が扱うのはレビュー担当が指摘する際の観点で
  あり、両者は別レイヤー（節も文書も別）にある。issue 本文が挙げていた「レベル1との混同」という
  懸念は杞憂と判定し、注記は不要とした。

### 2.52 手順資産の形式への判断根拠（Why）欄の追加

**判断**: 手順資産（`.hw/procedures/`）の形式に「判断根拠」欄を新設する（issue #80）。記す内容は
(a) なぜこの順序・担当・完了条件なのか、(b) 蒸留時に採らなかった代替手順とその理由、(c) 崩れたら
手順を見直すべき前提条件（撤退線）である。分量は上限5行とし（下限は設けない）、必須欄としつつ
「特段の判断なし」1行で足りることとする。あわせて実績欄の役割を「日付＋1行の事実（案件番号・差し戻し回数等）」に
限定し、学び・裁定・代替案の理由は判断根拠欄へ寄せる。欄を持たない既存の手順資産は不整合として
扱わず、次に再利用・更新するときに埋める。`hw:import-assets` による移入資産の降格変換
（手順資産としての実績リセット・出自記録付きの配置）でも同じ欄を通し、移入資産だけが欄を欠く
状態を作らない。欄の位置を含む詳細な形式は `agents/mgmt-coordinator.md`「作業方針」を正本とし、
本節では複製しない（2.12 が形式の詳細を同節へ委譲済み）。

**理由**: Why を失った手順は、前提が変わったときに機械的に踏襲するか、ゼロから組み直すかの二択に
なり、2.12 の成長ループが狙う「次回以降の計画分解コストの低減」が前提変化に対して働かなくなる。
根拠欄が残ることで、昇格可否を検討する際に根拠の妥当性を材料として参照できる余地も生まれる（現行の
昇格判断は再利用実績の目安5回に加えて「hw のフロー外でも使いたいか」「分岐が安定しているか」という
質的観点を既に含んでおり、本案件ではその観点一覧＝正本 `agents/mgmt-coordinator.md`「作業方針」の
昇格判断の記述へ判断根拠を追加していない。将来の可能性としての記述である）。必須としつつ
「特段の判断なし」1行の逃げ道を置いたのは、蒸留が承認不要の自動処理であり（2.12）、逃げ道なしに
必須化すると欄を埋めるためだけの中身のない記述が生まれるためである。分量に上限を置いたのは、手順
資産が次回の案件で参照される AI 作業用の簡潔な記述であり、根拠欄の肥大がトークン消費と読み飛ばしを
招くためである。
本判断が参照した記事 <https://zenn.dev/rabitarochan/articles/5fe5eb29a8dae8> には測定値が無く
（2026-08-07 WebFetch 実在確認、n=1）、採用したのは「Why を失った How は従うことしかできず適応
できない」という定性的指摘のみである（2.40 が求める測定条件の明記に照らした扱い）。なお、欄の
有無を機械検証する手立ては設けず、検知は人手（蒸留時・再利用時の目視）にとどめて残存リスクを
受容した。先例 2.42・2.43・2.44・2.48〜2.51 と同じ扱いである。

**検討時に退けた代替案**:
- **手順本体と根拠を2ファイルに分ける案**（根拠記事の SKILL.md + harvest.md 分離に相当）:
  hw の「1手順1ファイル」原則（2.12）と衝突するため採らなかった。記事から採ったのは「根拠を
  手順に併記する」という観点のみである。
- **欄を新設せず既存の「注意点」欄の運用で吸収する案**: Why が注意点の中に埋没し、所在が一意に
  ならない。実績欄の肥大（学び・裁定の混入）という本案件の出発点と同じ失敗形を別の欄で繰り返す
  ことになるため採らなかった。
- **既存4件の手順資産へ今回まとめて遡及記入する案**: 蒸留当時の判断を後から再構成することになり
  記述の精度が落ちる。次の再利用・更新の機会に、実際の判断が想起できる状態で埋める方針とした。
- **`.hw/decisions.md`（案件横断の軽量ADR台帳）へ根拠を寄せる案**: 同台帳は案件をまたぐ決定の
  記録であり、個々の手順に紐づく根拠の保存先としては役割が異なるため採らなかった。

---

## 3. 運用から得られた知見

- **希釈（instruction dilution）への配慮は継続的なコストである**: 共通規範を1件足すたびに、
  既存の規範を含めた全体の遵守率が下がりうる。共通ガードレール文を「短文6行の予算制」で運用し、
  新規条項は追加ではなく既存条項との入替を検討する、という制約を明文化したのはこの教訓による。
  規範を「思いついたら足す」運用は避け、追加前に他レイヤー（`tools`/`disallowedTools`・機械検証）で
  代替できないかを確認する手順を定めた。
- **「静かに失敗するガードレール」を作らない**: ハード制約は、書いた内容が実環境で本当に効くかを
  検証しない限り、効いているという錯覚だけを生む。`disallowedTools` に書くツール名の実機検証を
  ハード制約導入の必須条件にしたのはこのためである。ソフト規範側でも、逐語一致の機械検証
  （2.3節）とセットにすることで、同種の「書いたつもりが欠落している」失敗を防いでいる。
- **機械検証とソフト規範は代替関係にある**: 同じ効果を得られるなら、プロンプト内の記述より
  `frontmatter` のハード制約、それも難しければスクリプトによる事後検証を優先する。ソフト規範は
  「他レイヤーで代替できない場合の手段」という位置づけであり、最初の選択肢ではない。
- **委任コストは軽視できない固定費である**: サブエージェントの委任1回あたり数万トークン規模の
  固定コストが見積もられており、これが軽量ワーカー層（`haiku` の実働部隊）を見送る決定打になった。
  定型作業は「エージェントに委任してから軽量化する」のではなく、最初からスクリプト・skill側に
  寄せたほうが総コストで有利という判断につながっている。
- **モデル降格の効果は構造的に小さくなりやすい**: 呼び出し頻度が低いエージェントほど降格の安全性は
  高いが節約額も小さく、節約額が大きいエージェントほど高頻度で品質影響のリスクが大きい。加えて
  評価ループの差し戻しが平均1回増えるだけで単価差の節約は相殺される。降格は「品質が一切落ちない」
  ことが前提であり、原則として推奨しないという方針を採った。
- **品質ゲートの記録は集計可能な形式に統一する**: 判定結果・根拠・評価ノードを定型の1行（行頭一致）
  で記録する運用にしたのは、後から `grep` 等で集計し、判定基準の妥当性を定期的に振り返るためである。
  自由記述では、後日の分析コストが再び個別確認に戻ってしまう。

---

## 4. 未決事項・将来の再検討ポイント

- **hooks（層③）の採用可否**: 現時点では見送っているが、配布体制（マーケットプレイス公開状況・
  署名検証等）が変化した場合、または運用実績を見て再評価する前提を残している。採用する場合は
  `.claude/settings.json` への書き込み拒否と `/hw:init` での提案的追記が正面から矛盾するため、
  実行コンテキストの識別方法を実機検証したうえで判断する必要がある。
- **ガードレール案のうち保留・第3弾以降の項目**: 危険コマンドの実行時ブロック、秘密情報スキャンの
  独立スクリプト化と利用側リポジトリへの適用、依存パッケージ導入時の確認手順等は、今回未着手の
  まま保留されている。運用実績を見ながら再検討する。
- **`haiku` 階層の再評価トリガー**: 現時点で該当エージェントは無いが、将来のモデル世代交代で
  コンテキスト長・思考機能・知識鮮度の階層間差が縮まった場合、CONTRIBUTING 2.5 のチェックリストで
  再評価する。
- **CONTRIBUTING.md の章分割の要否**: 大型文書の分割は見送り、部分読み込み規約で代替した（2.24）。
  規約運用後に `.claude/scripts/aggregate-agent-token-usage.sh` で保守案件のトークン消費を実測し、削減が
  不足する場合に限り、自己完結性の高い手順章（2〜4章・8章）の切り出しを第2弾として再検討する。
- **品質ゲート判定基準の定期振り返り**: レベル1/2と判定した案件から不具合・セキュリティ事象が
  発生した場合、および3か月または20案件ごとに、`qa-lead` が判定基準を見直す運用としている。
  この振り返りをどう記録・反映するかの実務は、運用実績が積み上がってから固まる見込みである。
- **エージェント間の委任実績の定量把握**: 従来は限られた実測に基づく推定（1回あたり数万トークン）
  にとどまっていたが、2026-07-30 の対応で `.claude/scripts/aggregate-agent-token-usage.sh` を新設し、
  `~/.claude/projects/` 配下のトランスクリプトからエージェント種別ごとの委任回数・トークン使用量
  （input/output/cache_creation/cache_read）およびエージェント別キャッシュヒット率を実測できるように
  なった。今後の課題は、この実測値を
  継続的に振り返りへ反映する運用（前項「品質ゲート判定基準の定期振り返り」との統合可否を含む）を
  固めることに移る。
- **マージ高度化（2.25節の対スキル新設に伴う保留事項）**: `hw:import-assets` の衝突解決における
  「マージ」選択肢は、AI草案＋利用者確認の初版に留めている。マージ判断基準の精緻化、出自履歴
  （複数ソースの移入記録）の統合方法、共通祖先が存在する場合の3-way比較、マージ後資産の品質ゲート
  扱い（新規承認相当か既存資産の再検証相当か）は、別案件として今後検討する。
- **ガードレール共通ブロックの予算を空ける経路（2.28関連）**: 常時層（CONTRIBUTING 1.5の短文6行・
  1.8の判断手順1行）を定期的に棚卸しして陳腐化・重複した条項を統合・降格する運用は、CONTRIBUTING
  1.9「共通ガードレール層の棚卸し運用」として運用化済み（2.29）。一方、2.3で見送った hooks 層③
  採用の再評価は、本章冒頭「hooks（層③）の採用可否」のとおり未決のまま残る。
- **`.claude/scripts/sync-guidelines.sh` の自動実行（CI・hook組み込み）の要否（2.30関連）**: 2.30で新設した
  正典lib＋同期スクリプトは、現時点では保守者が明示実行する道具に留め、正典変更の伝播を
  CI・hook等から無人で自動実行する構成は組み込んでいない（自動反映が置換失敗を無人で握りつぶす
  リスクを避けるため）。運用実績を見て、CI連携等の自動化を再検討する。再検討の際は、同スクリプトが
  git HEAD の lib を source する構造上、PR 由来ブランチの HEAD を無条件に信頼する形にならないよう、
  信頼前提（`.claude/scripts/sync-guidelines.sh` ヘッダの「信頼前提」節）の sec-audit による再評価を必須と
  する（T4 ゲート sec-audit 再評価の申し送り事項）。

---

## 関連文書

- [README.md](README.md) — 利用者向けの機能・使い方・組織図・ワークフロー設計5原則・モデル割り当て表
- [DEVELOPMENT.md](DEVELOPMENT.md) — 保守環境・プラグイン読み込みの仕組み・検証手順
- [CONTRIBUTING.md](CONTRIBUTING.md) — エージェント/コマンド/スキルの追加・変更手順、命名・文体規約、
  ライセンス条件（CLA）
