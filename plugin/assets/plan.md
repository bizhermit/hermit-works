# <件名>

<!-- 実行計画のひな型の正本。計画ディレクトリ直下の index.md にこの形式で置く。
     案件に属する計画は .hw/plans/<YYYYMMDD>_<案件名>_<計画名>/、案件に属さない計画は .hw/plans/<YYYYMMDD>_<計画名>/。 -->

- 状態: <未着手 | 進行中 | 完了 | 中断 | 中止>
- 背景（Why）: <何を解決する計画か。2行以内>
- 完了条件: <この計画の完了条件。2行以内>
- スコープ外: <今回やらないこと。無ければ「なし」>

## 依頼原文

<!-- この計画への依頼を原文のまま引用する（各行の行頭に > を付す）。案件に属する計画は案件層からスコープを限定した依頼を、案件に属さない計画は利用者の依頼原文を引用する。記録済みの引用は書き換えず、追加の依頼は受けた順に追記する -->

> <この計画への依頼内容>

## タスク

<!-- 1タスク1項（### 見出し）。タスクは正誤を判定できる単位まで細分化し、依存の無いタスクは並列実行可。担当には計画のパスとタスクIDだけが渡されるため、この項を読めば着手できる内容で書く。各欄は複数行・箇条書きで書いてよい。入力・成果物は計画ディレクトリ配下または対象ファイルのパスで示す。記録は着手後に統括が追記する欄で、計画時は「-」とする -->

### T1 <タスク名>

- 状態: 未着手
- 依存: <先行タスクのID。無ければ「-」>
- 担当: <委任先のエージェント名または役割。統括自身が行うなら「統括」>
- 目的: <このタスクで何を達成するか。計画の完了条件のどこに対応するか>
- 作業: <何をどうするか。正誤を判定できる粒度で書く>
- 完了条件: <このタスクを完了と判定する条件>
- 入力: <参照すべきファイル・前段の成果物のパス>
- 成果物: <出力先のパス>
- 記録: <実行結果・補足。品質ゲートのタスクは評価の記録もここへ。着手前は「-」>

## 評価

<!-- 実行計画の評価の記録欄。判定結果・差し戻し回数（n/上限）を記録する。案件に属さない計画では利用者の求めに応じて実施する。手順8への差し戻しで再評価が発生したら、既存の行は書き換えず「n周目」を付した行を追記する -->

- 判定: <未実施 | 合格（差し戻しn/上限） | 差し戻しn/上限>

## 検収

<!-- 計画完了検収の記録欄。判定結果・差し戻し回数（n/上限）・利用者確認の結果を記録する。案件に属さない計画では対象外とし、未実施のまま残す。手順8への差し戻しで再検収が発生したら、既存の行は書き換えず「n周目」を付した行を追記する -->

- 判定: <未実施 | 合格（差し戻しn/上限） | 差し戻しn/上限>
- 利用者確認: <未実施 | 確認済み（結果） | 仮採用（完了報告で追認）>

## 利用者裁定待ちの決定事項

<!-- 該当がある場合のみ設ける。1論点1項で書く -->

## 裁定記録

<!-- 該当がある場合のみ設ける。裁定を受けた順に記録する。利用者に確認せず推奨案を採用した裁定は、自動裁定による仮採用であることを明記する -->

- <YYYY-MM-DD> <論点>: <裁定内容>

## 手順資産

<!-- 該当がある場合のみ設ける。この計画が参照・変更した手順資産（.hw/procedures/）を1件1行で記録する -->

- <YYYY-MM-DD> <参照 | 蒸留 | 反映 | 分裂>: <手順資産のパス>（<1行の補足。無ければ省略>）

## セッション記録

<!-- この計画を進めたセッションを、着手した順に記録する。セッションを跨いで引き継ぐ際に、どの記録がどのセッションの手によるものかを辿るために用いる。計画に着手した時点で1行追記し、記録済みの行は書き換えない -->

- <YYYY-MM-DD> <セッションID>: <このセッションで進めた範囲>
