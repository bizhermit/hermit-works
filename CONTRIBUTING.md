# CONTRIBUTING

このリポジトリは Claude Code 向けプラグイン「hermit-works」（プラグイン名 `hw`）の定義一式です。
中身は `agents/*.md`（専門家エージェント定義）・`commands/*.md`（`/hw:` スラッシュコマンド定義）・
`skills/<skill-name>/SKILL.md`（`hw:` 作業手順書スキル）の3種類と、それらを支える検証・生成スクリプト
（`.claude/scripts/`。1.2・8章参照）、同じ検証を push/pull_request 時に自動実行する CI 定義
（`.github/workflows/`。8章参照）で構成されます。

開発環境（devcontainer）やプラグインの読み込みの仕組みは [DEVELOPMENT.md](DEVELOPMENT.md) を
参照してください。

## 目次

- 1. 貢献の進め方
  - 1.1 エージェント追加の判断基準（デフォルト却下）
  - 1.2 定型作業はスクリプト/skill 化を優先する
  - 1.3 エージェントの削除・統合
  - 1.4 ガードレール追加の設計原則
  - 1.5 共通ガードレール文の逐語コピー必須
  - 1.6 完了報告への証跡の明記（推奨）
  - 1.7 非リーダー連携節の委任制限文（TK-2/E-1）に関する注記
  - 1.8 判断手順共通文（迷った場合の段階的判断）の逐語コピー必須
  - 1.9 共通ガードレール層の棚卸し運用
  - 1.10 保守運用の都合で配布資材を変更しない
  - 1.11 依頼の2分類（プラグインのメンテナンスとリポジトリの環境整備）
- 2. 新規エージェント追加手順
  - 2.1 ファイルの置き場所
  - 2.2 命名規則
  - 2.3 frontmatter テンプレート
  - 2.4 本文の構成
  - 2.5 モデル割り当て方針
  - 2.6 README 更新（必須）
  - 2.7 チェックリスト
- 3. 新規コマンド追加手順
  - 3.1 frontmatter テンプレート
  - 3.2 `$ARGUMENTS` の使い方
  - 3.3 本文の構成の目安
  - 3.4 README 更新（必須）
  - 3.5 チェックリスト
  - 3.6 保守用ローカルコマンド
- 4. 新規スキル追加手順
  - 4.1 ディレクトリ構成
  - 4.2 frontmatter テンプレート
  - 4.3 本文の構成の目安
  - 4.4 README 更新（必須）
  - 4.5 チェックリスト
  - 4.6 保守用ローカルスキル
- 5. 変更後の確認手順（必須）
- 6. 命名・文体規約
- 7. 貢献のライセンス条件（CLA）とクレジット
  - 7.1 ライセンス条件（許諾型CLA）
  - 7.2 クレジット
- 8. スクリプト・CI 定義の追加・変更手順
  - 8.1 適用範囲と1.2との役割分担
  - 8.2 配置・命名・ファイルモード
  - 8.3 スクリプトの実装方針
  - 8.4 回帰テストの要否判断と `.claude/scripts/tests/` の構成
  - 8.5 CI 定義の変更手順
  - 8.6 索引・構成表の更新
  - 8.7 品質ゲートと完了条件
  - 8.8 チェックリスト
- 9. ブランチ運用・リリース手順
  - 9.1 ブランチ運用
  - 9.2 リリース手順（develop→main のリリース PR 方式）
  - 9.3 チェックリスト

目次は章・節の見出しを追加・削除・改題した際に同一コミットで更新してください。

## 1. 貢献の進め方

- 作業はブランチ運用・リリース手順（9章）に従って進めてください。main への直接コミットは
  禁止です。
- **このプラグイン自体の改善も Claude Code を使って行う**方針です。エージェント追加・コマンド改修・
  スキル整備といった変更は、人手で直接編集するのではなく、Claude Code のセッション上で本ドキュメントの
  手順に従って進めてください（例: `docs-tech` にエージェント追記を依頼する、`eng-lead` 相当の判断で
  新規エージェントの役割を設計する、等）。
- 変更は「1コミット = 意味的に一体の変更のまとまり」を基本単位にしてください。通常は
  1エージェント/1コマンド/1スキルの追加または変更が1コミットの単位になりますが、特定の機能を
  実現するために複数の資材（例: エージェント＋コマンド＋スキル）が一体で必要な場合は、それらを
  まとめて1コミットにしてかまいません。まとめて行う場合も、README の該当表はすべて追記漏れなく
  更新します。
- 既存のエージェント・コマンド・スキルの**役割の重複**がないか、着手前に `README.md` の「組織図」
  「使い方」「スキル」の各表と `agents/` `commands/` `skills/` の一覧を確認してください。似た役割が
  既にある場合は新規追加ではなく既存定義の改修を検討します（判断基準は 1.1）。
- 本ドキュメントや `DESIGN.md` 等の大型文書を参照する際、委任側は対象の章・節を指定し、読む側は
  指定された範囲のみ部分読み込みしてください（冒頭目次または見出し検索で該当箇所を特定します）。
  文書横断の整合確認等、スコープ上全体の把握が必要な場合の全文読みは妨げません。
- 方針に迷う場合（配置先・粒度・形式・既存規約との整合等）は docs-lead に確認してから進めてください。
- 貢献物のライセンス条件とクレジットの扱いは7章を参照してください。**Pull Request の送信は7章の条件への
  同意とみなされます。**

### 1.1 エージェント追加の判断基準（デフォルト却下）

エージェントの追加は**デフォルトで却下**とし、以下5項目が**すべて YES** のときに限り追加を認めます。
1項目でも NO・判定不能があれば追加しません。エージェントは増えるほど呼び出し側の選択コストが上がり、
境界の重複が組織全体のルーティング精度を下げるため、「あると便利」は追加理由になりません。
削除・改名は破壊的変更（1.3）で取り消しが高くつく点も、判断を慎重にする理由です。

| # | 基準 | 判定方法（どう確かめるか） |
|---|---|---|
| 1 | **境界の非吸収性**: 既存のどの `description` にも2文以内の追記で自然に収まらない | README「組織図」から役割が近い既存エージェントを最大3体挙げ、**実際に追記文案を2文以内で書いてみる**。書けてしまう場合、あるいは書いた結果「いつ呼ぶか」が他と競合しない場合は、追記による解決とし追加は却下する |
| 2 | **反復性**: 反復的な委任シナリオを3つ以上、具体的状況として書き出せる | 「〜のとき、〜を依頼する」の形式で3件を書き出す。`.hw/plans/` `.hw/reports/` または直近の実作業から**実例として特定できること**。3件のうち2件以上が同一案件由来なら「反復的」とみなさない。業務領域の言い換え（例:「データ処理全般」）は不可 |
| 3 | **失敗実績**: 専任化しないと品質が落ちる、再現可能な失敗事例がある | 既存エージェントに任せた結果の具体的な失敗（差し戻し・手戻り・見落とし）を、日付／案件名／成果物のいずれかで**特定できる形で1件以上**示す。「専任でないと品質が落ちそう」という予測・推測は不可。実績がない場合は、まず既存エージェントに任せて実績を作る（＝現時点では却下） |
| 4 | **代替不能**: skill 化・コマンド化・スクリプト化・既存定義への追記で代替できない | 1.2 の対応表に沿って4つの代替手段を**1つずつ検討し、却下理由を1文ずつ書く**。1つでも実現できる手段があれば追加を却下する |
| 5 | **恒久性**: 恒久的な専門性として定着させる確信がある | 「この専門性は1年後も必要か」に YES と答えられるか。特定案件の期間限定、繁忙期のみ、試行目的（「まず追加して様子を見る」）はいずれも不可。削除が MAJOR 相当（1.3）である前提で判断する |

判定結果（5項目それぞれの根拠）は、追加時のコミットメッセージまたは `.hw/` 配下の計画・報告に
残してください。判定に迷う場合は docs-lead に確認します。

背景・理由は [DESIGN.md](DESIGN.md) 2.8 を参照してください。

### 1.2 定型作業はスクリプト/skill 化を優先する

定型作業は**エージェントへの委任ではなく、スクリプト化・skill 化を優先**します。エージェント本文に書いた
機械的な手順は実行のたびに揺らぎ、検証もできません。判断を含まない手順ほどコードに寄せてください。

| 作業の性質 | 実現手段 |
|---|---|
| 手順が固定で判断を含まない（検証・集計・整形・生成） | `.claude/scripts/` の bash スクリプト（`.claude/scripts/validate.sh` 方式。8章） |
| 判断は要るが手順は定型で、対象や文脈が案件ごとに変わる | `skills/<skill-name>/SKILL.md`（4章） |
| 利用者が明示的に起動する入口が必要 | `commands/<command-name>.md`（3章） |
| 上記のいずれでも表現できず、専門的な判断そのものが価値 | エージェント（1.1・2章。1.1 の5項目をすべて満たす場合のみ） |

既存エージェントの本文に定型手順を書き足したくなった場合も、まずスクリプト化を検討してください。
スクリプト化した手順は、エージェント本文からは手順を再掲せず「`.claude/scripts/<name>.sh` を実行する」と
参照させます。

スクリプト（`.claude/scripts/`）・CI 定義（`.github/workflows/`）を選んだ場合の配置・命名・実装方針・
完了条件は8章を参照してください。

背景・理由は [DESIGN.md](DESIGN.md) 2.9 を参照してください。

### 1.3 エージェントの削除・統合

**削除・改名は MAJOR 相当の破壊的変更**です。エージェント名は利用者が `@hw:<name>` で直接指定するほか、
他エージェントの `## 連携`・README・`commands/`・`skills/` から名前で参照されるため、削除・改名は公開API
の削除に相当します。統合（A を B に吸収する）も A の名前が消える以上、同じ扱いです。CHANGELOG には
MAJOR の破壊的変更として記録し、吸収先を明記します（`CHANGELOG.md` は初回リリース時に作成予定です）。

#### 削除・統合を検討すべき兆候

以下のいずれかに該当したら棚卸しの対象とします（該当＝即削除ではなく、検討の起点です）。

- **境界の消失**: 他エージェントの `description` と担当業務が重なり、呼び出し側がどちらを選ぶべきか判断できない
- **専任化根拠の消滅**: 1.1 の基準3で示した失敗事例が、他エージェントの改善・スクリプト化・手順整備によって
  すでに解消されている
- **成果物の二重管理の再発**: 同じ文書・設定を複数エージェントが所有し、所有者・整合ルールを決め直しても
  重複更新が再発する
- **恒常的な低接続**: 一定期間、他エージェントの `## 連携` からも実案件（`.hw/` の計画・報告）からも
  参照されていない

#### 段取り（非推奨化 → 予告 → 削除）

1. **提起と合意**: 上記の兆候を1つ以上具体的に示し、該当グループの `*-lead` と docs-lead で**吸収先**を決めます。
   吸収先が決まらない削除は行いません（担当業務が宙に浮くため）。
2. **非推奨化**: ファイルは削除せず、`description` の冒頭に「（非推奨: `<吸収先>` を使用）」を付記し、
   担当業務・連携の記述を吸収先へ移します。統合の場合は、吸収先の `## 担当業務` `## 連携` に移管内容を
   追記してからこの手順に進みます。CHANGELOG には非推奨化として記録します。
3. **予告と参照の掃き出し**: README「組織図」の該当行に非推奨である旨と吸収先を明記し、他エージェントの
   `## 連携`・`commands/`・`skills/` に残る当該名の参照を吸収先へ置換します。
   `grep -rn "<name>" agents commands skills README.md` で**残存参照がゼロ**になったことを確認します。
   ただし `commands/show-org.md` は `.claude/scripts/generate-show-org.sh` の生成物のため手編集せず、
   `bash .claude/scripts/generate-show-org.sh` を実行して再生成してください（手順4の削除時も同様です）。
4. **削除**: 残存参照ゼロを確認できてから `agents/<name>.md` を削除し、README の組織図・人数表記・
   モデル割り当て表を更新し、`bash .claude/scripts/generate-show-org.sh` を実行して `commands/show-org.md` を
   再生成したうえで5章の確認手順を実行します。CHANGELOG には MAJOR の破壊的変更として、
   削除したエージェント名と移行先を記録します。

非推奨化から削除までは**最低1リリース間隔を空けます**（同一リリース内での追加・即削除は行いません）。

### 1.4 ガードレール追加の設計原則

層①（エージェント・コマンド定義への記述）の規範は増やすほど1件あたりの追従率が下がります
（instruction dilution）。ガードレールを追加した結果、既存の規範（利用者資材優先の共通文等）まで
薄まっては本末転倒のため、層①へ追記する前に以下を確認してください。

1. 同じ効果が層②（`tools`/`disallowedTools`）または層④（`.claude/scripts/validate.sh`）で得られるなら、
   そちらを優先する。層①は他レイヤーで代替できない場合の手段とする。
2. 全エージェント共通のガードレールは各エージェントの `## 作業方針` に散らさず、既存の共通文
   （利用者資材優先の1段落＋短文6行。1.5参照）に集約する。**この共通ブロックは短文6行で予算を
   使い切っている**。新規条項を追加する場合は既存6行のいずれかとの入替・統合を検討し、単純な
   追加（7行目以降）は行わない。
3. 禁止事項は「〜しない」を単独行の短文で書く。1文の中に複数の別条件・別例外を混在させない
   （必要な例外条項自体は1つまでなら残してよい）。
4. 追加する規範は「破られたときにどう気づくか」（検知手段）をセットで定義する。検知手段のない
   規範は追加しない。
5. 破られると不可逆な損害が出る制約は、可能な限り層②/④で担保する。層①のみに置く場合は、
   実行時のハード制約を欠く残存リスクであることを明記する（本リポジトリは hooks 層③を採用して
   いないため、破壊的操作・秘密情報等の一部条項は現状①のみに依存する。将来 hooks を採用する
   場合に再評価する）。
6. 既存方針・過去の裁定（本節や1.8の判断手順共通文、`DESIGN.md` の既存決定を含む）と抵触する
   ことを、それ単独では追加提案の棄却理由にしない。可否は実害範囲・検知手段・他層代替可否
   （上記1・4・5の観点に相当）のメリット審査で判断し、審査結果と根拠を記録する。既存方針側に
   見直すべき点があれば、抵触を理由に提案を退けるのではなく、既存方針側の改訂・削除もあわせて
   検討する（適用は1.8、プラグイン全体の運用は `agents/mgmt-coordinator.md`「作業方針」節を正本とする）。

背景・理由は [DESIGN.md](DESIGN.md) 2.3 を、既存方針との抵触時の棄却規律については同 2.28 を
参照してください。

**配置基準（判定順）**: 新規の共通ルールをどの層・形式に置くか迷う場合は、次の判定順で決めてください。

1. 機械層で代替可能か: 層②（`tools`/`disallowedTools`）または層④（`.claude/scripts/validate.sh`）で
   同じ効果が得られるなら、そちらを優先する（原則1の具体化）。
2. 状況依存で対象エージェントを絞れるか: **トリガー行1行＋詳細スキル（`hw:permission-rules` 方式。
   DESIGN 2.27 追補）を標準の既定先とする**。層①側にはトリガー行1行のみを置き、詳細規則は
   スキル本文に集約する。
3. 全域・常時必要か: 上記いずれにも該当しない、全エージェント常時参照が必要な規範のみ、常時層
   （1.5 の短文6行・1.8 の判断手順1行）との入替・統合を検討する。この経路は 1.8 の審査ゲートを
   経てから採用してください。

**トリガー行の新設要件**: 上記2の経路でトリガー行を新設する場合は、以下をすべて満たしてから
採用してください（1点でも未達の場合は採用しません）。

(i) 実機検証: クリーン環境（汚染源のない模擬利用者リポジトリ・実サブエージェント委任）でスキルへの
    到達を確認する（2.3 節「ハード制約の導入は実機検証を採用条件とした理由」と同旨。前例は 2.27
    追補の permission-rules 検証）。
(ii) `.claude/scripts/validate.sh` への逐語検証の追加: 原則4「検知手段のない規範は追加しない」を適用し、
    トリガー行自体の欠落・改変を機械検証の対象にする。
(iii) 対象エージェントの限定: 当該規範に関与する範囲のエージェントに絞り込み、無関係なエージェント
    への一般化は希釈（instruction dilution）を招くため避ける。

### 1.5 共通ガードレール文の逐語コピー必須

`## 作業方針` 冒頭の共通ガードレール文（利用者資材優先の1段落＋短文6行）は、全 `agents/*.md`
で**逐語一致**が必須です。新規エージェント追加時・既存エージェント編集時のいずれも、正典
（`.claude/scripts/lib/guidelines.sh` の `GUIDELINE_COMMON_LINES` 定数、または既存の任意の
`agents/*.md`）からそのままコピーしてください。文言を独自に言い換えない・要約しない・省略しない
でください。`.claude/scripts/validate.sh` は `.claude/scripts/lib/guidelines.sh` を source してこの逐語一致を
検証し、欠落・改変は ERROR になります（正典＝lib・検証＝validate.sh の役割分担）。

共通行の文言そのものを変更する場合は、(1) `.claude/scripts/lib/guidelines.sh` の該当定数を編集し、(2)
`bash .claude/scripts/sync-guidelines.sh` を実行して対象ファイル群へ機械的に伝播させ、(3)
`bash .claude/scripts/validate.sh` を実行して整合を確認してください（伝播＝sync。`.claude/scripts/sync-guidelines.sh`
の対象分配は `.claude/scripts/validate.sh` の検証分配と同一です）。背景は [DESIGN.md](DESIGN.md) 2.30 を
参照してください。

### 1.6 完了報告への証跡の明記（推奨）

新規エージェント追加時、`## 連携` の完了報告項目に検証系の作業が含まれる場合は、「検証済み」を
実行コマンドと出力抜粋を伴う場合のみ有効とする旨を、既存の同グループファイルの書式に合わせて
検討してください（全エージェントへの一律追加は行いません。`agents/mgmt-coordinator.md` は
既にこの検証義務を持ちます）。

### 1.7 非リーダー連携節の委任制限文（TK-2/E-1）に関する注記

非リーダー41体（`mgmt-coordinator` と `*-lead` 9体を除く全エージェント）の `## 連携` 節冒頭には、
「変更を伴う依頼は原則として自ら起動せず、完了報告の引き継ぎ事項に記載して呼び出し元の判断に委ねる。
ただし (a)〜(c) は自ら起動してよい」という委任制限文が逐語で入っています（2.4「連携セクションの
書式」参照）。この文を読む際、以下2点を誤解しないでください。

- **(b)（評価ループ・三役審議の評価者/視点役の起動）・(c)（読み取り専用の調査補助）は、当該ファイルの
  主体（非リーダーのそのエージェント自身）が実行できるという意味ではありません。** 非リーダー41体は
  frontmatter で `disallowedTools: Agent` を持ち、`Agent` ツールの直接呼び出しはできません。ただし
  `disallowedTools: Agent` は `Agent` ツール呼び出しのみを塞ぐ設計であり、`context: fork` スキル
  （4.2参照）を `Skill` ツール経由で呼ぶ経路までは塞ぎません。この経路は非リーダーからでも
  サブエージェント生成（構造的な迂回）を成立させうることが実機検証で確認されています
  （残余リスクとして DESIGN.md 2.35 に記録。フォーク先の実権限は `agent:` 省略時は呼び出し元の
  ツールプールに束縛されるため、実機検証の範囲では、この経路単体での権限昇格は確認されていません）。
  (a)(b)(c) は Agent ツールを保持するリーダー層（`*-lead`・`mgmt-coordinator`）が実行主体となる、
  組織全体の委任ルールとして書かれています。非リーダーの視点では、自らの連携依頼がどの例外経路
  （評価ループ／三役審議／読み取り専用調査）に該当しうるかを示す説明であり、非リーダー自身の許可事項
  ではありません。
- **統括・管理グループには専属の `*-lead` が存在しません。** `mgmt-pm`・`mgmt-planner`・`mgmt-release`・
  `mgmt-risk` の委任制限文にある例外(a)「自グループの `*-lead` が自グループ内の担当へ委任する場合」は、
  mgmt 群では `mgmt-coordinator` が組織全体の司令塔とグループ内リーダーを兼務するため、`mgmt-coordinator`
  を指します。

### 1.8 判断手順共通文（迷った場合の段階的判断）の逐語コピー必須

`## 作業方針` 末尾に置く「規約・命名・表記等の慣習的な判断（技術的トレードオフの裁定を除く）に迷った
場合の段階的判断手順」を圧縮した1行は、`agents/mgmt-coordinator.md` を除く `agents/*.md` 全件
（リーダー・非リーダー問わず）で**逐語一致**が必須です（1.5 の共通ガードレール文ブロック＝短文6行の
予算とは別物の1行であり、6行の枠には含めません）。正典は `.claude/scripts/lib/guidelines.sh` の
`GUIDELINE_DECISION_PROCEDURE_LINE` 定数、または既存の任意の `agents/*.md`（`mgmt-coordinator.md`
以外）です。文言を独自に言い換えない・要約しない・省略しないでください。`mgmt-coordinator` は自ら
利用者に直接確認できる立場のため、この共通文をそのまま持たず独自の手順（`agents/mgmt-coordinator.md`
「作業方針」参照）を記述します。`.claude/scripts/validate.sh` は `.claude/scripts/lib/guidelines.sh` を source して
この逐語一致を検証し、欠落・改変は ERROR になります。文言を変更する場合は
`.claude/scripts/lib/guidelines.sh` の当該定数を編集し、`bash .claude/scripts/sync-guidelines.sh` で対象ファイルへ
伝播したうえで、`.claude/scripts/validate.sh` 冒頭の検証内容一覧コメントも実態に合わせて更新してください
（手順の詳細は1.5参照）。

**別枠の共通行の新規追加は、この判断手順行をもって審査ゲートとします**（「6行予算の枠外」を口実にした
共通行の際限ない追加を防ぐための運用であり、追加を一切認めない打ち止めではありません）。以後さらに
別枠の共通行を追加したい場合は、1.4 原則2（既存行との入替・統合の検討を優先する）および1.4 原則6
（既存方針への抵触を単独の棄却理由にしない）と同等の審査を必須とします。既存行・既存の判断手順行と
抵触することを理由に単独で棄却せず、実害範囲・検知手段・他層代替可否のメリット審査で可否を判断
してください。それでも追加が必要と判断した場合は、審査結果と理由を `DESIGN.md` に記録することを
必須とします。

背景・理由は [DESIGN.md](DESIGN.md) 2.11 を、審査ゲート化・棄却規律の経緯は 2.28 を参照してください。

### 1.9 共通ガードレール層の棚卸し運用

常時層（1.5 の短文6行・1.8 の判断手順1行）・トリガー行＋スキル層（1.4 の判定順(2)）・機械層
（層②/④）に配置したガードレールは、以下の条件で棚卸しを行ってください。

**発動条件**:
(i) 1.8 の審査ゲートで常時層への追加審査が発生した都度（審査と同時に、既存行の入替・降格候補も
    あわせて点検する）。
(ii) 定期（品質ゲート判定基準の定期振り返り〔3か月または20案件ごと・`qa-lead`〕に相乗りする）。

**点検内容**:
- 常時層各行の実効性（違反検知実績の有無・陳腐化。一次情報源は `.hw/reports/` の品質ゲート記録と
  `.hw/decisions.md` の判断記録とする）。
- 降格候補の判定（常時層 → トリガー行＋スキル層、または機械層への降格。hooks 層③再評価のための
  材料収集を兼ねる）。降格を採用する場合、移行先の検知手段（1.4 のトリガー行新設要件(i)(ii)、
  または層②/④の実装）が実装・検証済みになるまで移行元の行を削除しない（検証の空白期間を作らない）。
- 類似の非信頼入力宣言3種（`.claude/scripts/validate.sh` セクション9(d)・9(f)・9(g)）の統合可否。

**実行主体**: 本節の棚卸しは本リポジトリの保守案件（CLAUDE.md の一次受け→ `/hw:request` フロー）
として実施します。発動条件(ii)の「相乗り」は実施時期を既存の定期振り返りに合わせるという意味であり、
`agents/qa-lead.md` 等のエージェント担当業務定義の変更を伴いません（エージェント定義側への反映の
要否は、棚卸しの初回実施時に判断します）。

棚卸しの結果は、品質ゲート記録と同様に `grep` で集計可能な行頭一致の定型1行
（`棚卸し: <実施日> / <対象> / <判定・採否>`）で [DESIGN.md](DESIGN.md) に記録してください。

背景・経緯は [DESIGN.md](DESIGN.md) 2.29 を参照してください。

### 1.10 保守運用の都合で配布資材を変更しない

本リポジトリの資材は、利用側リポジトリに配布されて動作する**製品**です。本リポジトリ自身を
保守するための運用上の都合（例: 保守用の定期実行ジョブとの連携、保守者向けの報告内容の調整）を
理由に、配布対象の資材（`agents/`・`commands/`・`skills/`・`scripts/`〔将来、配布用のスクリプトを
置く場合の予約パス。現在は使用実績なし。8.2参照〕・README 等の利用者向け文書）を変更しないで
ください。

配布対象と保守対象の判定基準は「配布されるか」ではなく、**利用側リポジトリで意味を持つか**です。
本プラグインは `source: "./"` によりリポジトリ全体が物理的には配布されるため、配布されるという
事実だけでは線を引けません。保守用スクリプト（`.claude/scripts/`）・CI 定義
（`.github/workflows/`・`.github/dependabot.yml`）・保守作業専用文書等、本リポジトリ自身の保守に
のみ意味を持つ資材は、利用側リポジトリへ渡っても解決しないパス・本リポジトリ固有の運用を前提と
するため、配布対象から外れます。

- 保守運用の仕組みは資材の外で完結させます。実現手段は、スケジュール実行（claude.ai の routines
  等）・gitignore 済みの `.hw/` 配下・GitHub の issue／ラベル等リポジトリ外のメタデータ・
  セッション上での都度指示から選びます。
- 資材の変更に進んでよいのは、その変更が**利用者一般に価値のある製品改善**に該当する場合のみです
  （この場合は通常どおり CLAUDE.md の一次受け → `/hw:request` フローに乗せます）。
- 判定に迷う場合は「この変更は利用側リポジトリに配布されて意味があるか」を基準にし、NO なら
  資材の外で実現します（それでも迷う場合の確認先は1章冒頭と同じく docs-lead）。
- 保守作業専用の文書（CLAUDE.md・CONTRIBUTING.md・DESIGN.md・DEVELOPMENT.md）は配布物ではない
  ため本節の禁止対象外ですが、変更は通常の資材変更と同じフロー（品質ゲート含む）に従います。
- **配布側の資材（`agents/`・`commands/`・`skills/`・README 等）は、保守用スクリプト
  （`.claude/scripts/` 配下の実体・内部の節番号・定数名等）を名指しで参照してはなりません。**
  配布側の記述は、利用側リポジトリ単独で意味が通る表現に留めます。逆方向（保守作業側の文書・
  `.claude/` 配下の資材から、配布側の資材や、将来設ける場合の配布用スクリプトを参照すること）は
  差し支えありません。

背景・経緯は [DESIGN.md](DESIGN.md) 2.33 を参照してください。

### 1.11 依頼の2分類（プラグインのメンテナンスとリポジトリの環境整備）

本リポジトリで受け付ける依頼は、性質に応じて次の2つに分類してください。分類によって許容範囲が
変わります（受付時の判定動作は CLAUDE.md「hermit-works への振り分け（一次受け）」節を参照）。

- **プラグインのメンテナンス**: 利用側リポジトリに届いて動く製品、すなわち1.10 が列挙する配布
  対象の資材（`agents/`・`commands/`・`skills/`・`scripts/`〔将来配布用スクリプトを置く場合の
  予約パス〕・README 等の利用者向け文書）への変更を伴う依頼。
- **リポジトリの環境整備**: 本リポジトリで作業を回すための仕組み（保守作業専用文書
  〔CLAUDE.md・CONTRIBUTING.md・DESIGN.md・DEVELOPMENT.md〕・`.claude/`〔保守用スクリプト
  `.claude/scripts/` を含む〕・`.github/`〔`.github/workflows/`・`.github/dependabot.yml` を
  含む〕・`.hw/`・`.vscode/`・`.devcontainer/` 等）を整える依頼。配布物ではないため1.10 の
  禁止対象外ですが、保守作業専用文書の変更は1.10 が定めるとおり通常の資材変更と同じフロー
  （品質ゲート含む）に従います。

両者の扱いの違いは次のとおりです。

- **リポジトリ固有の事情の書き込み**: プラグインのメンテナンスでは不可です（配布物は汎用表現に
  限り、書き込んでよいのは利用者一般に価値のある改善のみです。1.10 参照）。リポジトリの環境整備
  では、本リポジトリ固有の運用事情を書き込んで構いません。
- **保守運用の都合を理由にした変更**: プラグインのメンテナンスでは1.10 の禁止対象です。リポジトリ
  の環境整備が対象とする文書・ディレクトリ（保守作業専用文書・`.claude/`・`.github/`・
  `.hw/`・`.vscode/`・`.devcontainer/` 等）は、いずれもこの制約を受けません。根拠系統は対象ごとに
  異なります。保守作業専用文書は1.10 が「配布物ではないため本節の禁止対象外」と定める対象そのもの、
  `.hw/` は1.10 が「資材の外で完結」の実現手段として明示する対象、`.claude/`・`.github/`・
  `.vscode/`・`.devcontainer/` は1.10 の配布対象列挙に元々含まれず禁止の前提自体が成立しません。
- **CHANGELOG・version の対象可否**: 分類そのものではなく、9.2 が定める「利用者面資材／保守面
  資材」の区分が判定基準です（正本は9.2）。9.2 時点の目安では、利用者面資材（`agents/`・
  `commands/`・`skills/`・`.claude-plugin/plugin.json`・README.md）は対象、それ以外（`.claude/`
  〔保守用スクリプト `.claude/scripts/` を含む〕・`.github/` を含む保守面資材、およびリポジトリの
  環境整備に属する変更）は対象外です。
- **品質ゲート**: いずれの分類でも、変更内容に応じて `agents/mgmt-coordinator.md`「品質ゲートの
  適用判定」節のレベル判定に従います（正本は同節）。振る舞い・設定を定義する文書（エージェント・
  コマンド・スキル・CLAUDE.md 等）はレベル2以上、CI 定義はレベル3に該当する点は同節のとおりです。

分類の判定は次の優先順で行います。

1. 上記の列挙（プラグインのメンテナンス／リポジトリの環境整備）に該当するパスへの変更は、その
   分類を適用します。
2. 配布資材と保守作業専用文書の両方を変更する依頼は、9.2 が「利用者面・保守面が混在する変更は、
   リリース側（利用者面）として扱います。」と定めるのと同じ考え方で、プラグインのメンテナンス側を
   適用します（配布資材を含むことが明らかで判定に迷う余地がないため、確認は不要です）。
3. 1.10・上記いずれの列挙にも該当しないパス（`.claude-plugin/`・`LICENSE`・`CHANGELOG.md`・
   `.gitignore`・`.gitattributes`・`.editorconfig` 等）のみの変更は、
   「この変更は利用側リポジトリに配布されて意味があるか」を基準に判定します。明らかな場合はその
   分類を根拠とともに採用してください。明らかでない場合は独断で決めず、まず1.10・1章冒頭と同じ
   確認先である docs-lead に確認します。それでも決まらない場合は、判断材料・選択肢・推奨案を
   添えて利用者に確認し、確認結果を `.hw/decisions.md` へ記録したうえで、以後の同種判断は記録済みの
   定め（文書化された定め）に従ってください。この段階的な確認手順は規約・命名・表記等の慣習的な
   判断に迷った場合の段階的判断手順そのものであり、本節固有の新しい規律ではありません（一般
   エージェントの手順は CONTRIBUTING 1.8、統括自身が判断する場合の手順は
   `agents/mgmt-coordinator.md`「作業方針」を参照してください）。

背景・経緯は [DESIGN.md](DESIGN.md) 2.45 を参照してください。

## 2. 新規エージェント追加手順

着手前に 1.1 の判断基準5項目をすべて満たしていることを確認してください（1項目でも NO なら追加しません）。

### 2.1 ファイルの置き場所
`agents/<group>-<role>.md` に新規作成します。ファイル名は frontmatter の `name` と完全一致させます。

### 2.2 命名規則
`<group>-<role>` の kebab-case。`<group>` は以下10種のいずれかを使用します（新規グループの新設は
docs-lead の承認が必要です）。

| プレフィックス | グループ |
|---|---|
| `strat` | 経営・戦略 |
| `mgmt` | 統括・管理 |
| `biz` | ビジネス・グロース |
| `ana` | アナリスト |
| `eng` | エンジニア |
| `infra` | インフラ・プラットフォーム |
| `qa` | 品質管理・QA |
| `sec` | セキュリティ |
| `docs` | ドキュメント・L10n |
| `ai` | AIサポート |

`<role>` はハイフンを含まない単一トークンで表現します（例: `backend`, `architect`, `l10n`）。複数の
意味を表したい場合も、ハイフンで連結せず `l10n` のように連結・省略した1トークンにまとめてください。
既存エージェントと役割が被らない名前を選びます。

#### 新規グループ新設の判断基準

グループの新設は1.1のエージェント追加基準よりもさらに慎重に扱います（グループはエージェントより
上位の分類であり、波及箇所が多く取り消しづらいため）。1.1の5基準に準ずる考え方で、以下をすべて
満たさない限り却下します。

- 既存10グループ（上表）のいずれの領域にも収まらない（1.1基準1「境界の非吸収性」に相当）
- 専任エージェントを2体以上具体的に見込める（1.1基準2「反復性」に相当。1体分の役割は既存グループ
  内の新規エージェント追加で対応する）
- 恒久的な分類として定着させる確信がある（1.1基準5「恒久性」に相当。試行目的の新設は不可）

（1.1基準3「失敗実績」・基準4「代替不能」は、グループという分類そのものには失敗事例・代替手段の
検討対象となる実体がなく、個々のエージェント追加時にのみ意味を持つ基準のため、グループ新設の
判定には準用しません。）

判定に迷う場合はデフォルト却下とし、docs-lead に確認してください。承認が得られた場合も、以下の
波及箇所すべてに追従してから完了とします。

- 2.2 プレフィックス表（上表）への行追加
- README「組織図」表への新グループ節追加
- `bash .claude/scripts/generate-show-org.sh` の再実行（`commands/show-org.md` の再生成）
- `## 出力` セクションの文言（2.4「出力セクションの文言」参照。新規グループは docs-lead に確認）
- `.claude/scripts/validate.sh` の `ALLOWED_GROUPS` 定数への追加。エージェント名の書式検証
  `naming-format` が対象とする既定10種のグループリストであり、README一覧との突合チェック
  `readme-sync` とは別の検証です

### 2.3 frontmatter テンプレート
以下をそのままコピーして3項目を埋めてください（`name`/`model`/`description` の3項目とも必須）。

```yaml
---
name: <group>-<role>
model: sonnet
description: <グループ名>グループの<役割名>として<担当業務を具体的に>を行う。<いつ呼び出すべきか>ときに呼び出す。
---
```

- `description` は呼び出し判断（ルーティング）に直接使われるため、**「役割・専門の1文」＋「呼び出しトリガーの
  1文」の計2文以内**、目安60〜100字で明記します。曖昧な表現（「〜のサポート」等）だけで終わらせず、担当業務の
  具体名を含めてください。
- 「いつ呼び出すべきか」の部分は、担当領域を言い換えただけの表現（例:「コンテナ関連の作業を依頼するときに
  呼び出す」）で終わらせず、**具体的な状況を列挙**してください（例: `agents/eng-api.md`「APIの新設・変更・
  レビュー時や、互換性を壊さない変更方法を検討するとき」）。呼び出し側が状況からルーティング可否を判断
  できることを基準にします。
- 「○○は△△が担う」等の担当境界・分界の説明は description に書かず、`## 担当業務`・`## 連携` 等の本文へ
  記載します（本文に同内容が既にあれば description からの削除のみでよく、なければ本文へ1行移設します）。
  情報は削減してよいものの、専門領域・呼び出し条件が失われエージェント選定を誤らせる削り方は避けてください。

#### ツール制約（オプション項目）
`tools`/`disallowedTools` はいずれもオプションで、未指定であればフルツール（`Bash` を含む）が使えます。
エージェントに書き込みをさせたくない場合のみ、以下いずれかの方式を選んでください。

- **原則: `tools:` 許可リスト方式**（例: `agents/qa-review.md`）。使用を許可するツールのみを列挙します。
  `Bash` を含めなければ、任意コマンド実行もできない厳密な読み取り専用エージェントになります。
  ```yaml
  tools:
    - Read
    - Glob
    - Grep
  ```
- **例外: `disallowedTools:` 拒否リスト方式**（例: `agents/sec-audit.md`）。診断コマンドの実行等で `Bash`
  自体は残す必要があるが、書き込みだけは禁止したい場合に限り使用します。
  ```yaml
  disallowedTools:
    - Write
    - Edit
  ```
  この方式を採る場合は、frontmatter だけでなく本文（`## 作業方針` 等）にも「診断・報告に専念し、コードや
  設定の自己修正は一切行わない」旨を明記し、ツール制約と本文の規律を一致させてください。

#### effort（オプション項目）
`effort` はオプションで、そのエージェントが使うモデルの reasoning effort（思考の深さ）を上書きします。
未指定時はセッション設定を継承します（既定）。指定する場合は `model:` の直後に置きます。

```yaml
---
name: <group>-<role>
model: sonnet
effort: low
description: <グループ名>グループの<役割名>として<担当業務を具体的に>を行う。<いつ呼び出すべきか>ときに呼び出す。
---
```

- 許容値: `low` / `medium` / `high` / `xhigh` / `max`（利用可能な段階はモデルに依存）
- 割り当て方針は 2.5「effort の割り当て方針」を参照してください

### 2.4 本文の構成
既存エージェント（例: `agents/eng-backend.md`）の見出し構成に合わせます。

```markdown
あなたは「hermit-works」開発組織の【<グループ名>】に所属する<役割名>です。

## 役割
（1〜3文で責務の要約）

## 担当業務
（箇条書き）

## 作業方針
（リポジトリ構成の前提〔単一/複数プロジェクト〕、確認すべき既存規約、実装後の検証手順等）

## 連携
（どんな場合にどのエージェントへ相談・引き継ぎするか。完了報告に含めるべき項目。書式は本節末尾を参照）

## 出力
（同じグループの既存エージェントの出力文言をそのままコピーする。詳細は本節末尾「出力セクションの文言」を参照）
```

#### リード級エージェントの追加セクション
各グループの `*-lead`（`ai-lead` `ana-lead` `biz-lead` `docs-lead` `eng-lead` `infra-lead` `qa-lead`
`sec-lead` `strat-lead` の9体）は、上記5見出しに加えて `## 作業方針` と `## 連携` の間に
`## 作業設計（ワークフロー5原則）` セクションを持ちます（直列分解・ルーティング・並列化・司令塔・評価ループ
の5原則を番号付きで記述。9体で文言が完全に一致しています）。`mgmt-coordinator` も同名の
`## 作業設計（ワークフロー5原則）` セクションを持ちます（司令塔ゆえの補足と `## 組織構成（振り分け先）` 等の
独自セクションを追加で持ちます）。

新規にリード級エージェント（新グループの `*-lead` 等）を追加する場合は、この
`## 作業設計（ワークフロー5原則）` セクションを既存リード（例: `agents/eng-lead.md`）から**逐語コピー**し、
見出し・番号・5原則の文言は変更しないでください。

`## 三役審議（多視点の意思決定）` は `strat-lead` `qa-lead` の2体のみが持つ**任意セクション**であり、
リード級の必須構成ではありません（新規リード追加時にこのセクションをコピーする必要はありません）。持たせる
場合の位置づけは `## 作業設計（ワークフロー5原則）` と `## 連携` の間で、`mgmt-coordinator` の
`## 品質ゲートの適用判定` と同様です。2ファイル間の共通骨格（定義・3視点の呼称と役割・マージ規則・裁定者）は
逐語一致とし、差し替えを許すのは「適用条件の具体例」と「判断領域の呼称（戦略判断/品質判断）」の2箇所のみです。片方のファイルだけを変更しないでください
（静的検証では検知できないため、変更時は必ず両ファイルの整合を目視確認してください）。

#### 複数ファイル間の逐語同期ブロック
上記の三役審議（`strat-lead`・`qa-lead` の2ファイル逐語同期）と同種の運用が、他のファイル組でも
採られています。`agents/sec-audit.md`・`agents/sec-appsec.md`・`agents/infra-devops.md` の
permissions トリガー行（DESIGN 2.27参照）が該当します。三役審議の2ファイル同期とは異なり、
こちらは `.claude/scripts/validate.sh` セクション9(h)（正典: `.claude/scripts/lib/guidelines.sh` の
`GUIDELINE_PERMISSION_TRIGGER_LINE` 定数）による逐語一致の静的検証を追加済みです（1.4 の
トリガー行新設要件(ii)を適用し、当初の目視確認のみの運用から移行しました）。変更する場合は
`.claude/scripts/lib/guidelines.sh` の当該定数を編集し、`bash .claude/scripts/sync-guidelines.sh` で3ファイルへ
伝播したうえで、`bash .claude/scripts/validate.sh` で整合を確認してください（手順の詳細は1.5参照）。

このトリガー行は「Claude Code の権限設定（`permissions`）を提案・変更・レビューする際は
`hw:permission-rules` スキルを読み込む」旨を伝える1行のみで、詳細規則（Bash パターンマッチングの
仕様）・確認手順・多層防御の考慮は `skills/permission-rules/SKILL.md` 側に集約しています
（実機検証により、この構成〔詳細をエージェント本文ではなくスキルへ集約し、エージェント側は
トリガー行1行でスキル参照を促す〕でも確実にガイダンスへ到達することを確認済みです。経緯は
DESIGN 2.27参照）。詳細規則を変更する場合はスキル側のみを更新し、3ファイルへのコピーは不要です
（3ファイルで同期が必要なのはトリガー行1行のみ。ただしトリガー行自体の文言を変更する場合は、
上記の手順（正典lib編集→sync-guidelines.sh実行→validate.shで確認）に従ってください）。

#### 連携セクションの書式
`## 連携` の箇条書きは、`- 〜は eng-lead に確認する。` のような**散文型**を標準とします。
`- eng-lead: 〜` のようなコロン列挙型は使いません。既存エージェント（例: `agents/eng-backend.md` の
`## 連携`）の文体に合わせてください。

**非リーダーエージェント（`mgmt-coordinator`・`*-lead` 以外）を新規追加する場合**は、`## 連携` の
最初の箇条書きとして、TK-2/E-1 委任制限文を既存の非リーダーファイル（例: `agents/eng-backend.md`）
から**逐語コピー**してください（1.7 も参照）。

```
- 『連携』に記載した**変更を伴う**依頼は、原則として自ら起動せず、完了報告の「引き継ぎ事項」に記載して呼び出し元の判断に委ねる。ただし次は自ら起動してよい: (a) 自グループの `*-lead` が自グループ内の担当へ委任する場合、(b) 作業設計5原則-5の評価ループおよび三役審議のための評価者・視点役の起動（グループ外を含む）、(c) 読み取り専用の調査補助。
```

文言を独自に言い換えない・要約しないでください。`.claude/scripts/validate.sh` がこの文言の逐語一致を
`agents/*.md` の非リーダー41体全件に対して検証します（欠落・改変は ERROR）。あわせて frontmatter に
`disallowedTools: Agent` を追加してください（TK-2/E-1 委任制限文と `disallowedTools: Agent` はセットで
初めて強制力を持ちます。既存 `disallowedTools` がある場合は配列に `Agent` を追加してマージします。
チェックリストは2.7参照）。

**例外（`tools:` 許可リスト方式との併用時）**: 2.3「ツール制約」の原則どおり `tools:` 許可リスト方式を
選び、その一覧に `Agent`/`Task` を含めない場合（例: `agents/qa-review.md` の
`tools: [Read, Glob, Grep]`）は、`disallowedTools: Agent` の追加は不要です。許可リストに
`Agent`/`Task` が無いこと自体で委任経路が塞がるため、`disallowedTools` を併記しても意味が重複します。
TK-2/E-1 委任制限文（本節冒頭）は方式によらず全非リーダーで必須のままです。`.claude/scripts/validate.sh` は
`tools:` が1件以上指定され、かつ `Agent`/`Task` を含まないエージェントを `disallowedTools: Agent` の
検査対象から除外します（`tools:` 未指定、または `tools:` に `Agent`/`Task` を含む場合は、
`disallowedTools: Agent` の記載を通常どおり要求します）。

#### 出力セクションの文言
`## 出力` の文言は、グループの性質に応じて既存の同グループファイルからそのままコピーしてください
（新規に文言を考案しない）。現状は以下の3系統があります。

| 系統 | 対象グループの目安 | 文言 |
|---|---|---|
| 一般 | strat/mgmt/biz/ana/infra/qa/sec 群 | `報告・成果物はすべて日本語で記述する。` |
| eng群 | eng 群（`eng-lead` 含む） | `報告・コードコメント・ドキュメントはすべて日本語で記述する（識別子は英語）。` |
| ai/docs群 | ai/docs 群（L10n除外の明記が必要な群） | `報告・成果物はすべて日本語で記述する（L10n成果物を除く）。` |

上記の1文に加え、担当業務に応じた追加の出力ルールを箇条書きで足すのは問題ありません
（例: `agents/ai-integration.md` のコスト・レイテンシ報告条件の明記、`agents/docs-l10n.md` の
翻訳成果物は対象ロケール言語で書く旨等）。新規グループを新設する場合の出力文言は docs-lead に確認して
ください。

### 2.5 モデル割り当て方針
README「モデル割り当て」表を参照し、以下の判定条件で決定します。
`model` の値は世代固定ではなく**階層エイリアス**であり、世代交代には自動追従します。
したがって判断軸は「今の世代のモデル性能」ではなく「役割の恒常的な特性」です。

#### 判定フロー（上から順に評価し、最初に該当した階層を採用）

| # | 判定条件 | 該当時 |
|---|---|---|
| 1 | **不可逆・広範囲な意思決定**を行うか（アーキテクチャ、リスク受容、リリース可否、グループ横断のルーティング）。判断誤りの修正コストが、判断自体のコストを大きく上回るか | `opus` |
| 2 | 上記に該当せず、**コード・スキーマ・設定の読解**を伴うか／**成果物の文章品質そのものが価値**か（実装・レビュー・監査・分析・執筆・翻訳） | `sonnet` |
| 3 | 上記のいずれにも該当せず、**判断を含まない集計・整形・転記**に限定され、かつ入力が単一ソースに閉じるか | `haiku` |

#### `opus` の判定は役職ではなく判断の性質で決める

`opus` は「リーダーだから」ではなく、**不可逆・広範な意思決定を担う役割か**（判定フロー #1）で決まります。
リーダー職と `opus` は結果として大きく重なりますが、規約上の根拠は役職名ではありません。したがって:

- **リーダーでなくても、#1 に該当すれば `opus`** を選びます。`eng-architect` は、サービス境界・
  サービス間通信方式の選定を誤った場合の作り直しコストが判断自体のコストを大きく上回るため #1 に該当し、
  `opus` です。これは規約の例外ではなく、規約どおりの帰結です。
- **リーダー級（`*-lead` / `mgmt-coordinator`）であっても**、担当が助言・取りまとめに留まり、決定権を
  他へ委ねる役割であれば #1 には該当しません。新規にリード級を追加する際も、#1 の該当性を個別に確認します。
- 判定方法: 「その役割が判断を誤ったとき、下流のどれだけの成果物をやり直すことになるか」で確かめます。
  やり直しの範囲が**単一の成果物に閉じる**なら #1 には該当せず `sonnet` です。複数エージェント・複数
  プロジェクトの成果物に波及し、かつ後戻りにリリースをまたぐ手戻りが伴うなら #1 に該当します。

#### 階層別の目安

| モデル | 対象の目安 |
|---|---|
| `opus` | **不可逆・広範な意思決定を担う役割**（役職に限定されない。判定は上記の補足を参照）。**低トークン量 × 高レバレッジ**（自ら実作業をせず、判断が下流の全作業を左右する）ポジション。現時点の該当エージェントは README「モデル割り当て」表（正本）を参照してください |
| `sonnet` | 上記・下記以外のすべて（新規エージェントは基本的にこちら）。実装・レビュー・監査・分析・執筆の実務中核 |
| `haiku` | 判断を含まない軽量タスクに**限定される**場合のみ。現時点の該当エージェントは README「モデル割り当て」表を参照してください |

#### `haiku` を選ぶ前の必須チェック（すべて「はい」の場合のみ採用可）

`haiku` 階層には、単価以外に他階層とのコンテキスト長・思考機能・知識鮮度の差があります。
1つでも「いいえ」がある場合は `sonnet` を選択してください。

- [ ] コンテキストが小さくても足りるか（**リポジトリ横断の探索・読解を行う役割は原則抵触する**）
- [ ] 深い思考を要する分類・切り分け・妥当性判定を伴わないか
- [ ] 情報鮮度が問われないか（法令・審査基準・技術動向を扱う役割は抵触する）
- [ ] 対外公開文書・ユーザー向け最終成果物を生成しないか
- [ ] 他エージェントの成果物を**評価・承認する立場ではない**か（評価ノードの見逃しは最も高価な失敗形態のため）

#### 降格を検討する際の注意

モデル降格の節約額は「呼び出し頻度 × トークン量 × 単価差」で決まります。本組織では
機械的な業務のエージェントほど低頻度（節約額がほぼゼロ）で、高頻度なエージェントほど
品質劣化の影響が大きいという構造があります。加えて評価ループは最大3回の差し戻しを許容
するため、**差し戻しが平均1回増えるだけで降格の節約は相殺されます**。降格は「品質が一切
落ちない」ことが前提の判断であり、原則として推奨されません。

迷う場合は `sonnet` を選択し、後日 `*-lead` または docs-lead の判断で見直してください。

背景・理由は [DESIGN.md](DESIGN.md) 2.2 を参照してください。

#### effort の割り当て方針

`effort` は明示指定せず既定（セッション設定を継承）とするのを原則とし、以下に該当する場合のみ
`low`/`medium` を明示します。

- 対象: 業務の主軸が定型的な集計・転記・整形であり、裁量の余地が小さいエージェント
- 対象外（既定継承のまま据え置く）: リーダー職（`*-lead`・`mgmt-coordinator`）、判断・検証系
  （設計判断・監査・リスク評価等）、実装系（実装は機械作業でなく問題解決を伴うため）
- `qa-review` は品質ゲートの評価ノードとして常用されるため、effort を下げません
- 個別エージェントの分類に迷う場合は、誤りの修正コストの大きさを基準に、保守的（既定継承）側へ
  倒してください

### 2.6 README 更新（必須）
`README.md` の「組織図（エージェント一覧）」表の該当グループ行に、新規エージェント名を追記します。
グループが `opus`/`haiku` 対象を含む場合は「モデル割り当て」表の該当セルも見直します。

`commands/show-org.md` の再生成も必要です。この生成物は `.claude/scripts/generate-show-org.sh` が
`agents/*.md` の frontmatter から機械生成するため、手編集はせず以下を実行してください
（エージェントの削除・`description` 変更のみを行った場合も同様です）。

```bash
bash .claude/scripts/generate-show-org.sh
```

### 2.7 チェックリスト
- [ ] 1.1 の判断基準5項目すべてが YES であることを、判定方法に沿って確認し根拠を記録した
- [ ] 1.2 の代替手段（スクリプト/skill/コマンド/既存への追記）を検討し、却下理由を書いた
- [ ] `agents/<group>-<role>.md` を作成し、ファイル名と `name` が一致している
- [ ] frontmatter に `name`/`model`/`description` の3項目がある
- [ ] `description` の「いつ呼ぶか」が担当領域の言い換えではなく、具体的な状況の列挙になっている
- [ ] 書き込みを制限する場合、`tools:` 許可リスト方式（原則）または `disallowedTools:` 拒否リスト方式
      （Bash等の診断ツールを残す場合のみ）のどちらかを選び、後者の場合は本文にも自己修正しない旨を明記した
- [ ] 本文が既存エージェントと同じ見出し構成（役割/担当業務/作業方針/連携/出力。リード級は
      `## 作業設計（ワークフロー5原則）` を既存リードから逐語コピーして追加）
- [ ] `## 連携` の箇条書きが散文型（`- 〜は…に確認する。`）になっている
- [ ] 非リーダー新規エージェントは `disallowedTools: Agent` を追加した（既存 `disallowedTools` がある
      場合は配列にマージした。`mgmt-coordinator`・`*-lead` は対象外。ただし `tools:` 許可リストを選び、
      その一覧に `Agent`/`Task` を含まない場合は追加不要（`tools:` 未指定時は対象外としない）。
      `.claude/scripts/validate.sh` もこの場合を検査対象外とする）
- [ ] 非リーダー新規エージェントは `## 連携` 先頭に TK-2/E-1 委任制限文（逐語）を追加した
- [ ] `## 出力` の文言が同グループの既存ファイルと一致している（新規に文言を考案していない）
- [ ] README「組織図」表に追記した
- [ ] README「モデル割り当て」表を必要に応じて見直した
- [ ] `bash .claude/scripts/generate-show-org.sh` を実行した
- [ ] 5章の検証手順を実行した

## 3. 新規コマンド追加手順

### 3.1 frontmatter テンプレート
`commands/<command-name>.md` として作成します。frontmatterは `description` のみ必須です。
`argument-hint` は任意キーです。

```yaml
---
description: <コマンドの用途を1文で>
argument-hint: <引数のヒント（任意）>
---
```

`argument-hint` は次の規約に従います。

- 本文で `$ARGUMENTS` を使う場合は `argument-hint` を書きます。
- 値は README「使い方」表の引数表記と逐語一致させます（README が引数表記の正本です）。
- `[` で始まる値は YAML のフローシーケンス（配列）と誤解釈されるおそれがあるため、引用符で
  囲みます（例: `argument-hint: "[対象]"`）。`<` で始まる値は引用符なしで問題ありません
  （例: `argument-hint: <内容>`）。

### 3.2 `$ARGUMENTS` の使い方
本文中で `$ARGUMENTS` と書くと、`/hw:<command-name> <ユーザー入力>` のユーザー入力部分がそこに展開
されます。`commands/request.md` の例:

```markdown
案件の内容: $ARGUMENTS

（内容が空の場合は、まず案件の目的・対象プロジェクト・完了条件をユーザーに確認してください。）
```

入力が空でも動作するよう、空の場合のフォールバック（確認質問を投げる等）を本文に明記してください。

### 3.3 本文の構成の目安
- 冒頭でコマンドが何を行うか、どのエージェント/スキルとして振る舞うかを宣言する
- `## 手順` として番号付きで処理フローを書く（5原則: 直列分解・ルーティング・並列化・司令塔と専門家・評価ループ
  を踏まえて設計する）
- 分担先の判断基準（各エージェントの `description` を参照して選ぶ、等）を明記する

### 3.4 README 更新（必須）
`README.md` の「使い方」表に `/hw:<command-name> <引数>` の行を追記します。この必須はここまでの
3.1〜3.3 が対象とする**配布コマンド**（`commands/`）に限ります。保守用ローカルコマンド
（`.claude/commands/`）は対象外です（3.6参照）。

### 3.5 チェックリスト
- [ ] `commands/<command-name>.md` を作成した
- [ ] frontmatterに `description` がある
- [ ] `$ARGUMENTS` を使う場合、空入力時のフォールバックを本文に書いた
- [ ] `$ARGUMENTS` を使う場合、`argument-hint` を frontmatter に書き、README「使い方」表の
      引数表記と逐語一致していることを確認した
- [ ] README「使い方」表に追記した
- [ ] 保守用ローカルコマンド（`.claude/commands/`。3.6）に該当する場合は、上記のREADME追記に
      代えて 3.6の配置・命名規約に従い、README を更新していないことを確認した
- [ ] 5章の検証手順を実行した

### 3.6 保守用ローカルコマンド

3.1〜3.5 は利用側リポジトリに配布される `commands/` 配下のコマンドを対象とします。本節は
それとは別に、本リポジトリ自身の保守作業専用として `.claude/commands/<command-name>.md` に置く
ローカルコマンドを対象とします（配布物ではありません）。既存例は `release.md`・
`release-notes.md` です。

- 配置は `.claude/commands/<command-name>.md`、命名は3.1と同じくkebab-caseです。呼び出しは
  `/<command-name>` であり、`hw:` 名前空間には属しません（`/hw:<command-name>` ではありません）。
- frontmatterは3.1と同じく `description` のみ必須です。
- `$ARGUMENTS` を使う場合は3.2の使い方（空入力時のフォールバックの明記を含む）に従います。
- README は更新しません（3.4の必須は配布コマンド限定であり、本節の対象は配布物ではないためです）。
- `argument-hint` は README に対応行が無いため、本文の引数説明に沿った表記を用います。
- 配布コマンド（`commands/`）か保守用ローカルコマンド（`.claude/commands/`）かの判定は、1.10の
  基準（利用側リポジトリで意味を持つか）に従います。依頼の分類（プラグインのメンテナンス／
  リポジトリの環境整備）は1.11によります。本節はこれらの基準を再定義しません。
- 5章の検証対象に含まれます（frontmatterの `description` 必須項目、文書内パス参照の実在検証、
  秘密情報混入チェック）。
- 配置・命名・ファイルモード等、スクリプト・CI 定義側の対応節は8.2です（本節はコマンドが対象、
  8.2はスクリプト・CI 定義が対象という役割分担です）。
- 既存2件は本文冒頭付近で「このコマンドはリポジトリローカルの保守用コマンドです（
  `.claude/commands/` 配下。プラグイン配布物の `commands/` には含まれず、`hw:` 名前空間にも
  属しません）。」と自己宣言しています。新規追加時も同様の宣言を本文冒頭付近に含めてください。

背景・経緯は [DESIGN.md](DESIGN.md) 2.47 を参照してください。

## 4. 新規スキル追加手順

### 4.1 ディレクトリ構成
`skills/<skill-name>/SKILL.md` として作成します（スキル名のディレクトリを切り、その直下に `SKILL.md`
1ファイルを置く構成。既存の `skills/repo-map/SKILL.md` を参照）。

### 4.2 frontmatter テンプレート
`name`/`description` の2項目が必須です。

```yaml
---
name: <skill-name>
description: <この手順が何をするものか>。<どんなときに使うか>ときに使う。
---
```

- `name` はディレクトリ名と一致させます。呼び出し時は `hw:<skill-name>` になります。
- `description` は「用途」＋「使うタイミング」の2要素を必ず含めてください（`skills/repo-map/SKILL.md`
  を参照）。

#### `context`/`background`（オプション項目）
`context`/`background` はいずれもオプションです（Claude Code Skills 標準機能）。未指定であれば
既定どおり呼び出し元が本文をそのまま実行します。

```yaml
---
name: <skill-name>
description: ...
context: fork
background: false
---
```

- `context: fork`: スキル本文を通常のサブエージェント（既定 `general-purpose`。`agent:` で明示指定も
  可）のプロンプトとして注入し、呼び出し元とは別の実行コンテキストで実行します。フォーク先は
  呼び出し元の会話履歴を引き継がず、結果は要約されて呼び出し元へ返ります。
- `background: false`: `context: fork` と併用した場合のみ有効です。フォーク実行の完了をそのターン内で
  待ち合わせます（省略時は既定でバックグラウンド実行）。

**採用可否の判定基準（次の3点をすべて満たすスキルに限り検討可）**:
1. 利用者との往復（確認・承認等）を含まないこと
2. 呼び出し元の会話文脈（それまでの判定結果・入力）に依存しないこと
3. 明示的なタスク（何をして何を返すか）を持つこと

対話・判断を含むスキルはフォークしない方針とします。フォーク先は呼び出し元とは別のサブエージェント
として起動されるため、利用者への確認・承認に用いるツール（`AskUserQuestion` 等）が実行文脈から除去
され、往復が成立しません（`background: false` にしても回避できません）。加えて会話履歴も引き継がない
ことが理由です。この機構は、会話履歴ごと引き継ぐ「会話のフォーク」とは別機構です。

対象スキルを拡大する際は、`agent:` 省略時のフォーク先ツール不在を機構レベル（エラーメッセージ）で
複数回確認することを追加の検証条件とします（既存2スキルの実機検証は各構成1〜2回の試行に基づくため。
詳細は DESIGN.md 2.35 参照）。

**採用する場合に本文へ明記する事項**:
- フォーク先は呼び出し元の会話履歴を持たない前提（既存状態を自力で読み直して再判定する等、
  自己完結的な手順であること）
- 出力を要約させたくない情報（差分の列挙等）がある場合は、要約せず列挙して返す旨
- フォーク失敗時は呼び出し元が直接実行にフォールバックする旨（縮退の規範化）
- 呼び出し元の共通ガードレール（1.5の逐語コピー等）に依存していたスキルは、フォーク先でその層が
  失われるため、該当文言を本文へ追加する（1.4原則4「検知手段のない規範は追加しない」に照らし、
  必要なら `.claude/scripts/validate.sh` 側の検知手段も合わせて整備する）

**運用規範（静的な部分検知あり。`.claude/scripts/validate.sh`。動的呼び出しは原理的に静的検知不可のため
運用規範を併用）**:
- `context: fork` スキルはメイン会話・リーダー層から呼びます。非リーダー、特に読み取り専用系
  （`tools:` 許可リスト型等）の呼び出し元から呼ぶと、フォーク先は呼び出し元のツールプールに束縛され
  書き込み不能となり、成果物を保存できません（1.7・DESIGN.md 2.35 参照）。拒否リスト型
  （`disallowedTools`）の非リーダーから呼んだ場合は書き込み不能にはならず、`Agent` ツールの拒否を
  すり抜けてフォーク自体が成立してしまう（構造的迂回。1.7 参照）ため、この運用規範は読み取り専用系に
  限らず全非リーダーに適用します。
- `agent:` を指定する場合はプラグインスコープ名 `hw:<name>` を逐語で使ってください。裸名（`<name>`
  のみ）は `claude plugin validate .` でも実行時エラーでも検知されず、無警告で `general-purpose` へ
  フォールバックします。

背景・判断の経緯は [DESIGN.md](DESIGN.md) 2.35 を参照してください。

### 4.3 本文の構成の目安
```markdown
# <スキルのタイトル>

（このスキルが何のためにあるかの概要）

## 手順
1. ...
2. ...

## 注意
（推測で書かない、不明点は明記する、等の品質担保ルール）
```

### 4.4 README 更新（必須）
`README.md` の「スキル（作業手順書）」表に `hw:<skill-name>` の行を追記します。この必須はここまでの
4.1〜4.3 が対象とする**配布スキル**（`skills/`）に限ります。保守用ローカルスキル
（`.claude/skills/`）は対象外です（4.6参照）。

### 4.5 チェックリスト
- [ ] `skills/<skill-name>/SKILL.md` を作成した（ディレクトリ名 = `name`）
- [ ] frontmatterに `name`/`description` がある
- [ ] `description` に用途と使用タイミングの両方が書かれている
- [ ] `context: fork` を使う場合、判定基準3点（利用者往復なし・会話文脈非依存・明示的タスクあり）を
      満たすか確認した
- [ ] `context: fork` を使う場合、本文に会話履歴非依存の前提・出力の要約回避（該当時）・フォーク
      失敗時の縮退を明記した
- [ ] `context: fork` で `agent:` を指定する場合、プラグインスコープ名 `hw:<name>` を逐語で記述した
      （裸名は無警告で `general-purpose` にフォールバックするため）
- [ ] README「スキル」表に追記した
- [ ] 保守用ローカルスキル（`.claude/skills/`。4.6）に該当する場合は、上記のREADME追記に代えて
      4.6の配置・命名規約に従い、README を更新していないことを確認した
- [ ] 5章の検証手順を実行した

### 4.6 保守用ローカルスキル

4.1〜4.5 は利用側リポジトリに配布される `skills/` 配下のスキルを対象とします。本節はそれとは別に、
本リポジトリ自身の保守作業専用として `.claude/skills/<skill-name>/SKILL.md` に置くローカルスキルを
対象とします（配布物ではありません）。既存例は `idea-issue-triage`
（`.claude/skills/idea-issue-triage/SKILL.md`）です。

- 配置は `.claude/skills/<skill-name>/SKILL.md`（4.1と同じくディレクトリ名 = スキル名、直下に
  `SKILL.md` 1ファイルを置く構成）です。
- frontmatterは4.2と同じく `name`/`description` の2項目が必須です。呼び出しは `<skill-name>`
  であり、`hw:` 名前空間には属しません（`hw:<skill-name>` ではありません）。
- README は更新しません（4.4の必須は配布スキル限定であり、本節の対象は配布物ではないためです）。
- 配布スキル（`skills/`）か保守用ローカルスキル（`.claude/skills/`）かの判定は、1.10の基準
  （利用側リポジトリで意味を持つか）に従います。依頼の分類は1.11によります。本節はこれらの
  基準を再定義しません。
- 5章の検証対象に含まれます（frontmatterの `name`/`description` 必須項目とディレクトリ名との一致、
  文書内パス参照の実在検証、秘密情報混入チェック）。
- 配置・命名・ファイルモード等、スクリプト・CI 定義側の対応節は8.2です（本節はスキルが対象、
  8.2はスクリプト・CI 定義が対象という役割分担です）。

**用語の注意（`.claude/skills/` の別義）**: `README.md` の作業ディレクトリ構成にある
`.claude/skills/` は**利用側リポジトリ**において、`.hw/procedures/` の手順資産が実績を重ねた末に
昇格する正式スキルの配置先を指す別物です。本節が扱う `.claude/skills/` は**本リポジトリ自身**の
保守作業専用ローカルスキルの配置先であり、パスの文字列が同じでも指す対象・意味が異なります。

背景・経緯は [DESIGN.md](DESIGN.md) 2.47 を参照してください。

## 5. 変更後の確認手順（必須）

**本章が本リポジトリにおける検証手順の正本です。** 他文書（[DEVELOPMENT.md](DEVELOPMENT.md) 等）に
手順の再掲がある場合も、内容は本章を基準に同期します。

この「正本と再掲」の関係は検証手順に限らず、本リポジトリの規約全般に適用されます。**規約の正本は
本ドキュメント（CONTRIBUTING.md）です**（検証手順に限っては本章が正本）。`CLAUDE.md`・
`DEVELOPMENT.md`・`.hw/conventions.md`・`.hw/repo-map.md`・`.github/workflows/validate.yml` 等への
記載は、参照の便宜のための再掲・索引であり正本ではありません。再掲は必要最小限に留め、正本（本
ドキュメント）を変更した際は再掲箇所も同時に更新してください（同期の責任は正本を変更した者に
あります）。再掲の方式（生成スクリプトへ埋め込むか、再掲を新設せず実行時に正本を読むか）の選択
基準は [DESIGN.md](DESIGN.md) 2.9 を参照してください。

ただし本ドキュメントが明示的に他所を正本と指定している規約は、その指定先が正本です（例: モデル
割り当ての現時点の該当エージェントは README「モデル割り当て」表（2.5参照）、品質ゲートのレベル定義は
`agents/mgmt-coordinator.md`「品質ゲートの適用判定」節、共通ガードレール文の正典は
`.claude/scripts/lib/guidelines.sh` の `GUIDELINE_COMMON_LINES` 定数（1.5参照。検証は
`.claude/scripts/validate.sh`、伝播は `.claude/scripts/sync-guidelines.sh` が担う。詳細は
DESIGN 2.30 参照））。

いずれの変更後も、以下の項目1・2は**必ず**実行し、対象スクリプトに回帰テストがある場合は該当するもの
も実行して、エラー・警告があれば解消してから完了報告してください。

1. プラグイン定義のバリデーション（必須）:
   ```bash
   claude plugin validate .
   ```

2. 静的検証スクリプトの実行（必須）:
   ```bash
   bash .claude/scripts/validate.sh
   ```
   `.claude/scripts/validate.sh`（infra-devops整備）は、`agents/*.md`・`commands/*.md`・
   `skills/*/SKILL.md` の frontmatter必須項目（命名規則・重複の検査は `agents/*.md` のみが対象）、
   `.claude/commands/*.md`（保守用ローカルコマンド。3.6参照）の frontmatter必須項目（`description`
   のみ。README突合の対象外）、README 記載のエージェント一覧・コマンド一覧・スキル一覧と実ファイル
   構成の整合性、`commands/show-org.md` の生成差分、秘密情報
   混入チェック（シークレットスキャン）、エージェント作業ガードレール（共通6短文・
   `disallowedTools`・TK-2/E-1委任制限文の逐語一致）、数量表記整合、Markdown文書（README.md・
   CLAUDE.md・CONTRIBUTING.md・DESIGN.md・DEVELOPMENT.md・`agents/*.md`・`commands/*.md`・
   `skills/*/SKILL.md`・`.claude/commands/*.md`）中のバッククォート付きリポジトリ内パス参照の
   実在検証（`.hw/` 配下・
   プレースホルダ・ワイルドカード〔glob展開で0件一致はERROR〕・利用者側資材の例示パス明示除外
   リストの4分類を除外し、各分類のスキップ件数を必ず出力。対象文書全体からの抽出トークンが0件の
   場合も検証の空振りとしてERRORとする）を検証する bash スクリプトです（devcontainer移行に伴い旧
   `validate.ps1` から移行。追加ランタイム不要の方針は維持）。

   文書中で利用者側資材の例示パス（本リポジトリ内には実在しないことを承知のうえでの例示。例:
   `.github/copilot-instructions.md`）を新たに記載する場合は、`.claude/scripts/validate.sh`
   セクション12の `DOC_PATH_REF_EXCLUDE_LIST` へ当該パスを追加してください（追加しないと存在
   しないパスとしてERRORになります）。

   README の「使い方」「スキル」表の見出しを変更する場合は、`commands/help.md` の実行時抽出（見出し
   アンカー）も併せて確認してください。

   項目1・2を一括実行し未コミット差分のスナップショットも取得するローカル補助として
   `bash .claude/scripts/verify-assets.sh` を利用できます（必須手順1・2の代替ではありません）。

   `.claude/scripts/validate.sh` 自体の回帰テスト（`.claude/scripts/tests/run-tests.sh`、
   qa-test整備）は `bash .claude/scripts/tests/run-tests.sh` で実行できます。
   `.claude/scripts/validate.sh` を変更した場合は、完了報告前に必ずこちらも実行し、全ケースPASS
   であることを確認してください。

   同様に、`.claude/scripts/git-changelog.sh` を変更した場合は
   `bash .claude/scripts/tests/run-git-changelog-tests.sh`（qa-test整備）を、
   `.claude/scripts/aggregate-agent-token-usage.sh` を変更した場合は
   `bash .claude/scripts/tests/run-aggregate-agent-token-usage-tests.sh`（qa-test整備）を、
   `.claude/scripts/sync-guidelines.sh` を変更した場合は
   `bash .claude/scripts/tests/run-sync-guidelines-tests.sh`（infra-devops整備）を、
   `.claude/scripts/close-linked-issues.sh` を変更した場合は
   `bash .claude/scripts/tests/run-close-linked-issues-tests.sh`（infra-devops整備）を、
   `.claude/scripts/verify-assets.sh` を変更した場合は
   `bash .claude/scripts/tests/run-verify-assets-tests.sh`（infra-devops整備）を実行し、
   全ケースPASSであることを確認してください。回帰テストが共有する共通ライブラリ
   （`.claude/scripts/tests/lib/` 配下）を変更した場合は、影響が lib/ を共有する全ハーネスに
   及ぶため、以下の全ハーネスを実行し、全ケースPASSであることを確認してください（新たなハーネスを
   追加した場合は、本リストと8.4の該当箇所を同時に更新してください）。

   - `bash .claude/scripts/tests/run-tests.sh`
   - `bash .claude/scripts/tests/run-git-changelog-tests.sh`
   - `bash .claude/scripts/tests/run-aggregate-agent-token-usage-tests.sh`
   - `bash .claude/scripts/tests/run-sync-guidelines-tests.sh`
   - `bash .claude/scripts/tests/run-close-linked-issues-tests.sh`
   - `bash .claude/scripts/tests/run-verify-assets-tests.sh`

   回帰テストを新設すべきかの判断基準と配置は8.4を参照してください。

3. ローカルセッションで変更を反映して動作確認する場合:
   ```bash
   # 本リポジトリのセッションでは /reload-plugins のみで反映されます
   /reload-plugins
   ```
   別リポジトリのセッションへ一時的に読み込む方法等は [DEVELOPMENT.md](DEVELOPMENT.md) を
   参照してください。

## 6. 命名・文体規約

- 既存の `agents/*.md` `commands/*.md` `skills/*/SKILL.md` `README.md` は**すべて日本語**で統一されて
  います。新規追加・変更も日本語で記述してください（コード識別子・コマンド名・技術用語の英語表記は可）。
- ファイル名・エージェント名・コマンド名・スキル名自体は英語の kebab-case（例: `eng-backend`,
  `repo-map`）を用います。
- 見出し構成・箇条書きスタイルは既存ファイルに合わせ、新形式（新しい見出し体系や表現ルール）を
  導入する場合は事前に docs-lead の承認を得てください。
- 列挙の省略は本文では「等」を用い、「など」は使いません（frontmatter の `description` および
  `commands/show-org.md` のその複製行は、エージェント選択に用いる文言のため現状表記を維持します）。
- 文体は文書内の役割で使い分けます。読者への説明・依頼はですます調、規範・手順そのものを述べる
  本文はである調です（`agents/*.md` の各セクション本文、`commands/*.md` の番号付き手順、
  CONTRIBUTING 1.4 の設計原則リストが後者にあたります）。

背景・理由は [DESIGN.md](DESIGN.md) 2.7 を参照してください。

## 7. 貢献のライセンス条件（CLA）とクレジット

本プラグインは [CC BY-ND 4.0](LICENSE) で公開されており、著作権者（bizhermit）はこれに加えて、
改変版の公開許可等の個別許諾を第三者に発行できる体制（デュアルライセンス）を採っています。
この体制を維持するため、外部からの貢献は以下の条件で受け入れます。

### 7.1 ライセンス条件（許諾型CLA）

**Pull Request の送信をもって、貢献者は以下のすべてに同意したものとみなします。**

1. **著作権の帰属**: 貢献物の著作権は、貢献者自身に帰属し続けます（著作権の譲渡は行いません）。
2. **メンテナーへの許諾**: 貢献者は著作権者（bizhermit）に対し、貢献物を本プラグインの一部として
   利用・複製・改変・配布・再許諾（第三者への個別許諾、商用許諾、および将来のライセンス形式の
   変更を含む）できる、世界的・無償・非独占・永続・取消不能のライセンスを許諾します。
3. **貢献者自身の再利用**: 貢献者は、自身が作成した貢献部分を、自身の他のプロジェクト等で
   自由に利用できます（本条件はそれを制限しません）。
4. **権利の保証**: 貢献者は、貢献物が自身の創作であること（または許諾を得る権限を有すること）、
   および第三者の権利を侵害しないことを表明します。第三者の著作物（他リポジトリのプロンプト等）を
   含める場合は、PR にその旨と出典・ライセンスを明記してください。

### 7.2 クレジット

- 取り込まれた貢献は、リリース時に `CHANGELOG.md` の該当エントリへ**貢献者名（GitHub アカウント名）
  付きで記録**します（[DESIGN.md](DESIGN.md) 2.10 の変更履歴の運用方針に従い、記載は mgmt-release が
  取りまとめます。`CHANGELOG.md` の作成時期は1.3を参照してください）。
- 貢献の規模・継続性に応じて、README への謝辞の記載を検討します。

## 8. スクリプト・CI 定義の追加・変更手順

### 8.1 適用範囲と1.2との役割分担

本章の対象は `.claude/scripts/` 直下・`.claude/scripts/lib/`・`.claude/scripts/tests/`・
`.claude/scripts/tests/lib/`・`.claude/scripts/tests/fixtures/`・`.github/workflows/`・
`.github/dependabot.yml` です。1.2 は定型作業をどの実現手段（スクリプト・skill・コマンド・エージェント）
で解決するかを選ぶための判断基準であり、本章はスクリプト・CI 定義を選んだ後の追加・変更手順です。
他の実現手段を選んだ場合の手順は、skill なら4章、コマンドなら3章、エージェントなら1.1・2章を参照して
ください。

### 8.2 配置・命名・ファイルモード

| 配置 | 用途 |
|---|---|
| `.claude/scripts/<name>.sh` | 単体で完結する検証・生成・git 補助スクリプト本体 |
| `.claude/scripts/lib/` | 複数のスクリプト本体が共有するライブラリ（例: 共通ガードレール文の定数を持つ `guidelines.sh`。1.5参照） |
| `.claude/scripts/tests/run-<対象>-tests.sh` | 対応するスクリプトの回帰テストハーネス |
| `.claude/scripts/tests/lib/` | 複数のハーネスが共有するアサーション等の共通ライブラリ |
| `.claude/scripts/tests/fixtures/` | 回帰テストが読み込む入力データ・期待値等の固定データ |
| `.github/workflows/<name>.yml` | CI ワークフロー定義 |
| `.github/dependabot.yml` | GitHub Actions の依存関係（バージョン）更新設定 |
| `scripts/<name>.sh` | 将来、配布用のスクリプトを置く場合の予約パス（1.10参照）。現在は使用実績なし |

本表はスクリプト・CI 定義が対象です。保守用ローカルコマンド（`.claude/commands/`）・保守用
ローカルスキル（`.claude/skills/`）の配置・命名は、それぞれ3.6・4.6を参照してください。

命名は kebab-case に `.sh` を付けた「動詞-目的語」形式を基本とします（例:
`generate-show-org.sh`）。git を操作するスクリプトは `git-` 接頭辞を付けます（例:
`git-changelog.sh`・`git-cleanup-branch.sh`）。テストハーネスは `run-<対象>-tests.sh` とします
（`.claude/scripts/tests/run-tests.sh` は `validate.sh` の回帰テストという最初期からの既存名を
維持する例外です）。

ファイルモードは `.sh` が `100755`（実行可能）、`.yml` が `100644` です。判定は git index 上の
モード（`git ls-files -s` 等で確認）とします。呼び出しは `bash <path>` を正とし、実行可能ビットの
有無に呼び出し方が依存しないようにします。

### 8.3 スクリプトの実装方針

スクリプト化する場合は `.claude/scripts/validate.sh` 方式を標準とし、以下をすべて満たします。

- 追加ランタイムを要求せず bash のみで動くこと
- ローカルでも CI でも同じ1コマンドで実行できること
- エラー時に「対象ファイル」と「なぜ不正か」を出力すること
- 5章の確認手順から呼び出せること（回帰テストの要否判断は8.4を参照）
- 冒頭に shebang `#!/usr/bin/env bash` を置き、続けて目的・採用理由・実行例をコメントで記すこと
- `set -euo pipefail` を設定すること（テストハーネスは、1ケースの失敗で処理全体を止めず残りの
  ケースも実行して集計する必要があるため `-e` を含めません）
- 環境依存の挙動差を避けるため `LC_ALL=C` を設定すること
- リポジトリルートは第1引数で受け取り、省略時は自動解決すること
- 秘密情報を出力しないこと（値そのものに限らず、値を含みうる変数のダンプ等も避けます）
- `.claude/scripts/validate.sh` 冒頭の検証内容一覧コメントは、検証項目の増減と同一コミットで更新すること

### 8.4 回帰テストの要否判断と `.claude/scripts/tests/` の構成

新設したスクリプトに回帰テストを設けるかどうかは、以下のいずれかに該当するかで判断します。

- 他資材の合否判定を左右する（例: `validate.sh` はエージェント・コマンド・スキルの適合性判定
  そのもの）
- 分岐・正規表現・パース処理を含む（単純な直列処理のみのスクリプトより誤りが混入しやすい）
- 静かに失敗するガードレールになりうる（検証すべき対象を検知できないまま、成功したかのように
  振る舞うリスクがある）

いずれにも該当せず回帰テストを新設しない場合は、その理由をスクリプト冒頭のコメントに明記してください
（`.claude/scripts/git-cleanup-branch.sh` が該当例です）。

回帰テストを新設する場合は以下に従います。

- ハーネス名は `run-<対象>-tests.sh` とする（8.2参照）
- アサーション等の共通処理は `.claude/scripts/tests/lib/` を `source` して使い、ハーネスごとに再実装しない
- `.claude/scripts/tests/lib/` 配下の変更は影響が lib/ を共有する全ハーネスに及ぶため、5章に列挙された
  全ハーネスを実行して確認する（現行のハーネス一覧・合格基準は5章参照。ハーネス追加時は5章の
  一覧も同時に更新する）
- テスト用の入力データ・期待値等の固定データは `.claude/scripts/tests/fixtures/` に置く
- 合格基準は全ケース PASS とする（正本は5章）

### 8.5 CI 定義の変更手順

CI（`.github/workflows/`）は検証手順の実行者であり、正本ではありません。手順を変更する場合は5章
（検証手順の正本）を先に更新し、CI 定義はそれに追従させます。

CI 定義は以下の方針に従います。

- `permissions` は必要最小限に絞ること
- シークレットを必要としないこと（将来的に必要になった場合は sec-lead に相談すること）
- 外部アクションはコミット SHA で固定し、参照したタグ名を行末コメントで併記すること（SHA の更新は Dependabot の更新 PR で行う）
- Claude Code CLI のインストールは固定バージョンのバイナリ直接取得＋リポジトリ管理の SHA256 照合方式を用いること（devcontainer は `install.sh` 方式のまま分岐する。理由・更新運用は `validate.yml` の該当コメント参照。判断記録 `.hw/decisions.md` 2026-08-01 B9）
- 同一ブランチ・同一PRでの重複実行を避けるため `concurrency` を設定すること
- コメントは日本語で記述すること
- ローカルの手順（5章）と差異がある場合は、その理由をワークフローのコメントに明記すること

品質ゲートは8.7を参照してください。

### 8.6 索引・構成表の更新

- README「組織図」「使い方」「スキル」表への追記は不要です（対象が3種類ではないため）
- 利用者向けの索引として案内したい場合は `.hw/conventions.md` の「利用すべきスキル・コマンド・
  スクリプト」表に追加してください（索引であり正本ではありません）
- DEVELOPMENT.md のディレクトリ構成表は、新しいディレクトリ階層を作った場合のみ更新します
  （既存階層内でのファイル追加は対象外です）
- `.hw/repo-map.md` の共通コマンドは、利用者が直接叩く入口コマンドを追加した場合のみ更新します

### 8.7 品質ゲートと完了条件

スクリプトの追加・変更はレベル2以上（qa-review 必須）、CI 定義の追加・変更はレベル3
（qa-review + sec-audit）として扱います。レベル定義の正本は `agents/mgmt-coordinator.md`
「品質ゲートの適用判定」節です。完了条件は5章の確認手順を満たすことです。

### 8.8 チェックリスト
- [ ] 対象が本章の適用範囲（`.claude/scripts/` 直下・`lib/`・`tests/`・`tests/lib/`・
      `tests/fixtures/`・`.github/workflows/`・`.github/dependabot.yml`）に該当することを確認した
- [ ] 他の実現手段（skill/コマンド/エージェント）で代替できないことを1.2に沿って検討した
- [ ] 配置先が8.2の配置表に沿っている
- [ ] ファイル名が命名規則（kebab-case+.sh、動詞-目的語、git操作系は`git-`接頭辞、テストハーネスは
      `run-<対象>-tests.sh`）に沿っている
- [ ] ファイルモードが `.sh`=100755／`.yml`=100644 になっている
- [ ] 冒頭に shebang と、目的・採用理由・実行例のコメントがある
- [ ] `set -euo pipefail` を設定した（テストハーネスは `-e` を除く）
- [ ] 追加ランタイムを導入していない
- [ ] `LC_ALL=C` を設定した
- [ ] リポジトリルートを第1引数で受け取り、省略時に自動解決する
- [ ] エラー時に対象ファイルと理由を出力し、秘密情報を出力しない
- [ ] `validate.sh` を変更した場合、冒頭の検証内容一覧コメントを同一コミットで更新した
- [ ] 回帰テストの要否を8.4の3基準で判断し、新設しない場合は理由をコメントに明記した（新設する場合は
      `lib/` の source・`fixtures/` への配置・全ケースPASSを満たした）
- [ ] CI 定義を変更した場合、5章を先に更新してから追従させ、`permissions` 最小・シークレット不要・
      外部アクションのタグ固定・`concurrency`・ローカル手順との差異のコメント明記を確認した
- [ ] 品質ゲート（スクリプト＝レベル2以上／CI 定義＝レベル3）に応じた評価ノードを起動した（8.7参照）
- [ ] 5章の確認手順（該当する回帰テストを含む）を実行し、8.6に沿って索引更新の要否を確認した

## 9. ブランチ運用・リリース手順

本章は、資材の変更をどのように分離された作業単位（ブランチ）で進め、いつ・どのようにバージョンを
更新するか（リリース手順）の正本です。CLAUDE.md の一次受け振り分けは「どのフロー（`/hw:request`
か直接対応か）で対応するか」を定めますが、本章はその後の実際の git 操作の進め方を定めます。

### 9.1 ブランチ運用

- ブランチは2本立てです。**`main`** はリリース確定・配布ライン、**`develop`** は統合ラインです。
  GitHub 上の既定ブランチはプラグイン配布が既定ブランチ参照であるため **`main` のまま**とします
  （`develop` へは変更しません）。
- **main・develop への直接コミットはいずれも禁止**します（例外はありません。リリース準備コミット
  も `release/v<version>` ブランチ上で作成し、`develop` 向け PR で統合します。9.2参照。裁定
  2026-08-03）。資材の変更はすべて作業ブランチ上で行います。GitHub 側でも `develop` に PR 必須の
  ブランチ保護（リポジトリルール）が適用されており、直接 push は拒否されます。
- 作業ブランチは **`develop` から分岐**します。ブランチ名は変更対象で使い分けます。
  - 資材（エージェント・コマンド・スキル・スクリプト・CI 定義・文書）の変更: `feature/<topic>`
  - 開発環境まわり（Dev Container・エディタ設定等）の変更: `chore/<topic>`
  - リリース準備（version 更新・CHANGELOG 追記の1コミット。9.2参照）: `release/v<version>`
    （例: `release/v0.6.0`）
- **1ブランチ = 1案件**を基本とし、当該ブランチで同時に作業するセッションは1つに限ります。
  複数案件を並行して進める場合は、`git worktree` で作業ツリー自体を分離してください。トークン
  上限などによりセッションを跨いで同一案件の作業を再開する場合は、新規ブランチを切らず同じ
  ブランチを継続して使用します（セッションを跨いだ再開の設計背景は
  [DESIGN.md](DESIGN.md) 1.3参照）。
- 作業ブランチの PR は **`develop` 向け**（`gh pr create --base develop` 等で base を明示）と
  し、**squash マージ**（または事前に1コミットへ整えたうえでのマージ）とします（従来どおりの
  規約です）。コミット件名は Conventional Commits に準拠させます。`.claude/scripts/git-changelog.sh` は
  件名の `<type>: ` 記法で変更を種別分類するため、規約に沿わない件名（例: 素のマージコミット
  メッセージ）は「その他」に分類され、リリース時の変更一覧作成で埋もれてしまいます。
- **push・`develop` 向け PR 作成までは、利用者の個別の明示承認を待たずに完走してよい**（裁定
  2026-08-08）。作業ブランチでの資材変更が完了し、5章の検証および品質ゲートを通過した後は、
  `git push` と上記の `develop` 向け PR の作成を、案件ごとの個別の承認を求めずに進められます。
  **ただし PR のマージは、従来どおり利用者本人による個別の明示承認が必須です（例外はありません）。**
  本項が緩めるのは push・PR 作成の承認手続きのみであり、マージの承認要件には一切影響しません。
  また、後述の issue のクローズ（`--apply` の実行に利用者の明示的な指示が必要という規定）、および
  9.2 が定めるリリース操作（リリース準備コミットの作成・push・PR 作成に利用者の明示的な指示が
  必要という規定）は本項の対象外であり、従来どおり変更しません。
- 次のいずれかに該当する場合は、push・PR 作成の前に**必ず停止**し、利用者へ判断を委ねます。該当
  するかどうか判断に迷う場合も、進めず止まる側に倒してください。
  1. 品質ゲートが未収束・不合格である（差し戻し上限到達、未実施〔実行上限等で評価ノードを
     起動できなかった場合を含む〕、検証コマンドがエラー・警告のまま、のいずれかに該当する）
  2. スコープ逸脱・裁定外の変更が差分に含まれる、または当初の裁定から逸脱している
  3. 秘密情報の混入疑いがある、または不可逆操作（履歴改変・強制 push・削除等）を伴う
  4. リリース操作（9.2 が定める version・CHANGELOG・タグ・リリース PR に関する操作）、または
     `README.md`・`LICENSE` 等、利用者・第三者へ直接向けた公開文書への変更を伴う
- `develop` から `main` への統合は、squash マージではなく 9.2 が定めるリリース手順（develop→main
  のリリース PR・マージコミット方式でのマージ）でのみ行います。
- 複数ブランチが並行し、後からマージする側は、マージ前に `develop` を取り込み（`rebase` 推奨）、
  5章の検証手順を再実行してからマージしてください。
- `commands/show-org.md` はスクリプト生成物（1.3・2.6参照）のため、競合が起きても手動マージで
  解消せず、`bash .claude/scripts/generate-show-org.sh` を実行して再生成してください。
- `develop` 向け PR のマージ時には、`.github/workflows/delete-merged-branch.yml` がリモートの
  作業ブランチを自動削除します（対象は `develop` を base とするマージ済み PR の head ブランチ
  のみ。main・develop 自体と fork のブランチは削除しません）。
- **issue のクローズは自動では行われません。** GitHub の closing キーワード（`Closes #<n>` 等）は
  PR が既定ブランチ（`main`）を対象とする場合にのみ解釈され、`develop` 向け PR では無視されて
  リンクも作成されないためです（GitHub 公式ドキュメント「Linking a pull request to an issue」。
  この仕様のため CI による自動クローズも成立しません。判断記録は `.hw/decisions.md` 2026-08-05 行）。
  案件完了時は `bash .claude/scripts/close-linked-issues.sh <PR番号>` で対象を確認し、
  `--apply` を付けて実行してクローズします（既定は dry-run。`--apply` は実行前に y/N 確認を求め、
  **AI エージェントの実行を含む非対話環境では `--yes` の併用が必須**です〔未指定なら何も操作せず
  中止します〕。`--yes` はプロンプトに応答できない実行環境のための指定であり、**利用者の承認を
  代替するものではありません**。issue のクローズは利用者に通知が飛ぶ外向きの操作であるため、
  エージェントが `--apply` を実行してよいのは利用者の明示的な指示がある場合に限り、指示がない
  ときは dry-run の結果（対象一覧）を提示して指示を仰いでください。マージ後は速やかに実行して
  ください。PR 本文はマージ後も編集可能で、本スクリプトは実行時点の本文を評価するためです。
  対象は本文に `Closes`/`Fixes`/`Resolves`
  ＋ `#<番号>` を書いた同一リポジトリの未クローズ issue に限られ、コードブロック・引用・
  インラインコード内の記述、他リポジトリ参照、PR 番号への参照は対象外です）。**issue の一部しか
  対応しない PR では `Closes` ではなく `Refs #<n>` 等**（クローズを伴わない参照）を使ってください。
  対応内容のサマリコメントは、スクリプトが投稿する定型コメントとは別に従来どおり投稿します。
- マージ済みブランチの後始末には `bash .claude/scripts/git-cleanup-branch.sh [<起点ブランチ名>]` を
  使います。作業ブランチの起点は `develop` であるため、**引数省略時の既定は `develop`**
  です（`bash .claude/scripts/git-cleanup-branch.sh` のみで `develop` 起点の掃除ができます）。
  `main` 等 `develop` 以外を起点にしたい場合のみ第1引数で明示してください
  （`bash .claude/scripts/git-cleanup-branch.sh main`）。VSCode からは `.vscode/tasks.json` 経由
  でも実行でき、入力欄（既定値 `develop`）でブランチ名を上書きできます。指定した起点
  ブランチを最新化したうえで、リモート追跡がなくなった（上記ワークフローによる自動削除、
  またはマージ済みで削除済み）ローカルブランチを一括削除します。
- `main` は [GitHub のブランチ保護](https://docs.github.com/ja/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches)
  （PR 必須・直接 push 禁止）を有効化することを推奨します。適用は GitHub 側の設定操作であり、
  利用者本人が行います（本プラグインは適用操作を代行しません）。ブランチ単位でマージ方式を制限
  できる GitHub の機能（ルールセット等）が**利用できる場合は**、`main` 向け PR の squash／rebase
  マージを制限し、9.2 のリリース PR squash 禁止（マージコミット方式のみ許可）を技術的にも担保
  することを併せて推奨します。利用できない場合は、リリース PR のマージ担当者による人手確認に
  依存する残存リスクとして認識してください（詳細は [DESIGN.md](DESIGN.md) 2.32「既知の残存
  リスク」参照）。

#### 1章「1コミット=意味的に一体の変更のまとまり」との整合

1章が定める「1コミット = 意味的に一体の変更のまとまり」という基本単位は、ブランチ内の作業粒度
としてそのまま維持します。本節が定めるのはそのコミット（群）を `develop` へ統合する際の扱いであり、
両者は矛盾しません。ブランチ上で（1資材単位、あるいは複数資材が一体となった単位で）複数コミット
に分かれていても、squash マージ（または1コミットに整えたうえでのマージ）により、**`develop` 上の
履歴では「1コミット = 1案件」** になります。1章の基本単位は「何を1つの変更とみなすか」の粒度、
本節は「`develop` の履歴上でその変更がどう記録されるか」の粒度であり、適用対象が異なります。
`develop` から `main` への統合はリリース単位のマージコミット（9.2参照）であるため、`main` 上の
履歴の粒度はさらにリリース単位に集約されます。

### 9.2 リリース手順（develop→main のリリース PR 方式）

- `.claude-plugin/plugin.json` の `version`・`CHANGELOG.md` は、資材変更の作業ブランチ上では
  更新しません。リリースは作業ブランチでの変更とは独立した作業として、**`develop` から分岐した
  リリース準備ブランチ `release/v<version>` 上で一括して行います**（version 更新・CHANGELOG 追記を
  1コミット〔リリース準備コミット〕にまとめ、`develop` 向け PR〔リリース準備 PR〕で統合します。
  `develop` への直接コミットは行いません。9.1参照）。
- **リリース発火基準**: CHANGELOG に書く価値のあるまとまりができた区切りで実施しますが、実施の
  要否はまず変更が及ぶ資材の面で判定します。**利用者面資材（`agents/`・`commands/`・`skills/`・
  `.claude-plugin/plugin.json`・`README.md`）に変更が及ぶ場合に限りリリース（バージョン更新）を
  発火**します。保守面資材（`.claude/`〔保守用スクリプト `.claude/scripts/` を含む〕・`.github/`・
  `CONTRIBUTING.md`・`DESIGN.md`・`DEVELOPMENT.md`・`CLAUDE.md`）のみの変更はリリースの契機とせず、
  次に利用者面資材が変更されるリリースへ同乗させます。「バージョンアップ不要」は「リリース発火
  なし」を意味するのみで、当該コミット自体は通常どおり `develop` へマージされる点に注意してください。
  利用者面・保守面が混在する変更は、リリース側（利用者面）として扱います。判定対象範囲は**直前
  タグから `develop` の HEAD まで**です（判定はリリース準備コミット作成前の `develop` の HEAD に
  対して行います）。この利用者面／保守面の区分は、1.10 の配布対象／1.11 の2分類と同一の基準
  （利用側リポジトリで意味を持つか）に基づいています。ただし列挙の粒度は本節の目的（リリース発火の
  要否判定）に応じて異なり、完全一致ではありません。`.hw/` は gitignore 済みでコミットに現れないため
  本節の列挙対象に含めていません。`.vscode/`・`.devcontainer/` は本節の保守面資材リストには明示
  列挙していませんが、下記フォールバック規定により実質的に同じ扱い（該当なし＝保守面扱いが既定）
  になるため、1.11 との判定結果は一致します。
- **フォールバック規定**: 利用者面・保守面のいずれのリストにも該当しないパス（`LICENSE`・
  `.gitignore`・`.vscode/`・`.devcontainer/` 等）のみの変更は、保守面扱い（リリース発火なし）を
  既定とします。ただし利用者への配布内容・利用条件に実質的な影響が及ぶ場合（例: LICENSE のライセンス
  種別変更）や判定に迷う場合は、リリース側（発火）に倒します（バージョン種別判定の「迷えば重い側」と
  同じ考え方です）。
- **バージョン種別の判定**: semver の考え方・改訂の背景は [DESIGN.md](DESIGN.md) 2.10・2.31 を
  参照してください。判定基準そのものは本節（9.2）が正本です。本リポジトリでは次を目安とします。
  - MAJOR: 呼び出しインターフェースを破壊する変更（エージェント/スキル/コマンドの削除・改名、
    必須 frontmatter キーの追加や意味変更等。1.3参照）（0.x の間は MINOR でも可）
  - MINOR: 呼び出しインターフェースは不変でも、利用者が変更を意識する必要がある場合（新しい
    機能・使い方の追加、既存呼び出しで観察できる振る舞い・出力・確認フローの変化等）
  - PATCH: 利用者が変更を意識する必要がない場合（意図した挙動へ戻すバグ修正、出力文言の微修正等）
  - 判定に迷う場合は重い側（MINOR）に倒します（品質ゲート判定と同じ考え方です）。操作的な判定
    テストとして、「CHANGELOG に利用者向けの行として書くべき内容があるか」を MINOR/PATCH の
    代理指標として用いてください（該当すれば MINOR 側、しなければ PATCH 側という目安であり、
    機械的な決定手続きではありません）。
- **変更一覧の作成**: `develop` 上で `bash .claude/scripts/git-changelog.sh <前回タグ> HEAD` を実行して
  作成します（引数省略時は直前タグから HEAD までを対象にし、タグが1つも無ければ全履歴を対象に
  します）。`CHANGELOG.md` 自体の新規作成は初回リリース時に行います（1.3の記載と整合します）。
- **リリース手順**: 利用者の明示的な指示を起点に、次の順で進めます。
  1. `develop` から `release/v<version>` ブランチを作成し、その上でリリース準備コミット
     （version 更新・CHANGELOG 追記の1コミット）を作成する。
  2. `release/v<version>` を push し、`develop` 向け PR（**リリース準備 PR**）を作成する。
     マージは利用者が行う（9.1の作業ブランチ PR と同じ squash マージ扱い。マージ後のリモート
     ブランチは `delete-merged-branch.yml` により自動削除される）。
  3. リリース準備 PR のマージを確認したうえで、`develop` → `main` の PR（**リリース PR**）を
     作成する。
  4. 利用者がリリース PR を**マージコミット方式**でマージする（＝リリース確定）。リリース PR の
     マージは利用者本人のみが行い、本プラグインが自動でマージすることはありません。
  5. マージを契機に CI（`.github/workflows/tag-release.yml`）が、リリース PR のマージ状態と
     マージ方式（GitHub が記録したマージコミットの親コミット数）を機械的に検証する。squash／rebase
     マージの疑いがある場合（親が1つの場合）はタグを打たずジョブを失敗させる。検証に通れば、
     タグ `v<version>` を**`develop` 側のリリース準備コミット（マージコミットの第2親）に**自動
     付与し、push する。リリース PR はマージコミット方式でマージするため、このコミットはマージ後
     `develop`・`main` 双方の履歴上の祖先になり、back-merge（`main` から `develop` への打刻用の
     取り込み）を別途行う必要がありません。両ブランチで `.claude/scripts/git-changelog.sh` の
     直前タグ検出が同一のコミットを指し、一致します。
  6. 利用者/AI は、CI（`tag-release.yml`）の実行結果とタグ `v<version>` の作成をもって完了を
     確認する（確認の具体手順は `.claude/commands/release.md` に委譲する）。CI が失敗した場合
     （squash／rebase 検知・タグ衝突等）は原因を確認し、解消したうえで従来の手動手順（マージ方式を
     親コミット数で確認 → `develop` 側のリリース準備コミットへ `git tag v<version>` → push）で
     フォールバックする。
- **リリース PR は squash マージを禁止**します。squash マージすると `develop` 側のコミット群と
  異なる新規コミットが `main` 上に生成されるため、(a) `develop` と `main` の履歴が同一内容でも
  食い違う（**履歴乖離**）、(b) 次回以降のリリース PR で内容が同一のはずの変更が差分として
  検出され解消不要なコンフリクトが生じる（**偽コンフリクト**）、(c) タグが指す `develop` 側の
  コミットが `main` の履歴上の祖先にならず、`git describe`・`.claude/scripts/git-changelog.sh` の
  直前タグ検出が `develop`・`main` の間で食い違う（**タグ検出破綻**）、という3つの問題を招く
  ためです（裁定 2026-08-02）。
- 本プラグインは GitHub パス配布（マーケットプレイス非公開）のため、`main` は常に動く状態を
  保つ必要があります。5章の検証手順が必須であることがこれを担保します。バージョン番号は
  利用を制限する取得ゲートではなく、変更を伝える目印として機能します。
- **リリース操作（リリース準備コミットの作成・push・PR 作成〔リリース準備 PR・リリース PR とも〕）
  は、利用者の明示的な指示があるときに限り実施します。** 変更一覧のドラフト作成等の準備までは自律的に進めて
  よいものの、実際のコミット作成・push・PR 作成は利用者の指示を待ちます。タグ付与はリリース PR
  のマージ後に CI（`tag-release.yml`）が自動実行するため、通常は AI 側の追加操作を要しません。
  CI 失敗時の手動フォールバック（タグ打刻・タグ push）についても、他のリリース操作と同様に
  利用者の明示的な指示があるときに限り実施します。

背景・理由は [DESIGN.md](DESIGN.md) 2.23・2.32 を参照してください。

### 9.3 チェックリスト
- [ ] 資材変更は `develop` から分岐した作業ブランチ（`feature/<topic>` または `chore/<topic>`）
      で行い、`main`・`develop` への直接コミットをしていない（リリース準備コミットも
      `release/v<version>` ブランチ経由。例外なし）
- [ ] 1ブランチ=1案件を維持し、当該ブランチで同時に作業するセッションを1つに限った（並行作業時は
      `git worktree` で分離し、セッションを跨ぐ同一案件の再開は同じブランチを継続利用した）
- [ ] 作業ブランチの PR は `--base develop` を明示して作成し、`develop` へのマージは squash
      （または1コミットに整えたうえで）とし、件名が Conventional Commits に準拠している
- [ ] push・PR 作成の前に、9.1 の停止条件4件のいずれにも該当しないことを確認した（該当時・迷う
      場合は停止して利用者へ判断を委ねた）
- [ ] 後発マージの場合、`develop` を取り込み（rebase 推奨）5章の検証を再実行してからマージした
- [ ] `commands/show-org.md` の競合は手マージせず `.claude/scripts/generate-show-org.sh` で再生成した
- [ ] PR 本文で issue を参照する場合、全対応なら `Closes #<n>`、部分対応なら `Refs #<n>` 等を
      使い分けた（9.1参照）
- [ ] 案件完了時に `bash .claude/scripts/close-linked-issues.sh <PR番号>` で対象を確認し、
      `--apply`（非対話環境では `--apply --yes`）でクローズした（自動クローズは GitHub 仕様上
      働きません。マージ後は速やかに実行します。9.1参照）
- [ ] マージ済みブランチは `bash .claude/scripts/git-cleanup-branch.sh`（既定 `develop`。VSCode タスクからも可）で後始末した
- [ ] version・CHANGELOG の更新は作業ブランチと分離し、`release/v<version>` ブランチ上でリリース
      準備コミットとして一括実施し、`develop` 向け PR（リリース準備 PR）で統合した
- [ ] リリース PR（`develop`→`main`）は squash マージせず、マージコミット方式で利用者本人が
      マージした
- [ ] タグ `v<version>` の作成は CI（`tag-release.yml`）の自動付与結果で確認した（`develop` 側の
      リリース準備コミット＝マージコミットの第2親に付与されている）
- [ ] CI 失敗時のみ、原因確認のうえで手動フォールバック（マージ方式確認 → タグ打刻 → タグ push）
      を利用者の明示的な指示に基づき実施した
- [ ] リリース操作（リリース準備コミット・push・PR 作成）は利用者の明示的な指示に基づいている
