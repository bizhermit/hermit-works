# hw:init の運用

`hw:init`は、本プラグインが前提とする規律の節と設定ファイルを利用者環境へ展開する。本書はその運用上の注意を記す。動作の仕様は[SKILL.md](../../plugin/skills/init/SKILL.md)を正本とし、本書には書かない。

## 展開したものの位置づけ

展開したものは初期展開物である。正本の写しではない。運用の実態に合わせて利用者が編集する前提のものであり、編集は`hw:init`の想定内である。

原稿（`plugin/skills/init/assets/`配下）は初期展開物の正本であって、展開先の正本ではない。展開先に加えられた変更は、再展開でも維持される。

## 原稿の更新を取り込む

`hw:init`は、記録されたバージョンと`plugin.json`のバージョンが一致する場合、展開も検証も行わない。原稿を直したときは、パッチバージョン以上を上げる。上げなければ、再実行しても展開先は古いままになる。

バージョンを上げて再実行すると、原稿と相違のある節が報告される。上書きは自動では行われないため、取り込みたい節を指示する。上書きすると、その節に加えられていた変更は失われる。必要な変更は利用者の手で入れ直す。

## 使用範囲とgitの扱い

`project`は、規律をチーム標準としてリポジトリで共有する場合に選ぶ。展開先は`CLAUDE.md`であり、`.hw/settings.json`と`.hw/conventions.md`がgit管理下に入る。展開先・`.gitignore`・`.hw/settings.json`・`.hw/conventions.md`のコミットは利用者が行う。

`local`は、個人利用でgit管理外に置く場合に選ぶ。展開先は`CLAUDE.local.md`である。`.hw/`配下をgit管理外にするには、`common`の`.hw/*`を打ち消す否定パターン（`project`の一覧）が`.gitignore`から除かれている必要がある。`.gitignore`は`.git/info/exclude`より優先されるため、否定パターンが残っている限り`.git/info/exclude`側では打ち消せない。`hw:init`はこの除去を行う。

`local`を選んだ場合も、`.gitignore`はリポジトリで共有されるファイルである。追記・除去された行をコミットするかどうかは利用者が決める。

## 節ごとの展開先は揃わないことがある

使用範囲が`local`でありながら`CLAUDE.md`に置かれている節は、利用者がその節だけ共有すると判断した結果として扱う。`hw:init`は配置を揃えず、その節の展開先を`CLAUDE.md`のままとする。結果として、展開先が`CLAUDE.md`の節と`CLAUDE.local.md`の節が混在しうる。これは異常ではない。

## 反映のタイミング

展開先の内容が効くのは次セッションからである。実行したセッションには反映されない。反映は、次セッションで`/context`を実行し、Memory filesに展開先が載っていることで確認する。
